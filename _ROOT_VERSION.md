---
type: version-log
tags:
  - AIMindVault
  - root
updated: 2026-09-14
---

# Root Version Log

멀티볼트 루트 레벨 변경 이력. `.claude/`, `.antigravity/`, 루트 설정 파일 등의 변경을 추적한다.

## 배포 반영 상태 컬럼 (2026-04-15 도입)

- **Korean**: `C:\SellingVault\Korean\AIMindVaults` 배포 반영 여부
- **English**: `C:\SellingVault\English\AIMindVaults` 배포 반영 여부
- 값: `✅` 반영 완료 / `🕓` 대기 / `–` 해당 언어 배포에 무관 (원본 전용 또는 custom 규칙)
- 2026-04-15 이전 항목은 스키마 도입 전이므로 공란으로 둔다.

## 신규 스키마 (2026-04-15 이후)

| 버전 | 날짜 | Korean | English | 변경 내용 |
|------|------|--------|---------|----------|
| R223 | 2026-09-14 | ✅ | 🕓 | **디스코드 무인 인박스를 배포본에 넣는다** (사용자 지시 — 가족에게 공유할 임시 한국어 배포). 종전엔 키트가 `Project_AIMindVaults/Contents/Project/scripts/` 안에만 있어 배포 대상이 아니었고, `run_trigger.py`·`run_agent_tasks.py` 가 그 루트 기준 경로를 상수로 박고 있어 옮기면 깨졌다. **① RUNNER 를 스크립트 자기 위치 기준으로** (원본·배포본 양쪽에서 같은 파일이 돈다). **② `deploy` 대상 항목에 `dest` 지원** (CoreHub `config.js`·`deploy-dist.js`, `bump-version --broadcast` 완료) — `scripts/discord`·`scripts/daemon` 을 `_tools/agent-daemon/` 으로 미러. 개인 채널 ID 가 든 `discord_config.json`, 커뮤니티 서버 운영 스크립트 4종, 개발학습 정리 지시서는 제외하고 `discord_config.example.json` 을 대신 넣었다. `__pycache__` 전역 제외. **③ 개발학습 정리 지시서가 없는 설치에서는 digest 를 띄우지 않는다** — 안 막으면 러너가 exit 2 로 죽고 로그가 안 남아 5분마다 시작 메시지만 쌓인다 (R217 과 같은 사고). 처음엔 `digest_due` 안에 넣었다가 배포본 selftest 가 깨져 `main` 으로 옮겼다. **④ 지시서 3종의 절대경로를 `<scripts>` 로** (`inbox-task.md`·`agent-task.md`·`_AGENT_TASKS/README.md`). **⑤ `docs/discord-inbox.md` 신규** — 배포본 경로 기준 설치·사용 가이드, README 표에 등록. 배포본 전용 파일은 직접 편집: `CLAUDE.md § 인박스` 추가 · 루트 `_INBOX.md` 빈 템플릿 · `.gitignore` 에 `discord_config.json`. **검증**: 원본·배포본 양쪽 selftest 3종 통과, 배포본에서 `Run-AgentTask.ps1 -DryRun` 이 `_tools/agent-daemon/...` 경로로 명령을 조립. 배포본 커밋·푸시는 사용자 승인 대기. 작업자: Claude. |
| R222 | 2026-09-13 | – | – | **`blender-mcp.md` 에 문서용 참조 이미지 규칙 1줄 추가** (사용자 승인). 2026-09-10 규칙은 "볼트에는 외부 파일 경로를 남긴다" 였는데, JissouGame 원작 관찰 노트에 그대로 따르니 노트를 열어도 이미지가 안 보이고 디바이스를 옮기면 경로가 죽었다. **참조 이미지는 작업 데이터가 아니라 문서의 일부** 로 규정 — 축소본은 볼트 `Contents/Project/_attachments/`, 고해상도 원본은 작업 루트 레퍼런스 팩. 경계는 볼트=근거·맥락, 작업 루트=실행용 자료. 정본 `.agents/rules/custom/Blender/` 수정 후 agents-sync 로 `.claude/`·`.codex/` 전파. 작업자: Claude. |
| R221 | 2026-09-12 | – | – | **`GamePublishing` 볼트 신설** (`Vaults/Domains_Business/GamePublishing/`). 게임 스토어 등록·수수료·정산·심의 지식을 담을 볼트가 없었다. Funding 이 가장 가까웠으나 그 볼트의 축은 **자금 조달**이라 유통과 성격이 다르다 — 분리 기준은 "플랫폼과 기관을 상대하는 일인가". BasicContentsVault 클론 (hubId=default), Contents/Domain 을 Store·Rating·Legal·Finance 로 구성. Steam·Google Play 등록·정산 노트 2건 작성 (`BAD=0`, `TAG_VIOLATIONS=0`). 루트 CLAUDE.md 키워드 매핑 + `_STATUS.md` 레지스트리 등록, 인덱스 빌드 완료. **obsidian.json 등록은 미수행** — 등록 시점의 Obsidian 실행 상태에 따라 SessionStart 훅이 자동 처리한다. **미결**: Funding 의 `20260808_인디게임_사업자등록_업종코드와_신고절차` 가 신설 볼트의 Legal 축과 겹친다. 작업자: Claude. |
| R220 | 2026-09-10 | – | – | 사용자 지시로 Blender 데이터 루트를 `C:/Dev_Blender/`로 분리. JissouGame 83파일 외부 이동·해시 검증, Blender 5파일 출력 경로 갱신·의존 누락 0. 루트 `.gitignore`의 Blender 백업 제외 제거, 외부 작업 루트로 이동. Blender 정본 규칙과 에이전트 미러·볼트 진입점·핸드오프 갱신. codex / 2026-09-10. |
| R219 | 2026-09-09 | – | – | **"돌았다" 는 거짓 기록 세 가지를 없앤다** (사용자 지시). **① 잠금 SKIP 이 성공으로 읽혔다 (재현 확인)** — 러너가 잠금 발견 시 `exit 0` 을 내서, 큐가 **한 번도 실행되지 않은 작업을 `done/` 으로 옮겼다.** 실측 재현: 0초 만에 `done=1`. 러너를 **`exit 75`** (EX_TEMPFAIL) 로 바꾸고, 큐는 75 면 옮기지 않고 `todo/` 에 남긴다. 집계도 `done`/`failed`/**`locked`** 3값 — `None` 을 성공이나 실패로 뭉개면 둘 다 거짓이다. 디스코드 쪽도 75 면 ❌ 대신 **⏳ 를 떼어** 다음 틱이 다시 집게 한다 (`discord_io.unreact` 신설). **② 태그 오타가 모델 ID 로 나갔다** — 문서는 "모르는 값은 무시" 인데 구현은 분류 안 되는 토큰을 전부 `-m` 으로 넘겨 `[opusss]` 하나로 회차가 죽었다 (codex 지적, 실측). 사용자 결정으로 **아는 값 목록(`MODELS`)을 두고, 하나라도 모르면 실행하지 않고 ⚠️ 를 단다.** ⚠️ 는 처리 표식이라 5분마다 같은 오타를 다시 잡지 않는다. 로그에 틀린 토큰과 쓸 수 있는 값 전체를 찍는다. **반만 알아듣고 도는 것이 제일 나쁘다.** **③ 그 과정에서 더 큰 것을 잡았다 — cp949 콘솔이 로그 한 줄에 죽는다.** 새로 넣은 줄의 em-dash(`—`) 가 `UnicodeEncodeError` 를 내 **프로세스가 exit 1 로 끝나고 집계 줄도 못 찍었다.** 특정 문자 문제가 아니라 한글 로그 전체가 지고 있던 위험이라, 세 스크립트(`run_agent_tasks`·`run_trigger`·`poll_inbox`) 진입점에서 stdout/stderr 을 UTF-8 로 고정했다. **④ `--file` 을 `todo/` 안으로 제한** — 실행 후 그 파일에 메타를 붙여 **옮기므로**, 오타 한 번에 진짜 노트가 큐 폴더로 끌려갔다. **⑤ `-DryRun` 을 진짜 읽기 전용으로** — 종전엔 출력 전에 로그 폴더 생성·SKIP 로그 기록·오래된 잠금 삭제가 돌았고, `-Branch` 를 같이 주면 **출력 전에 브랜치를 만들고 checkout 까지** 한 뒤 복귀 경로를 안 탔다. 실측: 로그 파일 수 40→40, 오래된 잠금 생존. **남긴 것**: `TimeoutMinutes` 는 실행 제한이 아니라 잠금 나이 비교라, 90분 넘은 회차의 잠금을 다음 진입이 훔칠 수 있다. 제대로 막으려면 heartbeat 가 필요하고 급조하면 멀쩡한 회차를 죽인다 — 현재 회차가 5~15분대라 여유가 크다고 보고 미뤘다. **검증**: 잠금 SKIP 재현 스크립트가 수정 전(★재현) / 후(todo 유지·`locked=1`) 를 대조 · selftest 양쪽 +6항목 (오타 unknown · 아는 모델 통과 · YAML 인라인 주석 · 공백 없는 `#` 는 값의 일부) · `--file CLAUDE.md` 거부 실측. 작업자: Claude (메인 PC). |
| R218 | 2026-09-09 | – | – | **개발학습 지시서·게이트 정정 + 오토 큐로 받은 지적 3건 반영** (사용자 지시 3건). **① 지시서 § 2-2 "Bash 한 셸로 끝낸다" 를 현실에 맞춘다** — 12·14·15·16차 **4회 연속** 성립하지 않았다. `rg` 는 Bash 에만 있고, **무인 회차의 Bash 는 `python <스크립트>` 가 승인 대기로 막힌다** → 두 셸을 쓸 수밖에 없다. 대화형에서는 한 셸로 되지만 이 문서는 무인 기준이라는 것까지 명시. 4회차 연속 어긋난 지시는 지시가 틀린 것이다. **② `Run-Gates.py` `frontmatter:video-keys` 오탐 수정** — `^source:\s*(\S+)` 의 `\s*` 가 **줄바꿈을 넘어가 다음 줄의 `-` 를 URL 로 집었다.** 그래서 리스트형 `source:` (출처 여럿을 묶은 웹 가이드) 가 영상 노트로 오인돼 3회차 연속 FAIL. `[ \t]*` 로 같은 줄만 보게 하고, 매치 실패(=리스트형)는 영상 노트가 아니므로 건너뛴다. 실측: 전 볼트 대상 검사 1056 → 1051개, 리스트형 5건이 정확히 빠졌다 (문제의 `20260831_AI_Image_Prompt_Writing_Guide.md` 포함). 함정 재발: heredoc 이 `\\t` 를 접어 **리터럴 탭이 소스에 박혔다** → Write 도구로 재작성 (R206 과 같은 원인). **③ `_AGENT_TASKS` 오토 큐로 재귀 가드 실측 + 설계 검토를 Codex 에게 위임** (큐 세 번째 실사용, effort high). **재귀 가드가 실제로 작동했다** — Codex 가 `todo/spawned_test.md` 를 만들었고 러너가 `RECURSION_BLOCKED` 로 잡아 지웠다. **Codex 가 안 물어본 것에서 찾아온 지적 5건을 코드로 직접 재현했고 전부 사실이었다.** 그중 **코드가 자기 문서와 모순되는 3건을 수정**: (a) **`parse_task` 가 YAML 인라인 주석을 안 뗐다** — `README` 예시(`agent: codex  # 설명`)를 그대로 베끼면 **agent 가 미인식돼 claude 로 가고** model 은 `"gpt-6-astra  # 선택"` 이라는 쓰레기 문자열이 됐다. 앞에 공백이 있는 `#` 만 주석으로 떼고 README 예시에서도 주석을 뺐다. (b) **재귀 스윕이 확장자를 안 봤다** — README 는 "새 `.md`" 인데 구현은 무엇이든 지워, 실행 중 사람이 넣은 파일까지 작성자 구분 없이 삭제할 수 있었다 → `.md` 한정. 겸해서 **스윕을 `finally` 로** 옮겼다 (자식이 예외로 죽으면 안 돌았다). (c) **디스코드 전송에 `allowed_mentions` 가 없었다** — `discord-bot.md § 6` 이 요구하는 방어인데 이 봇에만 빠져 있었고, 로그 보고가 사용자 지시 **원문을 그대로 복사**하므로 지시의 `@everyone` 이 실제로 발사될 수 있었다 → `{"parse": []}` 고정. **내 종전 서술 정정**: "Codex 에는 deny 에 해당하는 개념이 없다" (R214·R217) 는 **부정확하다** — `codex execpolicy` 와 `hooks stable true` 가 존재한다 (Codex 실측). 정확히는 **우리가 그것을 연결하지 않았다**. 또 "Windows Codex hooks 비활성" 이라는 저장소의 옛 전제도 재사용할 수 없다. **미결(사용자 결정 필요)**: 잠금 SKIP 이 exit 0 이라 **한 번도 안 돈 작업이 `done/` 으로 간다** · 태그의 모르는 값이 문서(무시)와 달리 **모델 ID 로 전달돼 오타가 회차를 죽인다** · `--file` 이 todo 밖 경로도 이동시킨다 · `-DryRun` 이 읽기 전용이 아니다 · `TimeoutMinutes` 가 실행 제한이 아니라 잠금 나이 비교에만 쓰인다. **검증**: selftest 2종 통과 · 5건 재현 스크립트가 수정 전후를 대조 · 게이트 전 볼트 실측. 작업자: Claude + Codex (오토 큐). |
| R217 | 2026-09-09 | – | – | **띄우기 사고 정정 + codex 가 안 받는 생각수준 차단** (2건). **① 개발학습 회차가 안 떴다** — R216 에서 `DETACHED_PROCESS` 로 띄웠는데 **그 플래그면 powershell.exe 가 시작조차 못 한다** (콘솔 호스트라 콘솔이 필요하다). 로그가 안 생기니 "마지막 실행" 이 갱신 안 돼 **5분마다 시작 메시지만 6번** 쌓였다. 중간에 "두 플래그가 상호 배타라 오류 87" 이라고 적었는데 **그것도 틀렸다** — 같이 줘도 `CreateProcess` 는 성공한다. 실측: `DETACHED_PROCESS` → 파일 쓰는 명령도 안 돎 · `CREATE_NO_WINDOW` → 정상 · 둘 다 → 안 돎. **`CREATE_NO_WINDOW` 하나로 고쳤고, 부모가 먼저 끝나도 자식이 끝까지 도는 것을 따로 실측**했다. **메시지를 띄운 뒤에 보내도록** 순서도 바꿨다 — 먼저 보내면 띄우기가 실패해도 메시지는 나가서 같은 사고가 반복된다. 실측: `digest=spawned` → 15초 뒤 로그 생성 → 다음 틱 `digest=0h`. 구 스케줄 작업 `AIMV-DevLearning-Digest` **Disabled**. **② codex 는 `minimal` 을 거부한다** — 오토 큐로 코덱스에게 자기 환경을 재게 해서 확인 (`_AGENT_TASKS` 첫 실사용). Astra 지원값은 `low|medium|high|xhigh|max` 이고 `minimal` 은 서버가 **HTTP 400 unsupported_value 로 회차를 죽인다** (조용히 기본값으로 안 간다 — 코덱스가 두 번 재현). 우리 `EFFORTS` 는 Claude 기준이라 그 값이 통과하고 있었다. **러너에서 codex 일 때만 `minimal`→`low` 로 내린다** — 두 큐가 같은 러너를 타므로 한 곳만 고치면 된다. Claude 는 그대로 `minimal` 을 받는다. **부수 확인**: 모델과 생각수준은 **독립 축**이라 `medium` 이 작은 모델을 고르지 않는다 · 사용자가 기억한 "5.6 sol" 은 실재하는 **모델** (`gpt-5.6-sol`, API 가 기준 Astra 의 약 1/2.5 — 단 **Codex 구독 차감률은 미확인**) · 무인 기본은 이미 `low` 라 **바꿀 것 없음** (코덱스 자신의 권고도 low 유지). 작업자: Claude (메인 PC) + Codex (오토 큐 회차). |
| R216 | 2026-09-09 | – | – | **개발학습 정리를 벽시계에서 떼어 5분 트리거에 얹는다** (사용자 제안). 종전 `AIMV-DevLearning-Digest` 는 매 2일 **04:37** 인데 그 시각 PC 가 자고 있어 **09-07·09-09 2회 연속 미실행** (`NumberOfMissedRuns=2`, 전원 이벤트 로그로 확인 — 22:2x 절전 → 오전 늦게 복귀). 작업에 `WakeToRun=True` 가 걸려 있어도 전원 옵션 `RTCWAKE` 가 **"중요 절전 모드 해제 타이머만"** 이라 사용자 작업은 PC 를 못 깨운다. **시각이 아니라 경과 시간으로 바꾼다** — `run_trigger.py` 가 매 5분 틱에서 마지막 회차 이후 **48시간**이 지났으면 띄운다. 깨어 있을 때만 도니까 구조적으로 못 놓친다. **상태 파일을 새로 만들지 않았다** — 러너가 남기는 `.vault_data/logs/devlearning-digest_*.log` 의 mtime 이 이미 "마지막 실행" 이다. 파일을 하나 더 두면 로그와 어긋날 때 어느 쪽이 참인지 판정할 근거가 없다. **붙잡지 않고 띄운다** (`CREATE_NO_WINDOW`) — 스케줄 작업이 `IgnoreNew` 라 회차 끝까지 기다리면 그 사이 5분 틱이 전부 버려져 **인박스 ▶️ 가 그동안 죽는다**. 겹침은 러너의 `devlearning-digest.lock` 이 이미 막는다. **인박스가 빈 틱에서만** 본다 — 사람이 시킨 것이 먼저고 두 회차가 같이 도는 것도 피한다. `--dry-run` 은 안 띄운다. 시작 시 로그 채널에 경과 시간 보고. **알려진 한계**: 실패한 회차도 로그를 남겨 48시간 동안 재시도하지 않는다 (종전 스케줄 작업도 결과 무관 2일 간격이라 성질은 같다) — 필요해지면 그때 넣는다. **검증**: selftest +5항목 (로그 없음/방금/47h/49h/타 작업 로그 무시) 통과 · `--dry-run` 이 `digest=due` 출력하고 안 띄움 (실측 111.8시간 경과). **남은 결정**: 구 스케줄 작업 `AIMV-DevLearning-Digest` 비활성화 — 자동화 변경이라 사용자 승인 대기. 작업자: Claude (메인 PC). |
| R215 | 2026-09-09 | – | – | **디스코드 채널 3분할 — 지시 하나, 로그는 에이전트별로** (사용자 지시). `#inbox`→**`#task-queue`**, `#log`→**`#log-claude`** + **`#log-codex`** 신설. 이름만 바꾸는 것은 코드 변경이 0 이다 (설정이 이름이 아니라 **ID** 를 들고 있다) — 실제 작업은 로그 라우팅이었다. **지시 채널은 나누지 않았다**: 나누면 큐가 둘이 되어 `run_trigger` 의 "최근 30건에서 ▶️ 찾기" 순서가 갈리고, 모델·생각수준은 어차피 태그로 줘야 해서 `#task-queue-codex` + `[opus]` 라는 모순이 생긴다. 로그 분리는 그 비용이 없다. **구현은 가상 키 하나로** — `discord_io.channel()` 이 `'log'` 만 `AIMV_LOG_CHANNEL` 환경변수로 풀어 준다 (없으면 `log_claude`). 러너가 태그를 읽은 **직후** 그 값을 넣으므로 "실행 시작" 보고부터 이미 맞는 채널로 가고, 환경변수가 자식 (PowerShell → CLI → 에이전트) 까지 상속돼 **지시서 § 7 의 `dio.send(cfg, 'log', ...)` 를 한 글자도 안 고쳤다.** `run_agent_tasks.py` 도 같은 변수를 넣어 `_AGENT_TASKS` 회차가 같은 규칙을 탄다. 채널 키 `inbox`→`queue` 리네임 (호출부 8곳). **검증**: `--dry-run` 이 `poll_inbox.py` 의 누락 키 2곳을 잡았다 (`run_trigger` 가 예외를 삼켜서 조용히 적재만 멈출 뻔했다) · selftest 2종 통과 · **실채널 발사 2건으로 라우팅 실측** (`AIMV_LOG_CHANNEL=log_codex` → `#log-codex`, 미설정 → `#log-claude`). 문서 갱신: 설치 가이드 (채널 3개·config 키·체크리스트·태그 사용법) · `_INBOX.md` · 루트 `CLAUDE.md` · `inbox-task.md`. 작업자: Claude (메인 PC). |
| R214 | 2026-09-09 | – | – | **Codex 도 무인으로 돌린다 + 긴 지시용 `_AGENT_TASKS/` 신설 + 회차마다 모델·생각수준 선택** (사용자 지시 3건). **① 러너를 두 CLI 공용으로** — `Run-ClaudeTask.ps1` → **`Run-AgentTask.ps1`** (`git mv`, 이력 보존). Codex 도 돌리는데 이름이 `Claude` 면 3am 에 헷갈린다. `-Agent claude|codex` · `-Model` · `-Effort` 신설. claude 는 `--model`/`--effort`, codex 는 `-m`/`-c model_reasoning_effort=` 로 번역해 넘긴다 (양쪽 CLI 도움말로 실측). 지시서·프로파일은 두 에이전트 공용. 호출부 2곳 갱신 — `run_trigger.py` 상수 + **스케줄러 `AIMV-DevLearning-Digest` 인자**. **Codex 권한은 사용자 결정으로 Claude 와 동급** (`--dangerously-bypass-approvals-and-sandbox`) — 우려를 한 번 말했고 재확인받아 진행. **다만 Codex 에는 `profiles/*.json` deny 겹이 없다** (Codex 에 그 개념이 없음) → 방어가 지시서 문구 하나뿐임을 러너 주석·지시서 § 7 에 명시. **② 인박스 태그** — 지시 맨 앞 `[codex]` · `[opus:xhigh]` · `[codex:gpt-6-astra:high]`. **순서를 강제하지 않는다** (`[high:codex]` 도 같게 읽는다) — 폰에서 한 손으로 치는 것이라 순서를 외우게 하면 안 쓴다. 판정은 값으로: 에이전트 낱말/생각수준 낱말/나머지는 모델. **모르는 값은 조용히 무시하고 기본값** — 오타로 회차를 죽이지 않는다. `parse_tag` + selftest 10항목. **③ `_AGENT_TASKS/` 신설** (루트, 볼트 아님) — `todo/`·`done/`·`failed/` + `README.md` 규약 + 지시서 `tasks/agent-task.md` (Claude·Codex 공용) + 큐 러너 `daemon/run_agent_tasks.py`. 인박스가 "폰에서 한 줄" 이면 여기는 "PC 에서 긴 지시" — frontmatter 로 `agent`·`model`·`effort` 를 준다. 실행 후 `done/`·`failed/` 로 이동하며 **원본에 실행 메타를 덧붙여** 이력이 남는다. **★ 재귀 금지 (사용자 요구)** — 이 폴더의 작업이 이 폴더에 새 작업 파일을 만들면 자기가 자기를 부르는 고리가 되고 무인에서는 안 멈춘다. **두 겹으로 막았다**: 지시서 § 1 문구(약속) + **러너가 실행 전후 `todo/` 스냅샷을 비교해 새로 생긴 `.md` 를 지우고 기록**(강제). 문구만 두면 에이전트가 무시했을 때 아무것도 안 남는다. **스케줄러에 걸지 않았다** — 폴더에 파일을 떨어뜨리는 것만으로 무인 실행이 시작되면 사람이 모르는 사이에 도는 것이 된다. 자동화 등록은 별도 승인 사안 (`script-creation-approval.md`). **검증**: `Run-AgentTask -DryRun` 3조합 (claude 기본 / codex+모델+effort / claude+모델+effort) 명령줄 확인 · selftest 3종 (`run_trigger` 태그 10항목 · `poll_inbox` · `run_agent_tasks` 파싱·재귀가드 8항목) · **실제 스모크 1회 완주** — Codex 가 `AIMV_TASK_FILE` 을 읽고 `agents-sync --verify` 를 스스로 돌린 뒤 (NOOP) 결과를 파일에 적고 exit 0 → `done/` 이동, `todo/` 비움, 부수 변경 0. **남은 것**: Codex 는 자기 런타임 모델 ID·effort 를 정확히 자기보고 못 한다 (Claude 는 `claude_usage.py` 가 찍는다) — 대응물 필요 시 후속. 작업자: Claude (메인 PC). |
| R213 | 2026-09-08 | – | – | **Codex elevated 샌드박스 복구 — 원인은 `.codex` 디렉터리 소유권 하나였다** (사용자 지시 "재설치해서 샌드박스 고쳐줘" → 재설치로 안 되자 검색 프롬프트 작성 → 외부 리서치 회신 → 진단·수정). **증상**: `codex doctor` 18 ok · 1 fail (`sandbox.helpers`, `helper_unknown_error`), 실제 회차에서 `exec_command failed: Rejected("Failed to create unified exec process")` 로 **파일 읽기 도구가 아예 안 떴다.** **배제한 것 (전부 실측)**: CLI 업데이트 0.148.0→0.153.4 · 완전 재설치 (staging 잔재 `@openai/.codex-NqKMjsZC` 제거 포함) · 바이너리 누락 (헬퍼는 `codex-resources/` 에 정상 존재) · 권한 부족 (**관리자 셸에서도 동일 실패**) · npm 설치본 한정 (**데스크탑 앱 자기 바이너리도 동일 실패**) · Defender (CFA off, codex 탐지 0건). **★ 내 가설이 틀렸다** — "elevated 가 Windows Sandbox(Pro 전용) 에 의존하고 이 머신은 Home 이라 안 된다" 로 좁혔는데, OpenAI 는 **Home SKU 에서 못 쓴다는 이유로 Windows Sandbox 기반을 명시적으로 기각**했다. elevated 는 로컬 사용자 2개 생성 + 방화벽 규칙 + ACL 로 구현되고 Home 에서 지원된다. **진짜 원인**: `sandbox.log` 의 한 줄 — `deny ACE failed on C:\AIMindVaults\.codex: SetNamedSecurityInfoW failed: 5` (액세스 거부). `.codex` 디렉터리 소유자가 내 계정이 아니라 **`CodexSandboxOffline`** 이라 셋업이 자기 deny ACE 를 못 걸었다 (`.git`·`.agents`·`.claude`·루트는 전부 정상 — 어긋난 것은 `.codex` 하나). `helper_unknown_error` 는 그 한 줄을 감싼 껍데기이고 **`codex doctor` 로는 원인을 절대 못 찾는다** — doctor 는 `setup_error.json` 에 기록된 실패를 읽을 뿐이다. **조치**: 관리자 셸 1회 `icacls C:\AIMindVaults\.codex /setowner <user> /T /C` (195 파일, 실패 0). **결과**: doctor **19 ok · 0 fail** · `sandbox provisioning: complete` · 로그 `errors=[]` · `setup_error.json` 삭제 · **우회 옵션 없이 실제 회차 통과** (파일 읽기 정상). **`config.toml` 은 끝까지 안 건드렸다** — `windows.sandbox = "elevated"` 그대로이고 이제 그대로 동작하므로 **데스크탑 앱 격리 수준도 안 내려갔다**. 노트 2건: `plan/issues/20260908_Codex_elevated_샌드박스_실패_검색_프롬프트.md` (프롬프트 4종 + 실측 사실 12줄) · `..._리서치_결과.md` (외부 회신 + 자체 검증 + 재발 시 절차). **★ 2026-09-09 후속 — 한 곳이 아니었다.** 프롬프트 2·3 회신을 정리하다 예방 차원에서 전 저장소를 스캔했더니 **`C:\AIMindVaults\.sync` 도 `CodexSandboxOffline` 소유**였다 (git 추적 107파일). 아직 증상은 안 냈지만 같은 지뢰라 `icacls /setowner` 로 되돌렸다 (308파일, 실패 0). 게임·Unity 레포와 `~/.codex` 는 정상. **교훈: 하나 고치고 끝내지 않는다 — 재발 시 절차 § 0 에 전 저장소 스캔을 앞에 넣었다.** 조치 후 `codex doctor` `✓ provisioning complete` 유지 확인. 프롬프트 2 회신 (설정 분리) 은 elevated 복구로 불필요해졌으나 부록으로 보존 — **`--profile` 이 별도 파일을 겹친다는 CLI 도움말이 회신의 "프로파일은 같은 config.toml 공유" 판단을 뒤집는다** (0.153.4). 프롬프트 3 회신은 **"elevated 는 Windows Sandbox·Hyper-V 에 의존하지 않는다"** 로 내 Home 가설을 두 번째로 부정 — 오류 문구→원인 매핑표만 건져 재발 절차로 이관. 작업자: Claude (메인 PC). |
| R212 | 2026-09-08 | ✅ | 🕓 | **Codex 에 Claude 와 같은 권한·활용을 준다 — 이름 기준 제한을 행위 기준으로** (사용자 지시 *"코덱스도 클로드 만큼의 권한과 활용을 가질 수 있게 진행"*, Codex 의 `_AGENT_COMMS` 검토 요청 회신). **★ 핵심은 규칙이 아니라 연결이었다** — Codex 실측 보고가 짚은 대로 **파일 미러 성공 ≠ 스킬 자동 발견**. Codex 는 `SKILL.md` (name·description frontmatter) 를 가진 디렉터리만 스킬 목록에 올리는데 우리 커맨드는 평범한 `<name>.md` 라, 미러가 다 성공해도 **목록에 하나도 안 떴다.** `agents-sync` 에 `emitCodexSkillEntry` 신설 — 미러 옆에 `<name>/SKILL.md` 진입점을 생성한다 (description 에 원문 `## 트리거` 키워드까지 흡수). **본문은 복제하지 않는다** (정본 경로만 가리켜 내용은 한 곳 유지) · 이미 `SKILL.md` 인 디렉터리 (Meshy 3종) 는 건드리지 않음 · orphan 판정 제외로 `--verify` 무결. **32개 생성, 재실행 NOOP.** **★ 더 큰 구멍 하나** — `agents-sync` 가 `custom/<domain>/` 만 돌고 **`custom/` 최상위 파일은 어느 미러에도 안 보냈다.** `.claude/` 쪽은 손으로 복사한 낡은 사본, `.codex/` 쪽은 **아예 없었다** — 즉 **에이전트 권한을 정하는 `agent-ownership.md` 자체가 Codex 에 안 보이고 있었다.** `ROOT_CUSTOM_MAP` 신설 (최상위만, 도메인 중복 집계 방지 `topOnly`). **제한 4건 개정**: ① `agent-ownership.md` — **에이전트 이름 기준 역할 분담표 폐기** (Claude=메인/Codex=보조). 소유권은 "지시를 받은 세션이 끝까지", 대신 **위험도 4등급** (되돌리기 쉬움 / 어려움 / 외부로 나감 / 전파본) 신설 ② `_essentials.md § 2` — "코드 제시 → 사용자 실행" 을 **"맡긴 범위 안에서는 직접 실행"** 으로. 금지는 실행이 아니라 **근거 없는 재시도** (같은 원인 2회 실패 시 중단·보고) ③ `script-creation-approval.md` — **남는 것과 안 남는 것을 갈랐다.** 저장소에 남는 스크립트·자동화 등록은 사전 승인 유지, 맡긴 작업의 `$env:TEMP` 일회성 보조는 보고만 ④ 루트 `AGENTS.md` — **"한 번 맡기면 그 안은 다시 안 묻는다"** 신설 + **"에이전트 이름으로 권한을 나누지 않는다"** 명시. **문언 충돌 2건 해소** (Codex 가 지적): `_essentials § 4` 의 모드 분리 vs `§ 7` 의 상태 갱신 필수 → `_STATUS.md`·핸드오프·`AGENT_STATUS.md`·`_AGENT_COMMS/` **4종은 모드 밖** (작업이 아니라 작업의 기록) · 동시 수정 금지 vs 양쪽 갱신 필수 → **소유자 1명만 쓰고 나머지는 COMMS 로 인계**. **유지한 것**: 전파본 직접 수정 금지 · 파괴적/외부 게시/비용은 항상 승인 (능력 제한이 아니라 유실·불일치 방지 장치라서). **검증**: `agents-sync --verify` NOOP · viz 50 pass · CLI 2 pass · `bump-version --broadcast` 2회 (6/6 Preset Hub). **내가 틀렸던 것 2건을 Codex 가 정정** — "미러 밖이라 Codex 가 못 읽는다" (접근이 아니라 발견 문제였다) · "Godot 은 CLI 통제 수단이 없다" (헤드리스 selftest 가 이미 있었다). **★ 검증 완료 (같은 날)** — Codex 세션을 열어 확인했다. 모델 호출 없이 CLI 가 조립하는 프롬프트를 덤프 (`codex debug prompt-input`) 해 `skills_instructions` 블록을 직접 읽었다. 총 65 스킬 중 **`C:/AIMindVaults/.codex/skills/` 출처 43개** (core 21 + 기존 수기 8 + custom 14) — **기존 11 + 이번 생성 32 와 정확히 일치.** 지목한 6개 (`unity-dev` · `reindex` · `vault-route` · `delegate-task` · `meshy-workflow` · `canvas-create`) 전부 로드 확인. **`.agents/skills` 로 옮길 필요 없다** — Codex CLI 가 프로젝트 로컬 `.codex/skills/` 를 재귀 스캔한다 (기존 수기 8종이 같은 루트에서 이미 쓰이던 것이 방증). **★ 실제 회차로 재확인 (같은 날, 사용자 지시)** — `codex update` (0.148.0 → **0.153.4**) 후 설정 모델 `gpt-6-astra` 로 실회차 실행. 목록은 오프라인 덤프와 **완전 일치** (6개 전부, 경로까지 Codex 가 직접 답함, 총수 43). 나아가 **실제 사용까지 확인** — `vault-route` 를 쓰라고 지시하자 Codex 가 ① `agents-sync --verify` 자발 실행 (NOOP) ② 생성 진입점 `.codex/skills/core/vault-route/SKILL.md` 읽기 ③ 진입점이 가리키는 **정본** `.agents/commands/core/vault-route.md` 로 이동해 읽기 ④ 규칙 적용 → **결론 `Godot` 볼트** + 저장 폴더까지 제시. **발견 → 진입점 → 정본 → 적용 4단계 전부 성립** — 본문 복제 없이 포인터만 둔 설계가 의도대로 돈다. **부수 발견 (우리 코드 아님)**: 업데이트 직후부터 **elevated Windows 샌드박스 프로비저닝 실패** (`helper_unknown_error`, `codex doctor` 18 ok · 1 fail). Defender 예외 미확인 경고가 같이 뜨고 npm 이 구 바이너리를 `EPERM` 으로 못 지운 것과 같은 계열로 보인다. 이번 회차는 `-c windows.sandbox="unelevated"` 로 우회했고 **설정 파일은 고치지 않았다** (사용자 보안 설정). 권장 조치는 Defender 예외 확인 또는 Codex 재설치. 배포 반영 🕓 (룰 개정분은 다음 배포 회차). 작업자: Claude (메인 PC). |
| R211 | 2026-09-08 | ✅ | 🕓 | **배포본 2회차 — 고친 뒤 같은 방식으로 다시 돌려 잡은 것** (R210 후속). 수정본을 **다시 클린 클론해 같은 한 줄 지시**로 신규 회차를 재실행했다. **R210 수정 재현 확인** — `npm.cmd` EINVAL 0건 (이전 5/12 실패) · DEP0190 경고 0 · 종료 시 셸 syntax error 0 · **12/12 · npm installs 11 · index 12 ok · 13s**. **① 첫 sync 가 추적 파일을 더럽혔다** — 클린 클론 직후 sync 한 번에 `Basic*Vault` 5개의 `Script_Registry.md` + `_WORKSPACE_VERSION.md` 10개가 modified 로 남았다. **배포 트리가 sync 결과와 어긋난 채 커밋돼 있어서**다. 사용자는 아무것도 안 했는데 워킹트리가 더러워지고 업데이트 pull 때 충돌을 손으로 푼다 — `node_modules` 추적 건과 같은 부류. **동기화를 한 번 돌린 결과를 그대로 커밋해 고정점**으로 만들었고, 새 클린 클론에서 sync 후 **더러워진 추적 파일 0개**로 실측 확인. **② `Sync This Vault.*` 위치 안내가 틀렸다** — 실제 설치 위치는 볼트 `.sync/` 인데 (Obsidian 노트 목록에 유틸 파일이 안 섞이게 한 의도적 설계) 안내 문구가 "볼트 루트" 라고 말해 **새 사용자가 없는 파일을 찾게 된다**. CoreHub `register-vaults.js` 정본 + `register-vaults` 스킬 3 미러 + README 정정, `bump-version --broadcast` (6/6) 후 배포본 12 사본 전파. **③ 남은 것 (범위가 커서 안 건드림)** — 추적 md 55 · js 61 이 제작자 개인 볼트 경로 `Vaults/Projects_Infra/Project_AIMindVaults/...` 를 인용 (`distribution-content-safety` 위반). 문서와 CLI 테스트 픽스처 양쪽이라 별도 사이클. 커밋 `460f2b0`, **push 안 함**. 작업자: Claude (메인 PC). |
| R210 | 2026-09-08 | ✅ | 🕓 | **배포본 정비 — 신규 사용자 첫 설치가 막혀 있었다** (사용자 지시. 2026-07-29 판매 중지로 동결돼 있던 배포를 해동). **검증 방식이 이번의 핵심** — 배포본을 클린 클론해 `claude -p "파일을 확인하고 환경사용을 위한 작업 진행"` 한 줄로 실제 신규 사용자 회차를 돌렸다. 문서만 읽었으면 못 잡았을 것들이 나왔다. **★ 설치 차단 버그**: `sync-all.js` 가 Windows 에서 `npm.cmd` 를 `shell:false` 로 spawn 해 **EINVAL** — Node 가 CVE-2024-27980 패치 (18.20.2+ · 20.12.2+ · 21.7.3+) 이후 `.cmd` 직접 spawn 을 거부한다. **기존 설치에는 `node_modules` 가 있어 SKIP 으로 빠지므로 안 드러나고 새 사용자의 첫 sync 에서만 터진다** — 실측 12볼트 중 Preset Hub 5개 실패. `npm` 일 때만 shell 경유 + 인자를 한 줄로 합쳐 호출 (DEP0190 경고가 첫 설치 화면에 뜨던 것도 같이 제거). **수정 후 완전 클린 클론에서 `Sync All Vaults.bat` 12/12 성공 · fail=0 · 16s** 로 실측. **런처 자기 덮어쓰기**: refresh 가 실행 중인 `Sync All Vaults.sh` 를 제자리에 덮어써 셸이 읽던 오프셋이 어긋나고 종료 직전 `syntax error near unexpected token` — `launchers.js copyTemplate` 을 temp + `rename` 원자 교체로. **저장소 위생**: `node_modules` **870 파일** · `.vault_data` 8 파일이 추적되고 있었다 (`.gitignore` 가 이미 제외를 선언했는데 그 전에 커밋된 잔재). 추적하면 사용자가 업데이트를 pull 할 때마다 충돌을 손으로 푼다 — R197 이 `_collections.json` 에서 짚은 것과 같은 종류. 추적 해제 + `.gitattributes` 신설 (없으면 Windows 클론이 셸 스크립트를 CRLF 로 바꿔 **macOS·Linux 에서 실행이 깨진다**) + `sync.log` 제외. **개인 자산 노출**: `AGENTS.md` 가 제작자의 키워드→볼트 매핑 **26줄과 개인 프로젝트 절대경로**를 그대로 싣고 있었다 (R084 와 같은 종류) → 형식 + 동봉 볼트 2개만 남기고 제너릭화. 코어 룰·스킬 예시의 개인 볼트명 (JissouGame · MachineAssembly · AI_Gen4Game 등) 제거. **깨진 참조**: 라우터가 **배포되지 않는 `/distribute`** 를 가리켰다 → 행 제거. `/canvas-create` · `/create-preset-hub` · `register-vaults` · `agent-ownership` · `multivault-personalization` · `obsidian-instance-control` 은 **코어가 참조하는데 안 실려 있었다** → 동봉. `.agents/` 미러가 `rules/core` 만 있어 반쪽이었다 → `commands/` · `custom/` 까지 채움. **문서**: README 규칙 수 11 → 14+4 (실제와 달랐다), 없는 `References/` 제거, 동봉 볼트 12개 안내, **viz 섹션 신설** (배포본에 실려 있는데 README 에 한 줄도 없었다). 코어 정본 델타 반영 (R166~R209 중 배포 대상). 커밋 `827260e`, **push 안 함**. **English 배포판은 미반영** (R191 에서 멈춰 있고 구조도 달라 별도 회차 필요). 작업자: Claude (메인 PC). |
| R209 | 2026-09-08 | – | – | **Godot GDScript 스타일 룰 신설 + 라우터 등록** (사용자 지시 "godot-style.md 룰 만들어", R208 § 8-2 제안의 1번). `.agents/rules/custom/Godot/godot-style.md` 신규 11절. **관례를 발명하지 않고 레포에서 읽어냈다** — `GodotVamSurLike` 의 `.gd` 52개 (런타임 `scenes/**` 11 + 도구 `tools/**` 41) 를 확인해 이미 서 있는 규약을 성문화했다. 정적 타입 전면 부착 · 시그널 과거형 (`died`·`cast_requested` — 명령이 아니라 통보라서) · `##`(무엇·왜) 와 `#`(왜) 분리 · 폴백 대신 `assert` (폴백이 있으면 순서 위반이 조용히 넘어간다, `mob.gd`) · 매직넘버 상수화. **런타임과 probe 스크립트를 갈랐다** — `tools/*.gd` 는 `extends SceneTree` 로 한 번 재고 버리는 코드라 § 2·§ 5 만 적용한다. **★ 앞선 판단을 뒤집은 실측** — R208 § 8-2 에서 "Godot 은 CLI 통제 수단이 없어 검증 계층을 못 만든다" 고 적었으나 **틀렸다.** `main.gd` 에 헤드리스 자기검증 (`--headless -- selftest`, S1~S17) 이 이미 있고 실제로 돌린다: `Godot_v4.7.1-stable_win64_console.exe --headless --path <repo> -- selftest` → `selftest 통과`, **EXIT=0** (실측 2026-09-08). 이것이 `unity-cli editor refresh --compile` 자리에 해당해 § 9 검증 절차로 편입했고, "시스템을 늘리면 selftest 항목도 늘린다" 를 강제로 넣었다. `_console.exe` 를 쓰는 이유 (일반 exe 는 stdout 이 안 붙는다) 도 명시. **구조 판단은 이 룰에 넣지 않았다** — 씬 분할·상속 대 컴포지션·폴더 구조는 Godot 볼트 `Concepts/Godot_모범_사례.md` 가 이미 들고 있어 참조만 건다 (Unity 가 스타일/도구/편집으로 나뉜 것과 같은 분업). 라우터에 "Godot 개발" 행 신설. **알려진 이탈 1건 기록**: `main.gd` 가 `const→@onready→signal→var` 순 (공식 순서와 다름) — 손댈 일이 생길 때 정리, 순서만 바꾸려고 769줄을 건드리지 않는다. 작업자: Claude (메인 PC). |
| R208 | 2026-09-08 | – | – | **게임개발 도메인을 Skill Router 에 등록 + 죽은 경로 정정** (사용자 지시 — Codex 를 게임개발에 투입하기 전 스킬·룰 인벤토리를 넘기다가 드러난 갭 5건 중 2건 처리). **① 라우터 5행 신설** (`.agents/rules/core/_skill-router.md`) — Unity 개발 / Unity DOTS / 게임 시스템 설계 / Blender·3D 모델링 / AI 3D 에셋. **스킬이 있는데 키워드로 안 걸리고 있었다** — `/unity-dev` · `/blender-workflow` · `/meshy-workflow` 는 존재하지만 라우터 표에 도메인 행이 Canvas 하나뿐이라 사람이 슬래시 명령을 직접 쳐야 발동했다. 룰 파일도 `.agents/rules/custom/` 에 있어 상시 주입이 아니므로, 라우터에 없으면 **작업 중 한 번도 안 읽힌다**. 종전 "도메인은 기본 등록하지 않는다" 문구는 등록 안내 + 자기 환경에 없는 행은 지우라는 안내로 교체. **② 죽은 경로 2곳** — `~/.claude/commands/game-architect.md` 와 `~/.claude/CLAUDE.md` 의 Unity 프로젝트 경로가 `C:/Dev_Game/GameBaseProject/` 인데 **그 경로는 존재하지 않는다** (실제 `C:/Dev_Game/Unity_Projects/GameBaseProject/`). 같은 표의 `/gameplay`·`/performance`·`/qa` 행도 같은 경로였고, 실제로는 프로젝트 로컬에 **5개** (`tools`·`visual` 누락) 라 전부 정정 + 스켈레톤 표기. 활성 프로젝트 표에 GodotVamSurLike 행 신설 (현재 주력인데 없었다). **주의**: 라우터가 가리키는 `/unity-dots`·`/game-architect` 는 `~/.claude/commands/` 전역 스킬이라 **`agents-sync` 미러 밖 — Codex 는 못 본다** (갭 4, `.agents/` 이관 여부는 사용자 결정 대기). 작업자: Claude (메인 PC). |
| R207 | 2026-09-06 | – | – | **인박스 큐를 게임 볼트에서 멀티볼트 루트로 올렸다 + 무인 인박스 디바이스 지정 (`!runner`)** (사용자 지시 *"하나의 디바이스를 지정해서 해당 디바이스로 작업되도록"* · *"명칭이 인박스가 맞나 … 그냥 aimindvault 및 디바이스에 원격으로 명령을 보내는 용도"*). **① 디바이스 지정** — 저장소가 여러 디바이스에 복제된 상태에서 각각 스케줄러를 걸면 **같은 ▶️ 를 둘이 집는다** (5분 격자가 같아 동시 발동이 오히려 흔하고, ⏳ 표식은 `find_trigger`→`react` 사이가 비어 경합을 다 못 막는다). `run_trigger.py` 에 `socket.gethostname()` 게이트 + `!runner [이름|me|status]` 추가. 지정값은 각 디바이스 `.vault_data/viz-prefs.json` 의 `runnerDevice` (git 미추적) — **git 을 안 탄다**. 디스코드 채널이 이미 모든 디바이스가 5분마다 읽는 공용 버스라 명령 한 번으로 수렴한다. 지정이 없으면 종전대로 전부 실행 (기존 1대 환경 무변경). 비지정 디바이스는 `poll_inbox` 도 건너뛴다 — `_INBOX.md` 가 git 추적이라 두 디바이스가 같이 쓰면 커밋이 갈라진다. **명령 처리는 계속**한다 (지정 변경을 따라가야 한다). **★ 중복 억제를 ✅ 표식이 아니라 값 비교로 했다** — 봇이 하나라 표식이 공유돼서, ✅ 로만 걸렀다면 **먼저 켜진 디바이스가 ✅ 를 달아 나중에 켜진 디바이스가 지정을 영영 못 읽는다.** 값을 바꾸는 명령은 이미 그 값이면 조용히 넘어가고 (5분마다 같은 줄이 안 쌓인다), 물어보는 명령만 ✅ 로 거른다. `!trigger` 도 같은 규칙으로 정리. `run_trigger --selftest` 8항목 신설. **② 실사용 첫 회에 드러난 버그** — `!runner` 를 치자 게이트는 정상 동작했는데 (`designated=… runner=True`) **같은 메시지가 `_INBOX.md` 대기 중에 `- [ ] (…, discord) !runner …` 로 적재됐다.** `poll_inbox._line` 이 `!` 명령을 안 걸러서다 — 무인 회차가 그 줄을 작업으로 집으려 든다. `body.startswith('!')` → `None` 으로 수정, `poll_inbox --selftest` 7항목 신설, 적재된 가짜 항목 1줄 제거. **이 버그는 `!trigger` 때부터 있었고** 그 명령을 거의 안 써서 안 드러났을 뿐이다. **③ 큐를 루트로** — 디스코드 `#inbox` 는 한 프로젝트가 아니라 **멀티볼트 전반·디바이스 제어**에 던지는 창구인데 큐 파일만 한 볼트 안에 있어 소유가 어긋나 있었다 (사용자가 "인박스라는 명칭이 맞나" 로 지적). `git mv` 로 `_INBOX.md` 를 루트로 (이력 보존), `discord_config.json` 의 `inbox_file` 을 `_INBOX.md` 로, 지시서 § 0 경로 갱신. **이름은 안 바꿨다** — `!` 명령을 큐에서 분리하고 나니 파일에 남는 것은 "처리 대기 항목" 뿐이라 *인박스* 가 내용과 맞는 말이고, 어긋난 것은 이름이 아니라 자리였다. 루트 `CLAUDE.md` 에 **§ 인박스** 신설 (`_INBOX.md`↔`_STATUS.md` 분리 규약을 볼트에서 이관 · 대상 볼트는 볼트 진입 프로토콜로 판정 · `!` 줄은 인박스 항목이 아님) + 루트 작업 범위에 `_INBOX.md` 추가. 볼트 `CLAUDE.md` 는 루트를 가리키게 축약. **검증**: 두 `--selftest` 통과 · 적재 대상 경로가 `C:\AIMindVaults\_INBOX.md` 로 해석되고 `## 대기 중` 섹션 존재 확인 · `--dry-run` `added=0` · `--status` `runner=True`. 작업자: Claude (메인 PC). |
| R206 | 2026-09-03 | – | – | **무인 인박스 — 실사용 하루 만에 드러난 결함 4건 수정** (사용자 지시 "무인 인박스 환경에 어울리지 않거나 부족한 점을 찾아서 픽스", Tripo 인박스 건을 유인으로 처리하면서 발견). **① 적재가 스케줄러에서 안 돌았다** — `poll_inbox.py` 는 **SessionStart 훅에서만** 돌고 Task Scheduler 는 `run_trigger.py` 만 부른다. 사람이 PC 에서 세션을 열기 전엔 폰 메시지가 `_INBOX.md` 에 안 들어가고, ▶️ 를 눌러도 지시서 § 0 이 "대기 중 비어 있음" 으로 즉시 끝난다 (R200 의 "적재는 항상 돈다" 가 문서에만 있었다). `run_trigger.main()` 이 `poll_inbox.main()` 을 먼저 부르게 (실패해도 트리거는 계속). **② 보류 항목이 큐 맨 위를 영구 점유** — 무인이 못 푸는 항목 (사람 눈·로그인·크레딧 버튼) 을 `- [ ]` 로 남기면 § 1 "맨 위 하나" 가 매 ▶️ 마다 같은 항목을 집어 **그 아래가 영원히 안 돈다** (9-02 Tripo 건이 하루 점유). `- [?]` 보류 표식 신설 — § 0·§ 1 이 안 세고, 사유 줄 `⛔ 보류 (날짜): …` 을 달며, **되살리는 것은 사람** (`- [ ]` 로 되돌리거나 새로 던진다). `/api/inbox-status` 가 `held` 를 따로 세어 viz 에 "보류 N건 (사람 판단 대기)" 표시. **③ ▶️ 누른 메시지와 실행 항목이 어긋났다** — 항목이 여럿이면 누른 것과 무관하게 맨 위가 돌아 폰의 ⏳·✅ 표식이 거짓말이 된다. `run_trigger` 가 `.vault_data/inbox_target.json` 에 그 메시지 본문을 쓰고 (한글을 PowerShell 인자로 넘기지 않는다 — 설계 원칙 1) 지시서 § 1 이 그 줄을 먼저 찾는다. 실행 후 삭제. **④ 적재가 LIFO 였다** — `_append` 가 섹션 머리에 붙여 먼저 던진 지시가 뒤로 밀렸다. 섹션 끝에 붙이는 FIFO 로 + `(없음)` 자리표시자 제거. 검증: `_append` 자체 검사 (FIFO 순서·자리표시자·빈 줄 3연속 없음) · `run_trigger --dry-run` (`POLL_DRYRUN` → `TRIGGER pending=0`) · viz 49/49 · `server.js` import OK. **지시서 § 6-1 에 Tripo 무인 범위 표** — 이미지 생성(무료) 까지 가능, 선별·3D(55크레딧) 는 유인. **함정 2건**: Bash 도구 heredoc 이 `\\` 를 `\` 로 접는다 (인라인 파이썬 패치가 두 번 헛돌았다 — 패치는 파일로 쓴다) · `poll_inbox.py` 는 CRLF 라 `newline=''` 로 읽고 써야 줄끝이 안 뒤집힌다. **남은 것**: 전용 프로필 Tripo 로그인 (사람) · `Script_Registry.md` Core 전파본 구조 (미해결 그대로). 작업자: Claude (메인 PC). |
| R205 | 2026-09-03 | – | – | **viz 작업표시줄 — 지구본 아이콘 + 버튼 분리 두 문제 수정** (사용자 보고). **증상 1 지구본**: `index.html` 에 `<link rel="icon">` 선언이 없었고, `viz.ico` 파일은 있는데 **서버가 안 내보냈다** — 정적 서빙이 `/lib/`·`/pages/`·`/styles/` 등으로 화이트리스트돼 루트 파일이 404. `chrome --app` 창의 작업표시줄 아이콘은 파비콘에서 오므로 못 찾으면 기본 지구본이 뜬다. **수정**: 파비콘 선언 + `/favicon.ico`·`/viz.ico` 라우트 (200, `image/x-icon`, 원본과 바이트 동일). **함정**: Chrome 이 파비콘을 프로필에 캐시해 **파일을 고쳐도 기존 프로필은 옛 아이콘을 계속 쓴다** — 새 프로필로 띄우면 즉시 AMV 가 나오는 것으로 격리 확인했고, 일반 탭 1회 로드로 갱신된다. **증상 2 버튼 분리 (사용자가 실제로 원한 것)**: 고정 아이콘을 눌러도 **별개 버튼이 새로 생긴다**. 원인은 AppUserModelID 불일치 — 런처가 `chrome --app` 으로 창을 여므로 작업표시줄에 뜨는 창은 exe 가 아니라 Chrome 앱 창이고, 고정 바로가기는 AUMID 없음(exe 경로 자동 생성) · Chrome 앱 창은 `Chrome.localhost_/` 였다. 창의 AUMID 는 `SHGetPropertyStoreForWindow` + `PKEY_AppUserModel_ID` 로 실측. **수정**: 바로가기(고정·시작메뉴)에 같은 AUMID 를 `IShellLink`+`IPropertyStore` 로 박아 **버튼 통합 확인** (백업 `.bak_*`). AUMID 가 호스트명으로만 만들어져 **포트가 안 들어간다** — fallback(8765→8767)에도 묶이는 이득이 있고, 반대급부로 다른 localhost `--app` 이 같은 버튼에 묶인다. **보존**: AUMID 는 로컬 바로가기 속성이라 git 에 안 담긴다 → `_build/set-shortcut-aumid.ps1` 신설 (`-Report` 조회 · 멱등 · 백업), `create-shortcut.ps1` 이 자동 호출, `viz/AGENTS.md` 에 절차 기록. **작업표시줄 고정은 사용자 행위**라 고정 후 한 번 더 실행해야 한다. **자기 정정 1건**: 아이콘이 바뀐 것만 보고 "해결됐다" 고 보고했으나 사용자가 원한 것은 버튼 통합이었다 — 증상 서술을 아이콘 문제로 좁혀 읽은 것. **검증**: 파비콘 200·바이트 동일 · 새 프로필 AMV 표시 · 기존 프로필 캐시 갱신 후 AMV · AUMID 적용 후 작업표시줄 캡처로 버튼 1개 확인 · 스크립트 재실행 `0 건 변경`. 작업자: Claude (메인 PC). |
| R204 | 2026-09-03 | – | – | **무인 인박스 현황을 viz 설정에 표시 — 대기 큐 건수 + 마지막 실행 결과** (R203 후속, 사용자 요청). **새 상태 파일을 만들지 않았다** — 출처가 둘 다 이미 있다. 대기 건수는 `_INBOX.md` 의 `## 대기 중` 구간 `- [ ]` 개수, 마지막 실행은 `.vault_data/logs/inbox-task_*.log` 최신 파일의 `=== exit=N @ ... ===`. 트리거가 따로 기록을 남기게 하면 그게 또 어긋날 지점이 된다 (R203 에서 상태 이중화를 없앤 것과 같은 이유). **신규 GET `/api/inbox-status`** (읽기 전용, POST whitelist 무변경). **registry 에 `statusEndpoint` 필드 신설** — surface 가 아니라 feature 속성이라 어느 surface 든 쓸 수 있다. adapter 에 `fillFeatureStatuses()` 추가, `sub` 안 `[data-feature-status="<id>"]` 자리표시자를 채운다 (`sub` 는 원래 escape 없이 삽입되는 자리라 HTML 을 넣을 수 있었다). settings.js 는 **await 하지 않고 호출** — 현황 조회가 느리거나 실패해도 설정 화면 렌더를 막지 않는다. **실측으로 잡은 결함 2건**: ① `node --check` 가 **ESM 파일의 문법 오류를 통과시켰다** — `server.js` 에 `
` 이 실제 줄바꿈으로 들어가 문자열이 끊겼는데 `--check` 는 OK 를 냈고 실제 기동에서 `SyntaxError`. ESM 검증은 `node -e "import('./server.js')"` 로 해야 한다 ② `querySelector` 로 자리표시자를 채웠더니 **settings 캐러셀의 클론에는 "확인 중" 이 남았다** — 실제로 눈에 보이는 쪽이 클론이라 브라우저로 안 봤으면 못 잡았다. `querySelectorAll` + 전부 채우기로 수정. **검증**: 엔드포인트 실응답 `{queued:0, lastRun:{at,exit:0,ok:true}}` · 브라우저에서 자리표시자 2개 모두 `대기 큐 0건 · 마지막 2026-09-02 20:47 — 성공` · viz 테스트 49/49 · orphan marker 0. 작업자: Claude (메인 PC). |
| R203 | 2026-09-02 | – | – | **무인 인박스 트리거 온오프를 viz 커스텀 기능에 편입** (사용자 요청). R200 이 만든 트리거의 제어 지점이 폰 `!trigger` 와 PC `Disable-ScheduledTask` 둘뿐이라 **PC 앞에서 상태를 보거나 끄는 자연스러운 자리가 없었다**. viz 설정의 커스텀 기능 섹션에 토글로 넣었다. **등록은 registry 객체 1개** — `CUSTOM_FEATURES` 에 `discordTrigger` 추가로 settings row 렌더 · `/api/viz-prefs` 기본값 · POST whitelist 가 **전부 자동** (R164 adapter 설계가 의도대로 동작). 신규 endpoint·페이지 0. **핵심은 상태를 한 곳에 모은 것** — `run_trigger.py` 가 자기 상태 파일(`.vault_data/discord_trigger.json`) 을 쓰고 있어서 viz 토글과 폰 `!trigger` 가 **서로 다른 값을 보는 상태**였다. 파이썬을 `.vault_data/viz-prefs.json` 의 `discordTrigger` 키로 옮겨 **세 경로가 같은 파일을 본다**. 옛 상태 파일 제거. 쓰기는 **read-modify-write + 원자적 교체** (`tmp` → `os.replace`) — 통째로 덮어쓰면 `gitAutoSync` 등 남의 설정이 날아간다. 파싱 실패는 기본값으로 뭉개지 않고 중단한다 (**R196 과 같은 종류의 사고** — 손상 파일을 조용히 읽고 덮어쓰면 양쪽 데이터가 소실된다). **검증**: registry 등록 확인 (`defaults={gitAutoSync,discordTrigger}`, settings-row 2건) · adapter 렌더에 `data-feature-toggle="discordTrigger"` 존재 + off 값이 `checked` 없이 렌더 · 저장/로드 왕복 (False↔True) 에서 `gitAutoSync` 보존 확인 · viz 테스트 **49/49 pass** · orphan marker 0. **적용 시점**: 다음 폴링(최대 5분)부터. 작업자: Claude (메인 PC). |
| R202 | 2026-09-02 | – | – | **트리거 폴링이 전체화면 게임을 떨어뜨리던 문제 수정 + 온오프 정리** (사용자 보고). 증상: 롤 중 5분마다 게임이 꺼짐. **원인은 실행이 아니라 폴링 자체** — Task Scheduler 가 `python.exe` 로 띄워 5분마다 콘솔 창이 잠깐 뜨고, 그때 전체화면 앱이 포커스를 잃는다. `!trigger off` 는 **실행만** 막고 폴링 창은 그대로라 해결이 안 됐다. **수정 2건**: ① 스케줄 작업 실행 파일을 `python.exe` → **`pythonw.exe`** (콘솔 없음) ② `run_trigger.py` 의 `subprocess.run` 에 **`CREATE_NO_WINDOW`** — 부모가 창이 없어도 자식 `powershell` 이 창을 만든다. **온오프 3단**: 폰 `!trigger off` (실행만 멈춤, 적재는 계속 — 되살릴 명령을 읽어야 하므로) · PC `Disable-ScheduledTask -TaskName 'AIMV-Discord-Trigger'` (백그라운드 활동 0) · 되돌리기는 `Enable-`. **남은 소음원 1**: ▶️ 가 실제 발동하면 **크롬이 headed 로 뜬다** (R201 브라우저 추가의 결과). 이건 창을 숨기면 Tripo 조작 확인이 안 되므로 그대로 두고, 게임 중에는 `!trigger off` 를 거는 것으로 정리했다. 안내를 `_INBOX.md` 상단에 실어 폰에서 바로 보이게 했다. **검증**: `pythonw` 전환 후 수동 실행 `LastTaskResult=0` · Disable/Enable 왕복 (`Disabled` ↔ `Ready`). 작업자: Claude (메인 PC). |
| R201 | 2026-09-02 | – | – | **무인 인박스에 브라우저 조작 추가 — Playwright MCP + 전용 프로필**. Tripo 정본 경로가 API·CLI 가 아니라 **크롬 확장 디버그 조작** 으로 바뀐 것 (무인 20차 정정) 이 계기. **실측으로 확인한 제약**: 대화형의 `mcp__Claude_Browser__*` · `mcp__claude-in-chrome__*` 는 `.mcp.json` 이나 settings 가 아니라 **호스트 앱이 주입하는 도구**라 Task Scheduler → `claude -p` 경로에 **원리적으로 안 붙는다** (헤드리스 프로브에서 4개 전부 `=no`, `WebFetch`·`Bash` 만 `=yes`). 설정으로 될 문제가 아니었다. **해결**: 루트 `.mcp.json` 에 `@playwright/mcp` 등록 — 정식 MCP stdio 서버라 무인 CLI 가 로드한다. **프로필은 A안 (전용, 사용자 결정)** — `C:/Users/c/.agent-browser-profile`. 대안 B (기존 크롬에 CDP 연결) 는 무인 세션이 **그 프로필의 모든 로그인 세션** (메일·깃허브 등) 을 쓸 수 있게 되어 기각. 브라우저는 **설치된 크롬 실행파일 재사용** (`--executable-path`) 이라 Chromium 150MB 추가 다운로드 없음. **함정 1건**: `.mcp.json` 서버는 승인 대기 상태로 뜨는데 (`⏸ Pending approval`), `~/.claude.json` 의 `enabledMcpjsonServers` 에 넣어야 한다. **프로젝트 키가 `C:\AIMindVaults` 와 `C:/AIMindVaults` 두 벌로 존재해** 한쪽만 고치면 안 먹는다 — 양쪽에 넣고서야 `✔ Connected`. **검증**: 헤드리스 `claude -p` 에서 `mcp__playwright__browser_*` 5개 `=yes` · 실제 `browser_navigate` 로 `studio.tripo3d.ai` 도달 확인 (`LOGGED_IN=no`) · 프로필 디렉토리 22MB 생성. **지시서 § 6-1 신설**: 무인은 `mcp__playwright__*` 를 쓴다 · 로그인 안 돼 있으면 **로그인을 시도하지 않고** Blocked 남기고 중단 (자격증명을 다루지 않는다) · **결제·구매·구독 변경 금지** (크레딧 소모 버튼은 사람이) · 계정 설정 변경·외부 발행 금지 · 메인 크롬 프로필 사용 금지 · 캡처는 `#log` 에 첨부. **남은 것**: 전용 프로필에 Tripo 로그인은 **사람이 한 번** 해야 한다. 작업자: Claude (메인 PC). |
| R200 | 2026-09-02 | – | – | **디스코드 ▶️ 반응 트리거 — 폰에서 무인 작업 발동** (R199 인바운드의 후속, 사용자 결정으로 넓은 권한). R199 는 큐잉까지였고 실행 시작은 뺐었다. **훅으로는 트리거가 불가능** — SessionStart 훅은 사람이 세션을 열 때만 도는데 트리거는 사람이 없을 때 돌아야 한다. 그래서 `Run-ClaudeTask.ps1` + Task Scheduler 로 복귀 (`AIMV-Discord-Trigger`, 5분 주기). **적재와 실행을 분리했다** — 적재(`poll_inbox.py`)는 항상 돌고 실행(`run_trigger.py`)만 on/off 한다. 적재까지 끄면 `!trigger on` 을 읽을 수단이 없어져 폰에서 되살릴 수 없다. **성능 실측**: 폴링 1회 0.43초 (파이썬 기동 + HTTPS 왕복 1회), 5분 주기 가동률 **0.14%** · 하루 CPU 125초 · 상주 프로세스 0. 비용은 폴링이 아니라 발동에 있다 (`claude -p` 풀 세션). **반응 프로토콜**: 📥 적재됨 · ▶️ 사람이 "지금 실행" · ⏳ 시작 · ✅ 완료 · ❌ 실패. 봇 반응을 처리 표식으로 써서 재발동을 막는다 (별도 상태 파일 불필요 — 디스코드가 상태를 들고 있다). **신규**: `run_trigger.py` · `tasks/inbox-task.md` (무인 지시서) · `profiles/inbox-task.json` · `discord_io.py` 에 반응 조회 3함수. **락은 새로 안 만들었다** — `Run-ClaudeTask.ps1` 에 이미 있다 (`$Task.lock` + 타임아웃 기반 stale 처리). **권한은 B안 (넓게, 사용자 결정)** — `bypassPermissions`, 볼트·레포 범위 제한 없음. 근거: 개인 작업이고 통신으로 시킬 수 있는데 안 할 이유가 없다. **대신 되돌릴 수 있는 상태를 유지한다** — `git push` 는 프로파일 deny + 지시서 § 6 + `Run-ClaudeTask` 프롬프트 문구로 삼중 차단이라 무인 작업은 **로컬 커밋까지**만 간다. 원격이 안 오염되므로 잘못되면 `git reset` 으로 복구된다. **지시서의 핵심 안전장치는 § 0** — `## 대기 중` 이 비면 **즉시 종료, 커밋도 안 함**. 참고 사례가 보고한 실패 모드(큐가 비면 루프가 절대 안 깨지는 작업을 지어내 며칠이 지나도 진척 없음)를 원천 차단한다. § 1 한 번에 한 항목 · § 4 사람 눈 판정·비가역 변경·결정 충돌이면 Blocked 남기고 중단 ("중단은 실패가 아니다"). **검증**: `--status` / `--dry-run` 정상 · 상태 저장·로드 왕복 · 스케줄 작업 수동 실행 `LastTaskResult=0` · 다음 실행 시각 등록 확인. **미검증**: ▶️ 실제 발동 후 무인 세션 완주는 사용자가 폰에서 눌러야 확인된다. 작업자: Claude (메인 PC). |
| R199 | 2026-09-02 | – | – | **디스코드 ↔ 인박스 인바운드 경로 신설 — 폰에서 던진 지시가 `_INBOX.md` 로 들어온다**. 기존 디스코드 자산은 전부 **아웃바운드**였고 (배포용 커뮤니티 서버 운영 스크립트 4종) 밖에서 볼트로 들어오는 경로가 없었다. **서버를 분리했다** — 커뮤니티 서버는 결정 4가 "완전 영문 only" 이고 모더 1~2명을 가정하므로, 한국어 개인 지시와 볼트 이미지를 거기 두면 언어 정책 충돌 + 권한 실수 시 개인 자산 노출 (R084 와 같은 종류) + 모더 이양 시 지뢰가 된다. 개인 작업 서버 신설, 전용 봇 `AgentWorkers` (토큰 분리 = 커뮤니티 봇 유출이 볼트 채널에 안 닿는다). **채널 2개** `#inbox` (사람이 답해야 하는 것) · `#log` (에이전트 기록) — 나눈 이유는 볼륨이 아니라 **알림**이다. 한 채널이면 완료 로그마다 폰이 울리고, 그러다 알림을 끄면 판정 요청을 놓친다. **신규 스크립트 3** (`Contents/Project/scripts/discord/`): `discord_config.json` (토큰 **경로**만, 값 아님) · `discord_io.py` (발송·첨부·폴링·반응·화이트리스트) · `poll_inbox.py` (새 메시지 → 대기 중, 커서로 멱등). **작업을 시작하지 않는다 — 큐에 넣기만 한다.** 트리거는 사용자 결정으로 이번에 제외. **봇 권한 5개만** (채널 보기·전송·첨부·기록 읽기·반응). 관리자·서버/역할/채널 관리·메시지 관리는 뺐다 — 인박스 왕복에 불필요하고 토큰 유출 시 피해 범위가 커진다. **작성자 화이트리스트 필수** — 없으면 채널에 들어온 누구나 인박스에 지시를 넣을 수 있고 봇이 자기 메시지를 되읽는 되먹임이 생긴다. **스케줄링은 Task Scheduler 가 아니라 SessionStart 훅** (사용자 승인 후 변경) — 인박스는 어차피 다음 세션이 집는 큐라 세션 시작 직전에만 최신이면 충분하고, 상시 프로세스도 `WakeToRun` 취약점 (2026-09-01 실측 2회 미실행) 도 없다. **실측으로 잡은 결함 4건**: ① `vault_root()` 가 상위 단계를 5로 셌는데 실제 7 → 단계를 세지 않고 `Vaults/` + `CLAUDE.md` 마커 탐색으로 교체 ② 훅의 한글 패턴 매칭이 콘솔 코드페이지 때문에 안 먹음 → 요약 출력을 `added=N` ASCII 로 고정 ③ 디스코드가 밑줄 시작 파일명을 마크다운 이탤릭으로 먹어 `_render.png` → `render.png` 로 올라감 → 발송 시 `u_` 접두어 자동 부여 ④ 훅 실패가 세션을 막지 않도록 최상위 예외 포획 (토큰 부재 모사에서 `POLL_SKIP` + exit 0 확인). **검증**: dry-run 신규 1·무시 2 (봇 메시지 차단) → 실적재 1 → 재실행 0 (중복 없음) · 반복 적재 포맷 빈 줄 누적 0 · 훅 명령 그대로 실행해 무출력 통과. **미해결**: 새 스크립트를 `Script_Registry.md` 에 등록하지 못했다 — 그 파일이 Core 전파본이라 (AIHubVault·AI_Coding 해시 동일, Project_AIMindVaults 만 로컬 편집으로 어긋남) R081 상 직접 편집 금지이고 편집해도 다음 `core-sync-all` 에 날아간다. **볼트마다 달라야 하는 파일이 Core 경로에 있는 구조 문제** 로 보이며 사용자 판단 대기. 작업자: Claude (메인 PC). |
| R198 | 2026-08-28 | – | – | **개발학습 dedup 명령 정정 — 무인 실행에서 실행 불가였던 것을 실제로 도는 형태로**. 11차 (첫 무인 노트 생성 회차) 가 스스로 보고한 결함: `§ 2단계` 의 dedup 명령을 그대로 실행할 수 없어 매 회차 즉석 우회하고 있었다. **원인은 두 제약이 서로 다른 셸에 있던 것** — `rg` 는 Bash(Git Bash)에만 있고 PowerShell PATH 에 없으며, `$TEMP` 리다이렉트는 무인 셸에서만 차단된다 (작업 디렉토리 밖 쓰기 금지, 핸드오프 함정 #8). 그래서 어느 셸을 골라도 한쪽이 막혔고, 셸을 갈아타는 우회가 매번 생겼다. **수정**: 스크래치를 `$TEMP` → `.vault_data/devlearn_*` 로 이동 (작업 디렉토리 안이라 무인 셸 통과 · gitignore 라 `git add -A` 에 안 딸려감 · `rg` 스캔 대상인 `Vaults/` 밖이라 dedup 오염 없음) → **Bash 한 셸로 완결**. 아울러 daemon 폴더 스캔 제외 `-g '!**/scripts/daemon/**'` 를 **명령 자체에 내장** 했다 — 지금까지 지시서 산문에만 있어서 빠뜨리기 쉬웠고, 실제로 2026-08-26 9차에서 지시서 본문의 영상 ID 가 "이미 노트화됨" 으로 오매치돼 제외 판정이 재확인되지 않은 전례가 있다. 지시서 § 2-2 는 중복 서술을 지우고 포인터로 축약 (두 곳에 적으면 어긋난다). **검증**: 정정한 명령을 그대로 실행해 `TOTAL=229 DONE=229 → 신규 0 · 제외 0` 확인. **미해결로 남긴 것**: 이 변경은 `temp-file-management.md` 의 "임시 파일은 반드시 `$env:TEMP` 하위, CWD 금지" 와 정면 충돌한다. 무인 셸에서 `$TEMP` 가 물리적으로 불가능해 현실이 이미 어긋나 있던 항목이고, **core 배포 규칙이라 예외 명문화는 사용자 판단 대기**. 정본 `.agents/` 편집 후 agents-sync 전파 (`Updated: 2`). 작업자: Claude (메인 PC). |
| R197 | 2026-08-27 | – | – | **`_collections.json` 배포 제외 확정** (사용자 결정). viz 컬렉션 코드 (`viz/`) 는 배포 대상이지만 **사용자 데이터 파일은 제외**한다. 근거 셋: ① 노트 참조에 사용자 볼트명·경로가 그대로 들어간다 (R084 개인 자산 노출 — 현재 파일에도 `AI_Coding`·`DevFoundation` 이 들어 있었다) ② **배포본은 사용자가 업데이트를 pull 받는 저장소** 라, 추적하면 사용자가 ★ 를 누른 뒤 매 업데이트마다 충돌을 손으로 풀어야 한다 — R196 에서 막은 바로 그 시나리오를 제품이 상시 유발하게 된다 ③ **파일이 없어도 문제가 없다** — 부재 상태에서 GET 이 기본 즐겨찾기 컬렉션을 응답하고 파일을 만들지 않으며, 첫 POST 때 생성됨을 실측 확인. 신규 사용자도 즐겨찾기가 바로 보인다. 검토한 대안: "빈 템플릿 포함" 은 ②를 그대로 떠안고 얻는 게 없어 기각. "포함 + 배포본 gitignore" 는 결과가 제외와 같은데 설정만 늘어 기각. **반영**: `.agents/rules/core/distribution-content-safety.md § 배포 미대상` (정본 편집 후 agents-sync 전파 — R194 에서 전파본을 편집해 되돌아간 전례가 있어 이번엔 정본부터) · `viz/lib/collections.js` 헤더 · 배포 변경 로그. deploy 는 whitelist 방식이라 `DEPLOY_TARGETS` 미명시로 이미 자동 제외되지만, 향후 루트 파일을 일괄 추가할 때 실수로 포함되지 않도록 룰에 못 박았다. 작업자: Claude (메인 PC). |
| R196 | 2026-08-27 | 🕓 | 🕓 | **컬렉션 디바이스 정합 — 손상 파일 데이터 손실 차단 + 노트북 검증 큐 발행**. **메인 PC 측 감사**: 저장 경로가 `ROOT_DIR` 상대 (server.js 위치 기준) 라 디바이스 독립 · 절대경로/hostname/사용자명 하드코딩 0 · git 추적·푸시 확인 · `.vault_data/` 에 흔적 없음. 설계는 정합. **그 과정에서 실제 데이터 손실 경로 발견·차단**: `_collections.json` 은 git 추적이고 POST 가 통째로 덮어쓰므로 두 디바이스가 각각 바꾸면 merge conflict 가 난다. 재현해 보니 **충돌 마커가 있는 파일을 서버가 조용히 "빈 기본값" 으로 읽었고**, 그 상태에서 ★ 를 한 번만 눌러도 POST 가 손상 파일을 덮어써 **양쪽 디바이스 데이터가 함께 소실**됐다. 원인은 `readCollections()` 가 **파일 부재와 파싱 실패를 같은 catch 로 뭉갠 것**. **수정**: 둘을 구분 — 부재면 기본값 (첫 실행), 파싱 실패면 GET 응답에 `corrupt: true` + 사유를 싣고 **POST 를 409 로 거부** (읽지 못한 파일은 절대 덮어쓰지 않는다). `#collections` 페이지는 빈 목록 대신 **경고 배너** 를 띄운다 — 빈 목록으로 보이면 사용자가 "사라졌다" 고 오해하고 새로 담다가 덮어쓴다. **검증**: 충돌 마커 파일로 GET `corrupt:true` · POST `HTTP 409` · **파일 무손상 (마커 그대로)** · UI 배너 표시 확인 → 정상 파일 복구 후 GET/POST 정상 복귀 확인. viz 50/50 pass. **노트북 검증은 큐로 위임** — `_AGENT_COMMS/to_claude/20260827_claude_claude_컬렉션_디바이스정합_검증.md` (기준 상태 2 컬렉션 심어 커밋, 절차·기대 출력·충돌 시 대응 명시). 메인 PC 에서 할 수 있는 검증은 모두 끝냈고 **크로스 디바이스 실측만 남았다**. 작업자: Claude (메인 PC). |
| R195 | 2026-08-27 | 🕓 | 🕓 | **viz 노트 ★ 즐겨찾기 토글 — 컬렉션 UI 진입점 완성** (R193 의 미완분). 4 페이지 (explorer · tags · calendar · additions) 가 이미 `<button data-open-note="vault|path">↗</button>` 공통 규약을 쓰고 있어 그 이음새를 재사용했다 — 페이지마다 구현하면 4벌이 되고 `_collections.json` 쓰기 로직이 흩어진다. **신규** `viz/lib/note-star.js` — `starButtonHtml()` 마크업 + 델리게이트 클릭 핸들러 + 낙관적 반영 후 POST (실패 시 롤백). 페이지당 배선은 import·마크업 1곳·attach·detach 4줄. `viz/lib/note-star.test.js` 4 케이스 (HTML 이스케이프·경로 정규화·기본 상태·키 왕복). **함정 2건 (실동작에서만 드러남)**: ① `paintStars` 가 `textContent` 를 바꾸는데 그게 childList 변경이라 MutationObserver 가 다시 감지 → **무한 rAF 루프**로 클릭 처리가 먹통. → paint 를 idempotent 로 (이미 맞으면 DOM 미접촉) + paint 중 observer disconnect. ② **`requestAnimationFrame` 은 페이지가 화면에 그려지지 않을 때 발화하지 않는다** (백그라운드 탭·창 가림). 디바운스가 영영 안 돌아 별이 안 칠해졌다 → `setTimeout` 으로 교체. 테스트 환경 문제가 아니라 실사용 백그라운드 탭에서 그대로 재현될 버그였다. **검증**: viz 50/50 pass · explorer/tags/additions 브라우저 실동작 (초기 칠하기 · 토글 on/off · 재렌더 후 상태 유지 · 파일 정합 · 상위 클릭으로 안 샘) · 콘솔 에러 0 · 마커 orphan 0. **calendar 는 소스 레벨 대조만** — 날짜 셀이 ECharts 캔버스라 DOM 클릭으로 노트 목록을 띄울 수 없었다. 배선 4요소가 검증된 3 페이지와 동일함을 확인. 작업자: Claude (메인 PC). |
| R194 | 2026-08-27 | 🕓 | 🕓 | **병렬 위임의 인덱싱 순서 규칙 — 하네스 수정** (2026-06-12 사용자 보고 큐 처리). **재현 확인**: master_index 2,177 노트를 실제 파일과 전수 대조 → **STALE 0**. 6-12 이후 인덱스가 여러 번 재빌드되며 증상은 자가 치유됨. 구조적 원인만 남아 그것을 고쳤다. **근본 원인 확정** (`index-build.js` 실독): 인덱스 `notes` 배열은 **파일시스템 walk 결과로만** 채워지고 (증분 캐시 키는 content hash 가 아니라 **경로**), walk 에 안 걸린 기존 엔트리는 버려진다. → 이동이 **끝난 뒤** 빌드하면 항상 정확하고, **이동 중** 빌드하면 옛 경로·새 경로·누락이 섞인 부분 스냅샷이 박힌다. viz 가 그 경로로 `obsidian://` 를 쏘아 링크가 깨졌던 것. **인덱서 결함이 아니라 순서 문제.** **규칙 신설**: `_AGENT_COMMS/multi-worker-protocol.md § 11` (신설, 뒤 섹션 11~14 → 12~15 재번호) — 인덱싱을 워커 병렬 단위에 넣지 않고 오케스트레이터 마무리 단계로 격상 / 순서 고정 (변경 워커 전원 완료·검증 → 볼트별 `index build -i` → `master-build` 1회) / 경로 변경 작업은 예외 없이 지연 / 단독 소유 볼트의 경로 미변경 편집만 워커 `review` 허용 / 한 볼트 두 워커 동시 수정 금지. § 13 금지 2항 + 라이프사이클 + 워커 큐 템플릿 § 주의 (콜드 스타트 워커는 큐만 읽으므로) 에도 반영. **반영 위치**: `.agents/rules/custom/agent-ownership.md` · `.agents/commands/core/delegate-task.md` (정본) → agents-sync 로 `.claude/`·`.codex/` 전파. **`.codex/rules/agent-comms.md` 직접 보강** — 정작 사고를 낸 Codex 쪽에 멀티워커 언급이 **0건**이었다. `/delegate-task` 예시 3 (볼트별 인덱스 재빌드 병렬) 은 **서로 다른 볼트·인덱싱만·경로 변경 없음**이라 안전하다는 주석 추가 — 병렬 자체를 금지로 오해하지 않게. **작업 중 자기 실수 1건 (R081 위반)**: R192 를 `.claude/rules/core/`·`.claude/commands/core/` 에 편집했는데 그곳은 **전파본**이고 정본은 `.agents/` 였다. 이번 세션의 agents-sync 가 R192 편집 3건 + R194 편집 1건을 **되돌렸다**. `.agents/` 정본에 재적용 후 재전파, 2회차 sync `NOOP` 로 멱등 확인. 교훈: `.claude/rules/`·`.claude/commands/` 는 전파본이다 — 편집 전 `.agents/` 에 같은 경로가 있는지 먼저 본다. **검증**: STALE 대조 명령을 문서에 싣기 전에 실행 (`STALE_TOTAL=0`), 전 에이전트 디렉토리 전파 확인, agents-sync 2회차 NOOP. 작업자: Claude (메인 PC). |
| R193 | 2026-08-27 | 🕓 | 🕓 | **viz 컬렉션 (즐겨찾기 + 노트 묶음) — 커스텀 페이지 시스템 실구현** (2026-06-12 사용자 요청 큐 처리). **사용자 결정 3건**: ① 저장은 루트 `_collections.json` **git 추적** (`.vault_data/` 는 `.gitignore` 라 viz-prefs 처럼 쓰면 디바이스 비정합 — viz-device-sync 원칙 위배) ② 커스텀 기능 registry 에 **page surface 를 실제 구현** ③ **즐겨찾기를 별도 기능으로 두지 않고** `favorites` 기본 컬렉션 하나로 통합 (저장·UI·API 절반). **메운 공백**: R164 시점에 surface 타입으로 `page-section`·`home-card` 가 JSDoc 에만 선언돼 있고 **어댑터 구현이 없어 실제로는 `settings-row` 만 동작**했다. `lib/custom-features-router.js` adapter 신설 → registry 에 page surface 객체 1개 넣으면 router VALID_PAGES/PAGE_TITLES · header PAGE_TITLES_LOCAL · home 도구 카드에 **자동 병합** (3 파일 무수정). **신규**: `lib/collections.js` (스키마·정규화·토글, server/client 공용) · `lib/collections.test.js` (7 케이스) · `lib/custom-features-router.js` · `pages/collections.js` · `_collections.json`. **server.js**: `/api/collections` GET/POST + POST whitelist 추가, 원자적 쓰기 (tmp → rename). 입력은 `normalizeCollectionsFile` 로 정규화 — 경로 `..` 탈출 차단, 중복 제거, 즐겨찾기 강제 복원. **실사고 1건 (R188 재발)**: `noteKey` 의 구분자로 **리터럴 NUL 바이트**가 소스에 박혀 `file` 이 JS 를 "data" 로 판정 (git diff 가 바이너리로 보임). 게다가 `buildNoteLookup` 이 같은 키를 손으로 다시 조립(공백 구분자)해 **전 항목이 "인덱스에 없음" 으로 조용히 오표시**됐다 — 문법 오류가 아니라 `node --check` 도 통과. 수정: 이스케이프조차 안 쓰는 `String.fromCharCode(0)` + 키 생성을 `noteKey` 하나로 통합. viz 전체 JS **NUL 전수 스캔 0건** 확인. **검증**: viz 테스트 46/46 pass (신규 7 포함), 브라우저 실동작 — 페이지 진입·노트 추가·저장·재진입 후 제목 정상 표시·홈 카드 자동 노출·콘솔 에러 0, `_collections.json` 원자적 쓰기 후 `.tmp` 잔재 없음, git 추적 확인. 작업자: Claude (메인 PC). |
| R192 | 2026-08-26 | 🕓 | 🕓 | **Obsidian 볼트 등록 — 생성 플로우 차단 해제 + SessionStart 자동 등록** (2026-06-12 사용자 보고 큐 처리). **조사 확정**: 큐가 요구한 "Obsidian 재시작 없는 등록" 은 **구조상 불가능**. Obsidian 은 `obsidian.json` 을 시작 시점에 읽으므로 실행 중인 인스턴스에 새 볼트를 밀어 넣는 경로가 없다 (외부 조사 2건 일치 — URI 는 이미 등록된 볼트만 전환 가능, 커뮤니티 도구도 "등록 → 재시작 → 열기" 3단계). 즉 기존 차단 사유가 절반만 맞았다 — "메모리 덮어쓰기 손실" 외에 **"실행 중 인스턴스는 새 항목을 어차피 못 읽는다"** 가 진짜 이유이고, `--force` 강행은 손실 위험만 지고 얻는 게 없다. **변경**: ① `.claude/settings.json` SessionStart 에 `register-vaults --apply` 훅 추가 — Obsidian 이 닫혀 있고 미등록 볼트가 있을 때만 등록, 그 외 무출력 통과 (`case ... *APPLIED*` 로 래핑, sh 실동작 검증). ② `/create-vault` § 5 — Obsidian 실행 중이면 **볼트 생성 플로우를 멈추지 않는다** (§ 5-2 신설). "지금 다 닫으세요" 요구 금지. ③ `vault-individualization.md` § 볼트 생성 후 필수 작업 5 · `/register-vaults` 스킬 동일 반영. **설계**: 별도 대기 큐 파일 없음 — CLI 가 볼트 스캔으로 델타를 계산하므로 **볼트 존재 자체가 상태** (개발학습 dedup 과 동일 철학). 새 스크립트 0개. **검증**: 훅 커맨드 sh 실행 exit 0 + NOOP 무출력, 현재 63/63 등록 상태 확인, settings.json JSON 재파싱 유효 (백업 `settings.json.bak_20260826_233146`). **미확인**: `/create-vault` § 5-3 의 "URI 가 등록도 처리한다" 서술이 외부 조사와 충돌 — 금지 결론은 불변이라 실측 전까지 주석만 달아둠. 작업자: Claude (메인 PC). |
| R191 | 2026-08-08 | – | – | **AIMindVaults 로고 교체 + 작업표시줄 고정용 바로가기** — 사용자 제작 신규 로고 (AMV 원형 심볼) 적용. **로고 처리**: 투명 배경 원본 (`1024x1024`, 32bppArgb, 모서리 알파 0) 을 선별 — 같이 받은 1254px 본은 알파 없는 남색 배경이라 미채택. 알파를 마스크로 유지한 채 RGB 를 검정으로 치환 → 여백 크롭 (`843x843` 콘텐츠 영역) → 정사각 캔버스 정렬. Windows 테마가 **밝게** 라 검정 아이콘이 작업표시줄에서 정상 대비. **아이콘 생성**: 9 사이즈 PNG-embedded ICO (`16/20/24/32/40/48/64/128/256`). 얇은 선이 축소 시 회색으로 날아가는 문제를 **크기별 알파 부스트** (16px ×2.8 ~ 64px ×1.15) 로 보정 — 보정 전 16·24px 가 거의 안 보였음. ICO 구조 검증 9/9 유효 PNG 프레임 + 파일 크기 정확 일치. **배치**: `viz/_build/aimindvaults.ico` (정본) · `viz/viz.ico` 교체 (백업 `viz.ico.bak_20260808`) · 원본 2종 `viz/_build/assets/` 보관. **바로가기**: 시작 메뉴에 `AIMindVaults.lnk` 생성 (대상 = `viz/Generate Visualization.exe`, 아이콘 = 신규 ICO). **작업표시줄 고정은 사용자 수동 조작 필요** — Windows 11 이 프로그래밍 방식 고정 verb 를 차단 (`시작 화면에 고정` 만 노출, 실측 확인). 참고: `ps2exe` 미설치라 exe 내장 아이콘은 옛것 유지 — 바로가기 아이콘이 우선하므로 작업표시줄 표시엔 영향 없음. `.NET System.Drawing.Icon` 은 PNG-embedded ICO 프레임을 못 읽으므로 (미리보기 노이즈 발생) 검증은 바이트 파싱으로 수행. **후속 (같은 날)**: ① exe 재빌드 — viz.ico 교체만으로는 컴파일된 exe 의 내장 아이콘이 안 바뀌어 동봉 ps2exe (`_build/tools/ps2exe.psm1`) 로 재빌드 (61,952 → 87,040 bytes, 내장 아이콘 추출 시 불투명 416px 전부 어두운 색 확인). 앞선 "ps2exe 미설치" 판정은 설치 모듈만 조회한 오류 — 레포에 동봉돼 있었음. ② **`build-icon.ps1` 재작성** — 기존 스크립트가 옛 디자인 (중앙 노드 + 모서리 4노드) 을 코드로 그려 `viz.ico` 에 덮어쓰는 구조였고 `AGENTS.md`·`create-shortcut.ps1` 이 그 실행을 안내하고 있었음 → **실행 시 새 로고 소실 위험**. `_build/assets/logo_source_transparent.png` 를 입력으로 검정 단색화 + 크롭 + 9사이즈 + 알파 부스트를 수행하도록 교체, 재실행 시 인라인 작업과 **SHA256 바이트 동일** 확인 (재현성 확보). `AGENTS.md` 빌드 절차에 "icon 변경 후 build-exe 필수" 명시. ③ 백업 `viz.ico.bak_20260808` 제거 (git 이력에 보존). **참고 — 사용자 혼동 원인**: 빈 화면 (VAULTS 0 / NOTES 0) 신고는 viz 결함이 아니라 `C:\SellingVault\English\AIMindVaults\viz` (배포본 사본, exe 55,808 bytes / 2026-05-29) 를 실행한 것이었음. 배포본은 템플릿 볼트만 있어 시스템 볼트 필터 후 0 이 정상. 정본은 VAULTS 50 / NOTES 1,816 정상 확인. SellingVault 는 사용자 판단으로 현행 유지. 작업자: Claude (메인 PC). |
| R190 | 2026-08-08 | – | – | **인덱서 타임스탬프 UTC → 로컬 전환 — 날짜 하루 밀림 해소** — R189 백필 중 발견. `Date#toISOString()` 이 UTC 로 변환하므로 한국(UTC+9) 로컬 00:00~08:59 에 갱신된 노트가 **전날로 집계**되고 있었음. 실측: 2,076 노트 중 **1,416건 (68%)** 이 하루 밀린 날짜로 캘린더에 표시 (예: 로컬 2026-05-12 → 인덱스 2026-05-11). 볼트의 모든 날짜 (프론트매터 `created`/`updated`, 버전 로그, 백업 파일명) 가 사용자 로컬 달력 기준이므로 인덱스도 로컬로 통일. **변경**: CoreHub `src/lib/local-time.js` 신설 (`localIso`/`localDate`/`localStamp`) + 호출부 17곳 교체 — `index-build` (fsMtime·birthtime·built), `master-index-build` (built), `owned-tags-build` (builtIso 타임시리즈 날짜), `agents-sync`·`bump-version`·`clone-vault`·`core-sync-all`·`create-hub`·`rebase`·`tag-migrate` (버전 로그·백업 파일명 날짜). `core-sync-all` 의 machine ISO 레코드와 `sync-workspace` 쿨다운 파일은 기계 내부 기록이라 UTC 유지. **마이그레이션**: 증분 빌드가 content hash 동일 시 기존 엔트리를 재사용해 옛 UTC 값을 보존하므로 **전 56 볼트 비증분 전체 재빌드** 필요 (증분으로는 마이그레이션 안 됨 — 재발 시 주의). 검증: `local-time.test.js` 5/5 pass, 전 볼트 재빌드 56/56 성공, **UTC 잔존 0건** (mtime 로컬 873 / frontmatter updated 우선 1,204), `bump-version --broadcast` 로 6/6 Preset Hub + 위성 62곳 `local-time.js` 전파 확인, viz API 실측 (2026-08-08 갱신 501건, 시각 범위 00:00~18:32). 참고: `sync-all` 일괄 실행 시 pre-sync 10건 실패는 Obsidian 리로드 경합 — 개별 재시도 10/10 성공 (데이터 영향 없음). 작업자: Claude (메인 PC). |
| R189 | 2026-08-08 | – | – | **노트 `created` 에 생성 시각 기록 — 규칙 개정 + 기존 943건 1회 백필** — 사용자 결정. 배경: viz 생성일 기준에서 시각이 전부 `00:00` 으로 뜨던 문제. 원인은 프론트매터 `created: YYYY-MM-DD` (날짜만) 를 인덱서가 `T00:00:00` 으로 정규화하는 것 (2,076 중 1,886건). (1) **앞으로**: 신규 노트는 `created: YYYY-MM-DDTHH:MM:SS` (로컬 시각) 로 쓰고 **이후 절대 수정 금지** — 갱신은 `updated` 담당. `_essentials.md § 6` 에 규정 신설, `juggl-note`·`note-from-article`·`note-from-pdf`·`note-from-video` 스킬 4종 반영, `agents-sync` 로 `.claude/`·`.codex/` 미러 10 파일 전파 (verify NOOP). 인덱서 `normalizeCreatedField` 가 이미 전체 타임스탬프를 보존하므로 **Core 수정 불필요**. (2) **1회 백필**: 파일시스템 birthtime 의 **날짜가 프론트매터 날짜와 일치할 때만** 시각을 보강 (943 파일). 날짜 변경 0건. **birthtime 일괄 덮어쓰기는 기각** — 실측 결과 1,207건 (64%) 이 편집·git 으로 리셋돼 있어 (CONTENTS_AI_RULES 26일차, GDScript 9일차, System_Design 4일차) 맞는 날짜를 틀린 값으로 덮게 됨. git 이력 백필도 검토했으나 배치 커밋 시 수백 파일이 동일 시각을 받아 부정확 + 런타임 git 의존 → 사용자 판단으로 기각. 결과: 시각 있는 노트 190 → **1,134건 (55%)**. 검증: `created` 줄 외 변경 0건 (diff 전수), 인코딩 손상 0건, 29개 볼트 인덱스 + master 재빌드, viz 실측 (2026-05-06 생성일 43건 전부 실제 시각 표시). 스크립트는 `$TEMP` 일회용, 볼트 미보관. 작업자: Claude (메인 PC). |
| R188 | 2026-08-08 | – | – | **viz Sankey 테스트 실패 해소 + `buildOption.js` 바이너리 판정 제거** — (1) **테스트 수정**: `buildOptionConnections — Sankey 3-단` 실패 원인은 데이터 드리프트도 회귀도 아니었음. 이 테스트는 실데이터가 아닌 인라인 픽스처를 쓰며, 함수 본문은 직전 커밋 `8d2a8a12` 와 바이트 동일 (이 테스트는 통과한 적 없음). 진짜 원인은 **테스트가 링크 배열의 인덱스 순서를 단언**한 것 — 샘플이 자동 owned (`owner === tag`) 경로를 타는데, 구현은 tag 노드 in-degree 보장을 위해 `owner→tag` 링크를 user 링크들 뒤에 덧붙인다 (buildOption.js:409-414, 의도 주석 존재). 링크 내용은 처음부터 정확했고 순서만 달랐음. Sankey 는 배열 순서와 무관하게 그래프를 구성하므로 인덱스 단언을 **간선 집합 비교**로 교체 (회귀 탐지력 동일 — 간선 하나라도 깨지면 실패). (2) **리터럴 NUL → `\0` 이스케이프**: `pairKey` 의 구분자가 소스에 literal NUL 로 박혀 있어 git·grep·file 이 파일을 바이너리로 판정 → `git show`/`git diff` 가 "Bin 21050 → 22589 bytes" 만 출력해 회귀 조사 시 양쪽 버전을 꺼내 수동 비교해야 했음. 동작 동일 (U+0000 구분자, 교환법칙·충돌 방지 유지) 하면서 파일이 다시 UTF-8 텍스트로 인식됨 — `git diff --no-index` 로 텍스트 diff 복구 실증. 검증: 전체 스위트 39/39 pass (직전 38/1 fail), 브라우저 `#connections` Sankey 정상 렌더 + 콘솔 에러 0. 작업자: Claude (메인 PC). |
| R187 | 2026-08-08 | – | – | **viz 노트 목록 정렬 UI + 생성일 가짜 시각 표기 수정** — 사용자 지적 2건. (1) **정렬 기준 UI 신설** — `viz/lib/note-sort.js` 공용 모듈 (`sortNotes` / `sortGroups` / `timeLabel`) + 두 페이지에 세그먼트 토글 추가. 모드 2종: `recent` (기준 시각 내림차순, 동시각은 mtime → 제목 tie-break) · `catTitle` (카테고리명 → 제목, ko locale 가나다/A-Z). additions 는 flat 목록 정렬 + `sort=` 해시 파라미터 (뒤로가기·링크 공유 대응), calendar 는 일별 영역의 그룹 순서 (recent = 노트 수 / catTitle = 이름순) 와 그룹 내 노트 순서에 동시 적용. 정렬은 클라이언트 전용이라 재조회 없음. (2) **생성일 `00:00` 오표기 수정** — frontmatter `created: YYYY-MM-DD` 를 인덱서가 `T00:00:00` 으로 정규화하는데 (2,076 중 1,886건) 이를 자정 생성처럼 `00:00` 으로 그리고 있었음. 시각 정보 없는 스탬프는 `—` 로 표기. 검증: `node --test lib/note-sort.test.js` 9/9 pass, 브라우저 실측 (정렬 토글·해시 반영·카테고리 A-Z·생성일 201건 전부 `—`·콘솔 에러 0). 작업자: Claude (메인 PC). |
| R186 | 2026-08-08 | – | – | **Codex `create-video-note` 모드 분기 + 화면 정보 분석 반영** — 사용자 피드백에 따라 `.codex/skills/create-video-note/SKILL.md`를 R185 정본 취지와 맞춰 보강. 노트 시작 시 `학습 정보 정리`(`study-note`)와 `컨텐츠 요약`(`reference`)을 강제 선택하고, 컨텐츠 요약은 영상 작업·레퍼런스 수집용으로 맥락·전개·장면·연출·톤·참고 포인트를 남기도록 분리. 자막 없음 또는 개발·알고리즘·UI 시연처럼 화면 정보가 핵심인 영상은 `yt-dlp` 저화질 확보 + `ffmpeg` 프레임 샘플링으로 코드·수식·다이어그램·설정값을 확인하도록 명시. 검증: `agents-sync --verify` NOOP, `quick_validate.py` `Skill is valid!`. 작업자: Codex. |
| R185 | 2026-08-08 | – | – | **`/note-from-video` 노트 모드 2종 분리 + 화면 정보 분석 추가** — 사용자 결정 (2026-08-08): (1) **노트 모드 강제 선택** — 학습 정보 정리 (기본, `study-note`, 영구 사용 정보 노트 — 기존 방식) vs 컨텐츠 요약 (`reference`, 컨텐츠의 맥락·구성·연출 파악 — 영상 작업·컨텐츠 레퍼런스 수집용, JissouGame 컨텐츠 수집 계열). (2) **화면 정보 분석** — 자막 없는 영상은 프레임 추출 (`yt-dlp` 저화질 + `ffmpeg -ss` 챕터 시작점·간격 샘플링 → Read 분석) 로 내용 파악, 자막이 있어도 개발 시연·알고리즘 설명 등 화면 비중 높은 영상은 프레임 확인 병행 (화면 정보가 스크립트보다 중요한 경우 다수). 2단계 자막 없음 fallback 을 화면 분석 전환으로 교체. 정본 `.agents/commands/core/note-from-video.md` — agents-sync 전파 필요. 판매 중지 상태라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R184 | 2026-08-08 | – | – | **Codex 영상 노트 스킬 품질 기준 보강** - 최근 Claude 영상 노트 산출물과 기존 Codex 스킬을 비교해 `.codex/skills/create-video-note/SKILL.md`를 전면 보강. 30분 이상 영상의 폴더 노트 + 챕터 노트 분기, `source_id` dedup, 원어 자막 우선 기준, ZK Domain Preset 태그 규약, 원본 링크 본문 명기, MOC/인덱스 갱신, post-edit review/index 완료 기준을 Codex 전용 스킬에 고정. 검증: `quick_validate.py`는 임시 PyYAML + `PYTHONUTF8=1`로 실행해 `Skill is valid!` 확인. 작업자: Codex. |
| R183 | 2026-08-07 | – | – | **R181 nul 종결 기록 정본 이식 — 전파본 전용 편집 사고 복구** — R181 (2026-08-05) 에서 `shell-redirect-safety.md` 의 § 2 훅 커맨드 라인 + § 5.9 Incident 종결 섹션을 **전파본 (`.claude/rules/core/`) 에만 추가**하고 정본 (`.agents/rules/core/`) 미반영 → 2026-08-06 agents-sync 가 낡은 정본으로 전파본을 덮어써 워킹트리에서 R181 내용이 삭제된 상태를 커밋 전 diff 검토에서 발견. HEAD 의 전파본 내용을 정본에 이식 후 agents-sync 재전파 (Updated 2 — `.claude/` + `.codex/`), 전파본 == HEAD 복원 확인. R081 편집 모드 규칙 (정본·전파본·생성물 분류 선행) 재확인 사례. 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R182 | 2026-08-06 | – | – | **post-edit-review 볼트 전용 type 합집합 + 인덱서 aliases 인덱싱 (2026-07-01 Core 큐 처리)** — note-types.yaml rules 3 에 명시만 되고 미구현이던 볼트 전용 type 검증 합집합을 구현. `post-edit-review.js` 에 `loadVaultTypes()` 신설 — 대상 볼트 CLAUDE.md 의 "볼트 전용 type 선언" 라인에서 backtick 토큰 추출 (라인의 괄호 이후는 주석으로 제외) 후 코어 사전과 union. XG5000 실측: `type-not-core` WARN 56 → 0 — ZK 6종 (`fleeting`/`literature`/`permanent`/`moc`/`synthesis`/`pattern`) 전부 통과, 잔여 1건은 `Contents/_index.md` 의 미등록 type `meta` 정당 검출이라 코어 타입 `folder-index` 로 정정 (EXCLUDE_TYPES 아님 — 인덱스 유지). **WARN→BAD 격상 데드라인 (2026-08-01) 경과 리스크 해소**. **함께 구현**: `index-build.js` 노트 오브젝트에 frontmatter `aliases` 필드 인덱싱 + `index-search.js` 스코어러 aliases 매칭 (title 급 가중치 10) — XG5000 명령어집 니모닉 검색이 headings 우회 없이 직접 히트 (실측: full rebuild 후 aliases 노트 53/55, `index search -q "MOV"` → `xgk-cmd-11-데이터-전송-명령` score 13 = alias 10 + heading 3). master 인덱스는 슬림 노트 형태 (headings·summary 미포함) 유지 방침이라 aliases 미전파 (단일 볼트 검색 전용 — 검토 결론). CoreHub 정본 편집 → `bump-version --broadcast` Preset Hub 6/6 전파, `CORE_SYNC_FAILED=0`. XG5000 CLAUDE.md 의 "합집합 구현 전까지 무해 경고" 낡은 주석 제거. 원본 큐 `_AGENT_COMMS/to_claude/20260701_claude_claude_core_review_type합집합_aliases인덱싱.md` resolved + archive. 판매 중지 상태라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R174 | 2026-08-03 | – | – | **개발학습 플레이리스트 정리 스킬 신설 + agents-sync 도메인 감지 갭 수정** — 사용자가 모아두는 YouTube "개발학습" 플레이리스트 (81건, 계속 추가) 를 "개발학습 정리" 한 마디로 신규 영상만 골라 노트화하는 스킬. 신규 스크립트 0 — Codex 스킬 `create-channel-video-notes/scripts/list_channel_videos.py` (yt-dlp `--flat-playlist` 래퍼) 를 그대로 호출하고, 노트 생성은 기존 `/note-from-video` 파이프라인에 위임. **dedup 은 상태 파일 없이 노트 자체로** — 영상 ID 목록을 `rg -F -f ids.txt -g "*.md" Vaults` 한 번 (실측 1.6초) 으로 스캔해 `source:` frontmatter 에 이미 박힌 영상 제외 (실측 81건 중 44건 기노트 / 37건 신규). 라우팅은 루트 CLAUDE.md 키워드 매핑 + 본 플레이리스트 빈출 주제표 (Godot · Blender · DevFoundation · AI_Coding · IndustrialControl · GameArt · ContentCreation), 10건 초과 시 처리 전 확인. 알려진 함정 기록: `--print-table` 은 cp949 콘솔에서 이모지 제목에 `UnicodeEncodeError` → JSON 출력만 사용 + `PYTHONIOENCODING=utf-8`. 정본 `.agents/commands/custom/DevLearning/dev-learning-digest.md` + `.agents/rules/core/_skill-router.md` 트리거 1행. **함께 수정 (Core)**: `agents-sync` 의 `listDomains()` 가 `.agents/rules/custom/` 하위 디렉토리만 도메인으로 인식해 **룰 없이 커맨드만 있는 도메인이 전파에서 조용히 누락** 되던 갭 — `rules/custom` + `commands/custom` 합집합으로 변경 (CoreHub 정본 편집 → `bump-version --broadcast`, Preset Hub 6/6 전파, `CORE_SYNC_FAILED=0`). 수정 후 도메인 감지 9건 (DevLearning · CreateVault 신규 인식), `.claude/commands/custom/DevLearning/` + `.codex/skills/custom/DevLearning/` 생성 확인. 사용자 개인 학습 워크플로우라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R175 | 2026-08-03 | 🕓 | 🕓 | **영상 노트 규칙 2건 — 30분 이상 폴더 구조화 + 본문 영상 링크 명기** — 사용자 결정 (2026-08-03): 긴 영상을 노트 1개에 밀어넣는 방식 폐기. `/note-from-video` § 5 정본 개정 — (a) **§ 5-0 길이 분기**: `duration` 30분 미만 = 단일 노트, 30분 이상 = `{노트명}/` 폴더 + 폴더 노트 (`type: folder-index`, 전체 요약 + 원본 링크 + 챕터 노트 목차 위키링크) + 챕터 노트 N개 (`type: study-note`, 3~8개 목표 — 챕터 20개짜리 강좌는 인접 챕터를 주제 단위로 묶음, 각 노트 완결성 + 폴더 노트 역링크 필수). 폴더명도 URI 예약문자·이모지 금지 동일 적용. (b) **§ 5-2 링크 명기**: frontmatter `source` 로 끝내지 않고 H1 + Juggl 바로 아래 `> 원본 영상: [제목](URL) — 채널 · 길이` 줄 강제, 챕터 노트는 구간 타임스탬프 링크 (`https://youtu.be/{id}?t={초}`) 사용. (c) 폴더 구조 시 폴더 노트·챕터 노트 **전부** 동일 `source`/`source_title`/`source_channel` — 하나라도 빠지면 R174 dedup 이 중복 생성. `/dev-learning-digest` 는 정본 위임 + 배치 보고 형식만 보강 ("신규 N건 (그중 30분 이상 M건 → 폴더 구조)"), 플레이리스트 `--flat-playlist` 결과에 duration 이 비어 있으므로 길이는 노트 직전 `yt-dlp --dump-json` 으로 확인, 자막 없는 30분 이상 영상은 쪼개지 않고 단일 노트 + 한계 명시. agents-sync 4건 전파 (core/note-from-video + custom/DevLearning × Claude·Codex). core 스킬 개정이라 배포 대기 (🕓/🕓). 작업자: Claude (메인 PC). |
| R176 | 2026-08-03 | – | – | **Design 볼트 신설 (Domain_Art) + 개발학습 플레이리스트 30분 미만 23건 노트화 완료** — R174 스킬로 수집한 신규 37건 중 **30분 미만 전량 처리**. (1) **Design 볼트 신설** — 타이포·폰트·디자인 AI 계열을 ArtInsight (미적 감각·담론) · GameArt (게임 비주얼) · LightAndColor (색채 이론) 어디에도 섞지 않겠다는 사용자 결정에 따라 전용 볼트 생성. `clone` (hub=AIHubVault, hubId=default) → CLAUDE.md 개별화 (역할·수집 범위 6항목·"수집하지 않는 것" 경계표 8행 + "결과물을 어떻게 만드는가=Design / 왜 좋아 보이는가=ArtInsight / 색이 어떻게 작동하는가=LightAndColor" 판단 기준) → `Contents/Domain/` 4 폴더 (Typography·Layout·AI_Workflow·Reference) + Domain.md 개별화 → `_STATUS.md` 초기화 → 루트 `_STATUS.md` Domain_Art 레지스트리 1행 + 루트 `CLAUDE.md` 키워드 라우팅 1행 → 인덱스 빌드. **obsidian.json 등록 미완** (Obsidian 실행 중 — `register-vaults` dry-run `TO_ADD=1` 확인, 종료 후 `--apply` 필요). (2) **노트 23건** — Blender 4 · DevFoundation 6 (ZK 프리셋이라 `Contents/_AI_Drafts/devlearning/` + `Zk`/`AiDraft`/`Literature` 태그 규약 준수) · Design 4 · AI_Gen4Game 2 · AI 2 · AI_Coding 2 · GameArt 1 · Godot 1 · ContentCreation 1. 전 볼트 `BAD=0` · `TAG_VIOLATIONS=0` · `INDEX_UPDATED=1`, 신규 노트 WARN 0. R175 규칙대로 전 노트 본문 상단에 원본 링크 줄 + frontmatter `source` 3종. 한국어 자동자막 오인식은 노트 하단에 보정 표로 명시 (변수 네이밍 영상은 인식 품질이 낮아 논지 재구성임을 고지). (3) **제외 2건 확정** — PLC·로봇제어 실무 계열 (미쓰비시 PLC 중급3편, C언어 로봇제어 182분). 스킬 라우팅표에 제외 규칙 + 통계/디자인 분기 반영. **남은 작업**: 30분 이상 12건 (폴더 구조 대상, 최장 240분). 작업자: Claude (메인 PC). |
| R177 | 2026-08-04 | – | – | **개발학습 플레이리스트 30분 이상 7건 폴더 구조 노트화 (R175 규칙 첫 적용)** — R176 에서 30분 미만 23건을 끝낸 뒤 대형 영상 처리. **폴더 + 폴더노트(`folder-index`) + 챕터 노트** 구조로 총 7 폴더 · 챕터 노트 44 · 폴더노트 7 = **51 노트**. 내역: 32m AI 영상 자동화 (ContentCreation, 챕터 5) · 33m Phaser 2D 에이전트 스킬 (AI_Coding, 5 — 사용자 규칙 "Godot+AI 는 Godot 분류" 에 따라 엔진 확인 결과 Phaser JS 라 AI_Coding 유지) · 37m Blender 리깅 (Blender, 5) · 37m 커넥션 풀 (DevFoundation, 5) · 53m System Design (DevFoundation, 7) · 68m 컴공과 진로 (Career, 6) · 75m LeetCode DSA 패턴 (DevFoundation, 8) · 194m 바이브코딩 1인창업 (AI_Coding, 8). 원본 챕터가 최대 51개(194m)라 **주제 단위로 3~8개로 묶는** R175 규칙을 실적용 — 챕터 마커가 없는 영상(37m 커넥션 풀)은 주제 전환 기준으로 직접 분할. 각 챕터 노트에 구간 타임스탬프 링크(`?t=`) + 폴더노트 역링크. **사용자 지시 (2026-08-04): 요약 압축보다 내용 보존 우선** (영상과 대조 예정) → 이후 노트는 개념·근거·수치를 원본에 가깝게 보존하고 실시간 코딩 시연만 절차 요약, 그 기준을 폴더노트에 명시. 전 볼트 `BAD=0` · `TAG_VIOLATIONS=0` · `INDEX_UPDATED=1`, 신규 노트 WARN 0. **남은 4건** (223m Blender 입문 · 240m DSA 풀코스 · Godot 2건은 사용자 지정 후순위) 은 다음 세션 — 대형 영상은 1건당 한 세션 권장 (194m 자막 12만 자). 사용자 개인 학습 워크플로우라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R178 | 2026-08-04 | – | – | **루트 위생 정리 — nul 파일 14건 · 워크트리 잔재 3건 · Design 볼트 obsidian.json 등록** — (1) **nul 14건 삭제**: 핸드오프에는 Godot 볼트 1건만 기록돼 있었으나 `cmd dir /A /B /S` + `findstr` 정식 스캔 결과 14건 (루트 · `_AGENT_COMMS` · CoreHub 및 `.trash` · Blender · DevFoundation · Godot 3곳 · AI · Project_AIMindVaults · Project_MyVaults · 워크트리 2곳). `shell-redirect-safety.md § 7` 대로 PowerShell `Get-ChildItem -Filter nul` 은 사용하지 않음 (과거 41,440건 오탐 방식). `[System.IO.File]::Delete('\\?\...')` UNC 접두사로 14/14 삭제, 실패 0, 재스캔 0건 확인. (2) **워크트리 잔재 정리 (R110)**: `great-hodgkin-091bae`(빈 폴더, 미등록) · `upbeat-wright-92b4bc`(빈 폴더, 미등록) · `upbeat-greider-7ad560`(실제 worktree, detached HEAD `116421b6`). greider 는 삭제 전 3중 검증 — 작업 트리 변경 없음 · `116421b6` 이 main 조상 · 브랜치 `claude/upbeat-greider-7ad560` 도 main 에 완전 포함. `git worktree remove` + `branch -D` + 빈 폴더 제거 + `prune`, 잔여 0. (3) **Design 볼트 등록**: R176 에서 Obsidian 실행 중이라 차단됐던 건. Obsidian 창 0개 확인 후 dry-run `TO_ADD=1` → `--apply`, `ADDED=1`, 백업 `obsidian.json.bak_20260804_015010`. **미결로 남긴 것**: obsidian.json 에 `AppFlowy` / `Appflowy` **대소문자만 다른 중복 항목 2건** — 실제 폴더는 `Appflowy`(소문자 f) 이고 정상 볼트이나, 루트 `_STATUS.md` 레지스트리와 CLAUDE.md 키워드 매핑은 `AppFlowy` 표기. 볼트 스위처 중복 노출 상태. 폴더 rename(A) 과 표기 하향 통일(B) 중 사용자 결정 대기. 루트 운영 정리라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R179 | 2026-08-04 | – | – | **AppFlowy 볼트 디렉토리 대소문자 정합 — `Appflowy` → `AppFlowy` rename (R178 미결 해소)** — 사용자 결정: **표기 일관성 우선** ("데이터에 계속 예외를 두면 나중에 피곤함"). 2026-05-13 R115 에서 "AppFlowy 디렉토리 rename 보류" 로 남겨둔 건을 종결. **작업**: (1) Windows 대소문자 무시 파일시스템이라 `git mv` 2단계 (`Appflowy` → `AppFlowy_tmp` → `AppFlowy`) 로 rename, 166 파일 경로 갱신 확인. (2) obsidian.json 에 있던 **대소문자만 다른 중복 항목 2건** 중 소문자 `9adcb2d9fbdd0614` 제거 — 규칙대로 `ConvertFrom-Json`/`ConvertTo-Json` 왕복 없이 인덱스 기반 텍스트 삭제 + `json.loads` 유효성 검증, 사전 백업. register-vaults 재확인 결과 **62 scanned / 62 matched / TO_ADD=0**, AppFlowy stale 소멸. (3) vault_index + master 전체 재빌드 — partial 만으로는 master 에 옛 `Appflowy` 슬라이스가 남아 full rebuild 필요. **부수 효과 (이번 rename 의 실질 이득)**: 기존에는 디렉토리가 `Appflowy` 라 vault_id 도 `Appflowy` 였는데 **정작 그 태그를 쓰는 노트는 0건이고, 실제 사용 태그 `AppFlowy`(5건) 는 owner null 일반 태그로 떨어져 있었다.** rename 으로 vault_id 와 태그가 일치해 **자동 owned 성립**, TagOwners 중복 항목 1건 해소 (329 → 328), `OwnerConflicts=0`. 볼트 `Tags/OWNED_TAGS.md` frontmatter·표·비소유 태그 설명과 루트 `Tags/OWNED_TAGS.md` 항목도 갱신 (변경 이력 섹션 추가). 루트 `_STATUS.md` 레지스트리와 `CLAUDE.md` 키워드 매핑은 원래 `AppFlowy` 표기였으므로 **수정 없이 자동 정합**. **의도적으로 손대지 않은 것**: `_ROOT_VERSION` 과거 항목 · `_AGENT_COMMS/archive/` · viz 스냅샷 · 교차검증 노트의 소문자 표기 — 그 시점의 사실 기록이므로 보존. `.claude/settings.local.json` 의 과거 Move-Item 권한 문자열도 이미 실행된 일회성 기록이라 유지. 루트 운영 정리라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R181 | 2026-08-05 | – | – | **`nul` 재발 원인 특정·종결 — 전역 Stop 훅의 `taskkill /F ... 2>nul` (R180 후속)** — R180 이 봉쇄만 하고 원인 미상으로 남긴 건을 해결. **경위**: pre-commit 훅이 커밋마다 `nul` 을 차단 (실전 3회) → 재발분이 이전과 달리 **80 bytes** 라 내용을 CP949 로 판독 → taskkill 에러 문구 "잘못된 인수/옵션 - **'F:/'**" 발견 → `/F` 가 `F:/` 로 바뀐 것은 **MSYS 경로 변환** = bash 경유 실행의 지문 → 전역 `~/.claude/settings.json` Stop 훅 `taskkill /F /IM serena.exe 2>nul \|\| true` 특정. **훅 커맨드 문자열은 sh (Git Bash) 로 실행**되므로 (a) MSYS 가 `/F` 를 망가뜨려 taskkill 이 항상 실패하고 — 즉 **serena 종료 기능도 4개월간 동작한 적이 없다** — (b) `2>nul` 이 에러를 literal 파일로 기록, (c) **매 턴 종료마다 발화**해 세션 내 명령 배제 테스트 전체가 헛돌았으며, (d) PreToolUse Bash 훅 검사 범위 밖이라 자동 차단도 불가했다. 파일 위치가 옮겨 다닌 것 (루트·attachments·Review 폴더) 은 훅 CWD 가 지속 셸의 마지막 cwd 를 따라간 것. 이 리스크는 **2026-04-23 교차검증 노트에 이미 기재돼 있었으나 방치**된 건이었다. **수정**: 전역 훅을 `taskkill //F //IM serena.exe 2>/dev/null \|\| true` 로 교체 (이중 슬래시 = MSYS 변환 방지, 재현 테스트로 정상 파싱 + nul 미생성 확인). **룰 반영**: `shell-redirect-safety.md` § 2 에 "훅 커맨드 = sh 실행 + 이중 슬래시 이스케이프 + PreToolUse 검사 밖" 명시, § 5.9 종결 기록 신설. 교훈 3: 주기적 재발은 주기적 트리거 (훅·스케줄러) 부터 의심 / 발견 파일은 삭제 전 크기·타임스탬프·내용 판독 (빈 파일이 아니면 내용이 작성자를 지목한다) / 훅 커맨드는 작성 시점 사람 검증 필수. 판매 중지 상태라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R180 | 2026-08-05 | – | – | **`nul` 재발 대응 — 탐지 방법 결함 발견 + pre-commit 워킹트리 스캔 설치** — 세션 중 `nul` 파일이 반복 생성돼 원인 추적. **원인 특정 실패, 다만 범위는 좁혔다**: 툴 사용 로그 전수 검색 결과 Bash 도구의 CMD 구문 호출 **0건** (`block_nul_redirection.py` PreToolUse 훅 정상 작동 확인), `cli.js review`·`agents-sync`·`git add/commit`·Godot 헤드리스 실행 모두 재현 실패, 상주 프로세스는 Adobe CC node 뿐. 남은 후보는 세션 밖 (Obsidian Shell Commands · Windows 훅 비활성인 Codex · 사용자 직접 실행 `.bat`/터미널). **핵심 발견 — 탐지 방법이 틀렸다**: 예약 이름을 **인자로 직접 넘기면** 셸·API 가 디바이스로 해석해 실제 파일이 있어도 "없음" 을 반환한다. 실측 대조 — `cmd dir /A /B <경로>\nul` **File Not Found** · `Test-Path -LiteralPath '\\?\...'` **False** vs `cmd dir /A /B /S` (경로 인자 없이 트리 열거) **찾음** · Python `os.walk` **찾음** · `[System.IO.File]::Exists('\\?\...')` **True** (0 bytes 실파일). 이 세션에서 "잔여 0건" 을 반복 보고한 검증이 전자 형태여서 **원인 배제 테스트가 한동안 무효**였다 (정확한 방법으로 재실행해 배제 결과 자체는 유지 확인). **두 번째 발견 — pre-commit 만으로는 못 막는다**: Windows 에서 생기는 stray `nul` 은 **git 이 스테이징조차 못 한다** (`git add -A` 후에도 미추적). 커밋 게이트는 타 OS 발 히스토리 오염만 막고 로컬 워킹트리 오염은 못 잡는다. **조치**: (1) `.claude/templates/ci/` 의 pre-commit 2종을 `.git/hooks/` 에 설치 (`chmod +x`), (2) `check-reserved-names.sh` 를 **staged 목록 + 워킹트리 열거 병행** 으로 보강 (`.git`·`node_modules`·`.godot` 제외, 실측 약 1초) — 생성은 못 막아도 **방치를 막는다**, (3) `git diff --cached` 실패 시 `check=False` 로 조용히 통과하던 무력화 경로를 returncode 확인으로 수정, (4) 템플릿 정본 동기화, (5) core 룰 `shell-redirect-safety.md` § 5.3 · § 7 정정 — 탐지 방법 대조표와 "존재 확인과 탐색은 다른 도구" 원칙 명시. 훅 동작 검증: 실제 `nul` 존재 시 `Blocked reserved Windows names: .\nul` + exit 1, 부재 시 통과. 판매 계획 중지 상태라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R173 | 2026-07-29 | – | – | **Godot 공식 문서 MOC 골격 20건 + 원문 미복제 결정** — R172 볼트에 공식 문서 대응 구조 구축. **실측** (`godotengine/godot-docs`): .rst 1,596 (`classes/` 1,079 = 68% · `tutorials/` 398 · `engine_details/` 65 · `getting_started/` 33 · 기타 19) + 이미지 1,834 + 저장소 655 MB. 기준 버전 Godot 4.7 stable. **원문 복제 기각** — 근거 4건: (a) 전체 55 볼트 합계 1,550 노트인데 문서 하나가 1,596 → 외부 참조가 개인 지식 총량 초과, viz·master 인덱스 잠김, (b) git 에 655 MB 영구 적재 + 주간 갱신 커밋 누적 → 디바이스 clone 비용 상시화 (R170 cold start 개선과 상충), (c) **인덱서가 본문 비색인** (title/tags/headings/summary 만 — 2026-07-01 Core 큐 미처리 항목) 이라 복제해도 설명·코드 예제 검색 불가, (d) `classes/` 68% 는 에디터 `F1` 이 설치 버전과 자동 일치 제공 → 사본은 버전 어긋나면 오정보. **채택**: 원문은 에디터 F1 · 공식 사이트 · 공식 오프라인 HTML zip (gitignore) 로 두고 볼트에는 직접 작성 지식만. MOC 20건 (`Contents/Domain/` Concepts 6 · Pipeline2D 4 · Pipeline3D 3 · Guides 5 · Troubleshooting 2) 각각 공식 문서 링크 + 다루는 범위 + Project_Murim/Project_SkyDog 연결 + 빈 섹션 구조. 제목·헤딩은 색인되므로 검색 동작. 크로스 볼트 위키링크는 Obsidian 미해석이라 프로젝트 볼트 참조는 평문 처리. 결정 근거 노트 `Concepts/Godot_문서_정리_방식_결정.md` (재검토 조건 3건 명시). `Domain.md` 진입점 갱신 + `Review/Review.md` 인덱스 (버전 명시 강제 규칙). 검증 `BAD=0` · `TAG_VIOLATIONS=0` · `INDEX_UPDATED=1` (Notes 25 / Tags 54 / viz 표시 20). 작업자: Claude (메인 PC). |
| R172 | 2026-07-29 | – | – | **Godot 엔진 도메인 볼트 신설 (Domains_Game)** — Unity·UnrealEngine 과 같은 계층의 엔진 도메인 볼트. R171 의 게임 프로젝트 2종이 모두 Godot 기반이라 엔진 지식 축적처가 필요. **조치**: `node cli.js clone` 으로 BasicContentsVault → `Vaults/Domains_Game/Godot/` 복제, hub=`AIHubVault` (hubId=default) — 2026-06-05 UnrealEngine 생성 시 사용자가 결정한 "일반 주제형 AIHubVault + BasicContentsVault 템플릿" 선례 준수 (Domain Preset ZK 미사용). CLAUDE.md 개별화 — 역할·수집 범위 10항목·"수집하지 않는 것" 경계표 9행 (Unity/UnrealEngine 타 엔진, GameDesign/GameArt 엔진 무관, Blender 외부 DCC, AI_Gen4Game, Project_Murim/Project_SkyDog 프로젝트 구현 결정, Python/JavaScript/DevFoundation 언어 일반) + 태그 규칙 + 버전 명시 원칙. `Contents/Domain/` 하위 6 폴더 신설 (`Review` · `Guides` · `Concepts` · `Pipeline2D` · `Pipeline3D` · `Troubleshooting`). `_STATUS.md` 초기화. **템플릿 결함 fix**: 클론 소스 `BasicContentsVault` 의 `Contents/Contents.md` 가 **빈 파일**이라 review `BAD=1 (no-frontmatter)` 발생 → frontmatter + H1 + Juggl + Index 채움. `Domain.md` · `Project.md` 도 frontmatter 만 있고 tags·H1·본문 부재 (`no-tags,no-wikilink,no-h1` WARN) → 동일 보강. **이 결함은 BasicContentsVault 템플릿 자체에 있어 이후 모든 클론에서 재발한다 — 템플릿 정본 수정이 별도 트랙으로 필요** (본 사이클에서는 Godot 볼트만 국소 수정). 루트 `_STATUS.md` Domains_Game 레지스트리 1행 + 루트 `CLAUDE.md` 키워드 라우팅 (R171 에서 프로젝트 볼트로 임시 지정했던 Godot 행을 본 볼트로 정정). 검증 `BAD=0` · `TAG_VIOLATIONS=0` · `INDEX_UPDATED=1` (Notes 6 / Tags 3, 잔여 WARN 7 은 전부 클론 템플릿 기존 항목). obsidian.json 등록 완료. 사용자 개인 도메인이라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R171 | 2026-07-29 | – | – | **게임 프로젝트 볼트 2종 신설 (Projects_Game) + 판매 계획 중지 결정 기록** — 사용자가 Godot 게임 개발로 방향 전환하며 신규 프로젝트 2건 착수. (1) `Project_Murim` — 무림 세계관 일대기 RPG (Godot 2D, 도트, isometric 지면 + 타일 건물). 운명 설정 후 시작, 일대기 완성 시 새 운명 재시작, 이전 캐릭터 업적이 다음 캐릭터 기연으로 계승 (1회용, 획득 상한 없음). 정·사·마 3세력 베이스. 주요 시스템 = 기연 / 무공제작 + 비급화 / 영약 / 무공 상극 + 스탯 / 절차적 생성 시간 흐름 (마을·지형·정세·문파 흥망·무림공적 수배 단계·인연). (2) `Project_SkyDog` — 강아지 비행 배달부 방치형 게임 (Godot 3D). 주기적 우편물 배달로 자원 획득 → 퀘스트 → 스토리 체험이 보상. 재화 2원화 (인게임 = 배달 / 유료 = 광고 시청 또는 구매), 스토리를 인게임 재화로 구매하는 것이 핵심 컨텐츠 상품, 스킨은 유료 재화. **조치**: `node cli.js clone` 으로 BasicContentsVault → 양 볼트 복제 (379 파일 each), hub=`AIHubVault_Project` (hubId=project, 기존 Project 계열 관례) → CLAUDE.md 개별화 (역할·수집 범위·수집하지 않는 것 경계표·태그 규칙·note-types 미등록 타입 주의) + `_STATUS.md` 초기화 (Now/Next/Decisions) + 사용자 구술 기획을 `Contents/03_Design/` 에 `design` 타입 브리프로 기록 (확정 사항과 미결 항목 분리, AI 임의 추가 0) → 루트 `_STATUS.md` Projects_Game 레지스트리 2행 + 루트 `CLAUDE.md` 키워드 라우팅 3행 (무림 계열 / 강아지 배달 계열 / Godot 공통) → 초기 인덱스 빌드. 검증 양 볼트 `BAD=0` · `TAG_VIOLATIONS=0` · `INDEX_UPDATED=1`. **함께 기록**: 판매 계획 중지 결정 (2026-07-29) — AIMindVaults·viz 를 개인용 도구로 전환, SellingVault 배포·`/distribute`·viz 영문화·수익 정산 채널 검토·마케팅 자산 트랙 동결. 루트 `_STATUS.md` + Project_AIMindVaults `_STATUS.md` § Decisions 기록. **미완**: obsidian.json 등록 (Obsidian 실행 중이라 `register-vaults` 차단 — 종료 후 재시도 필요). 사용자 개인 프로젝트라 배포 무관 (–/–). 작업자: Claude (메인 PC). |
| R170 | 2026-07-13 | 🕓 | 🕓 | **viz 새 디바이스 cold start 자동 부트스트랩 — npm install 선행 + sync-all full + exit code 정직 보고** — 사용자 보고 "컴퓨터 옮길 때마다 시각화 백 안 돎" 전수 검토 + 수정. 원인: 부트스트랩이 Setup New Device.bat 수동 의존 + viz bg sync 축약판 (CoreHub npm install 없음 → fresh clone 에서 `ERR_MODULE_NOT_FOUND` 즉사, `--skip-npm` → 볼트 로컬 cli spawn 전멸, exit code 미확인 → **done 허위 보고**). 조치 (`Start-Visualization.ps1` + exe 재빌드 60.5KB): cold start 감지 → step 0 npm_install (CoreHub 의존성) → sync-all `--skip-npm` 제거·`--root` 명시 (볼트 node_modules 자동 설치, 존재 시 즉시 skip) → exit≠0 failed 정직 보고 (로그 경로 포함) → cold start 는 R149.2 skip 무시 ("새 디바이스 초기화 — 수 분 소요" 배너) → git pull exit code 판정 → R133 동기 fallback AUTO_SYNC off 전용 강등. core 룰 `viz-device-sync.md` **정본 (.agents/rules/core) 편집 + agents-sync 전파** (전파본 직접 편집이 sync 에 되돌려지는 것 확인 — R081 정본 원칙 재확인). 매니페스트 § 6.16 행 6건. 검증: 웜 E2E 3s done 회귀 0 + cold 분기 스크래치 (npm 실행 + exit 1 → failed). 남은 트랙: obsidian.json 불일치 감지 배너 (R168 이월) · server fs.watch 부재 파일 폴백 · sync-all 단일 프로세스 리팩토링 · 배포 zip MotW. 검토 노트: Project_AIMindVaults `20260713_viz_새디바이스_부트스트랩_자동화_검토`. 작업자: Claude (회사 개인컴). |
| R169 | 2026-07-13 | 🕓 | 🕓 | **viz 동기화 배너 ExecutionPolicy 픽스 — bg sync spawn Bypass + exe 재빌드 + sync-banner stale 감지** — viz sync 배너 "동기화 시작 중" 무한 대기 버그. 원인: `viz/_build/Start-Visualization.ps1` 백그라운드 sync spawn (R149) 이 `-ExecutionPolicy Bypass` 없이 `-File` 실행 → 유효 정책 Restricted 머신 (전 스코프 Undefined = Windows 클라이언트 기본값) 에서 "running scripts is disabled" 로 spawn 즉시 사망 → `.sync-status.json` 이 `running/starting` 인 채 갱신 주체 소멸 → 배너 무한 spinner. 재현 확정 (clean-env `-File` 실행 → UnauthorizedAccess). 조치: (1) spawn 인자 `-ExecutionPolicy Bypass` 추가 + 헤더 주석, (2) `Generate Visualization.exe` ps2exe 재빌드 (58KB), (3) `viz/components/sync-banner.js` stale 감지 신설 — `started_at` 경과 임계 (starting 2분 / 전체 30분) 초과 시 ⚠ "동기화 응답 없음" 표시 (read-only, done 수신 시 자연 복구) + 영문화 매니페스트 § 6.12 행 2건. 검증: Restricted 노트북 단위 실행 EXIT=0 + E2E done 사이클 3s 완주 + 회귀 (X-AIMV-Root 헤더·페이지 렌더·콘솔 에러 0) 통과. 신규 사용자 머신 대부분 Restricted 기본값 → **배포본에서 R146/R149 자동 동기화 전멸 상태였을 가능성, 배포 우선순위 높음** (exe 바이너리 동기화 = R166 `--force-binary` 후보 연계). 메인 PC 교차 검증 큐: `_AGENT_COMMS/to_claude/20260713_claude_claude_viz_ExecutionPolicy_픽스_검증.md`. 이슈·계획 노트: Project_AIMindVaults `20260711_viz_동기화_배너_무한대기_ExecutionPolicy_이슈분석` + `..._픽스_계획`. 작업자: Claude (회사 개인컴). |
| R168 | 2026-07-01 | 🕓 | 🕓 | **AGENT_ONBOARDING 현행화 — Claude 온보딩 Phase 1·R125 반영 + 공통 문서 정리** — `AGENT_ONBOARDING_CLAUDE.md` 전면 재작성 (2026-04-18 Phase 1 이전 상태로 스테일했음): 규칙 개별 14표 → `_essentials.md` + `_skill-router.md` 상시 주입 + `rules-archive/` 조건부 로드 구조로 교체, 스킬 목록 구버전 17개 (미존재 `/grok-route` 포함) → 현행 core 21개, R125 `.agents/` 공용공간 + agents-sync 미러 구조 반영, 세션 시작 순서에 CODEX 핸드오프·`_AGENT_COMMS/to_claude/` 스캔 추가, MCP 규칙 경로 R125 카테고리화 (`custom/Unity/` 등) + 디바이스별 변동인 고정 서버 목록 제거. `AGENT_ONBOARDING.md` (공통) 4곳: 트리 스테일 카운트 제거 + `.agents/`·rules-archive 반영, §17 Obsidian URI 볼트 예시에서 개인 볼트명 제거 (distribution-content-safety 잔존 위반 정리), workspace 모드 Multi-Hub 문구 (CoreHub/Preset 구분), 하단 규칙 참조 표 현행화. 개인 자산 키워드 스캔 2 파일 CLEAN. 커밋만 생성·push 보류 (사용자 네트워크 트리거): `5eaccb09` 신규 볼트 트리 first-add / `0983ed42` R167 / 본 커밋 R168. 작업자: Claude (회사 개인컴). |
| R167 | 2026-07-01 | – | – | **루트 CLAUDE.md 키워드 라우팅 — Domains_LSElectric 2행 신설** — XG5000 볼트 (XGK, XGB, XGI, LS산전 PLC, XGK 명령어, 명령어집, XGF 등) + XP-Builder 볼트 (XPBuilder, XGT Panel, LS HMI, LS 작화 등) 키워드 행 추가. IndustrialControl 행에서 "LS XG5000" 키워드 제거 (전용 볼트로 이관). 배경: XGK/XGB 명령어집 V2.3 레퍼런스 레이어 구축 (XG5000 볼트, 카테고리 43 + 참조 5 + MOC + 명세, 니모닉 792 검색 마스킹) 과 함께 루트 `_STATUS.md` 볼트 레지스트리에 Domains_LSElectric 섹션 (XG5000·XP-Builder) 등록. 사용자 개인 볼트 라우팅 변경이라 배포 무관 (–/–). 함께 발견된 Core 개선 후보 2건은 `_AGENT_COMMS/to_claude/` 큐 발행 — (a) post-edit-review 볼트 전용 type 합집합 미구현 (`type-not-core` WARN, 1개월 후 BAD 격상 전 구현 필요), (b) 인덱서 aliases 필드 비인덱싱 (니모닉 검색은 H3 headings 구조로 우회 적용). **후속 (같은 세션)**: 키워드 행 1건 추가 — `구미출장` (viz 노트 열기 빈 페이지 버그 해결 과정에서 obsidian.json 21건의 non-git 사본 `C:\AIMindVaults-main` 등록을 -Private 로 일괄 전환하며, -main 에만 존재하던 구미출장 볼트를 git 레포로 이관 + Projects_Industrial 레지스트리 섹션 신설에 따른 라우팅 반영). 작업자: Claude (회사 개인컴). |
| R166 | 2026-06-29 | 🕓 | 🕓 | **viz 홈 활동 패널 `created` 기준 전환 + visible 필터 적용 (fresh-clone mtime 쏠림 fix)** — 새 디바이스(회사 개인컴)에 개인 레포 클론 후 viz 첫 실행 시 "최근 7일 작업량" 이 오늘 하나에 534 몰림 + "최근 추가된 항목" 이 06-29 일색. **원인**: git clone 은 파일 mtime 을 보존하지 않고 클론 시점으로 전부 리셋 → `/api/activity` 가 OS 파일 mtime 집계라 클론 당일로 쏠림. (인덱스/`node_modules` 는 `.gitignore` 라 디바이스별 자체 빌드 필요 — 본 디바이스 최초 빌드: CoreHub cli-node `npm install` + 57 볼트 `index build` + `master-build` → 51 볼트/1577 노트.) **fix**: (1) `viz/server.js handleApiActivity` — 노트 stamp 를 `n.created || n.mtime` (frontmatter created 우선, git 동기화되어 디바이스 무관). weekly 버킷·recent 정렬·recent 항목 `date` 필드 모두 stamp 기준. recent 동률(created date-only) 시 mtime 2차 정렬. (2) `viz/server.js` — `import { filterVisibleNotes } from './lib/system-vaults.js'` 추가, `allNotes` → `visibleNotes = filterVisibleNotes(allNotes)` 로 시스템 Hub + 메타 노트(Domain.md·CONTENTS_* 등) 제외 (홈 KPI 와 동일 노트 집합 정합). (3) `viz/pages/home.js renderActivity` — recent 표시 날짜 `formatDate(r.mtime)` → `formatDate(r.date || r.created || r.mtime)`, 주간 요약 "노트 갱신" → "노트 생성". **결과**: 오늘 막대 534→133, recent 에서 스캐폴드/메타 노트 제거. 남은 06-29 쏠림(실콘텐츠 중 frontmatter `created` 부재 노트가 인덱서 빌드일로 채워진 케이스)은 사용자 결정으로 **인덱서 미수정** — 향후 실제 편집 시 자연 정상화. **배포 🕓/🕓** — 사용자 commit 보류 요청, 다음 배포 사이클에 Korean/English 반영 후보(viz/ 자체가 배포 대상). 새로 추가된 산업 도메인 볼트(`Domains_Engineering`/`Domains_IT`/`Domains_LSElectric`/`Domains_Mitsubishi`)는 아직 git untracked. 작업자: Claude (회사 개인컴). |
| R165 | 2026-06-09 | ✅ | 🕓 | **viz `Generate Visualization.exe` 재빌드 (R163 후속 누락 fix)** — R163 의 Start-Visualization.ps1 `.vault_data/viz-prefs.json` 읽기 로직이 ps2exe 컴파일 본체 `Generate Visualization.exe` 에 미반영 (.exe 는 5/29 R146 시점 빌드 본체) → 사용자가 viz Settings 의 Git 동기화 토글 OFF 후에도 viz 재시작 시 자동 동기화가 계속 작동. **fix**: (1) 진단 — `[Process/User/Machine]` 3 scope env var 모두 unset 확인 (우선순위 문제 아님), `.vault_data/viz-prefs.json` `gitAutoSync: false` 정상 저장 확인, `.sync-status.json` 최신 sync 결과 갱신 확인 → 원인 = .exe 컴파일 본체 stale. (2) `viz/_build/build-exe.ps1` 재실행 → ps2exe 가 Start-Visualization.ps1 새 본문 (R163 의 viz-prefs.json 읽기 + 우선순위 적용 블록) 을 컴파일 → `Generate Visualization.exe` 55.8KB → 57.5KB 갱신. (3) 정본 + Korean SellingVault .exe binary commit + push. **향후 보강 후보 (R166+)**: viz/_build/Start-Visualization.ps1 변경 시 `Generate Visualization.exe` 재빌드 강제 트리거 (룰 또는 PostToolUse hook). 현재는 사용자/에이전트 수동 (build-exe.ps1 직접 실행). 이 누락이 R163 검증 step 에 명시 안 됐던 것 — 본 R165 핸드오프에 ".exe 재빌드" 가 PS1 변경 작업의 필수 후속 step 임을 명시. **Korean=✅** (deploy + commit + push [513c93a](https://github.com/warqun/AIMindVaults/commit/513c93a)). **English=🕓** — `deploy --english-mode` 가 binary 도 보호하고 `.claude/settings.local.json` 의 `Write/Edit/Bash cp (C:/SellingVault/**)` deny 룰 까지 차단해 영문판 .exe 자동 갱신 불가. 정식 해결은 R166 후보 — deploy CLI 에 `--force-binary` 옵션 추가 (--english-mode 일 때 `.exe`/`.ico` 등 binary 는 텍스트 보호와 분리해 강제 덮어쓰기) 또는 사용자 수동 cp. **사용자 검증 ✅** (2026-06-10): viz `.exe` 재시작 후 viz-prefs.json `gitAutoSync: false` 정상 적용 — 백그라운드 sync 미실행 확인. 작업자: Claude (메인 PC). |
| R164 | 2026-06-09 | ✅ | 🕓 | **viz 커스텀 기능 시스템 — registry + surface adapter + 추적 마커 패턴으로 리팩터링 (R163 후속)** — R163 에서 settings.js 안에 하드코딩됐던 Git 동기화 토글/액션을 3곳 (settings.js HTML / server.js whitelist / Start-Visualization.ps1 의 prefs 적용) 분산 상태에서, 정본 registry 한 곳 + 페이지별 adapter 패턴으로 통합. 새 커스텀 기능 추가 = `CUSTOM_FEATURES` 배열에 객체 1개 push 면 settings.js / server.js 둘 다 자동 흡수 (action.endpoint 가 새 endpoint 일 때만 server 에 endpoint 추가). **fix**: (1) `viz/lib/custom-features.js` (신규) — `CUSTOM_FEATURES` 배열 (id/category/addedIn/label/sub/defaultValue/toggleApplyHint/surfaces) + `featureById`/`featureIdSet`/`defaultsFromFeatures`/`featuresForSurface` helpers + JSDoc `CustomFeature`/`CustomFeatureSurface`/`CustomFeatureAction` 스키마. (2) `viz/lib/custom-features-settings.js` (신규) — `surfaces.type === 'settings-row'` adapter. `renderSettingsSection(vizPrefs)` 가 row HTML 자동 생성 (모든 row 에 `data-custom-feature="<id>"` 속성 자동 박음 — DevTools/CSS 식별용). `attachSettingsHandlers({ container, vizPrefs, showToast })` 가 토글/액션 click 위임 처리, `c._customFeaturesAttached` flag 로 idempotent (renderAll 매번 호출돼도 한 번만 부착). 알려진 `postSuccessHook` 키워드 매핑 (`restartSyncBanner` → `components/sync-banner.js` dynamic import + `startSyncBanner()`). `Object.assign(state.vizPrefs, updated)` 로 외부 ref mutation (reassign 회피). (3) `viz/pages/settings.js` — 패널 3 "표시 토글" 아래 하드코딩된 커스텀 기능 row 12 줄 → `${renderCustomFeaturesSection(state.vizPrefs)}` 1줄. attachHandlers 의 `data-feature-toggle` 분기 + `#set-sync-now` 버튼 핸들러 (총 60 줄) → `attachCustomFeaturesHandlers({container, vizPrefs, showToast})` 1 호출로 위임. (4) `viz/server.js` — `import { defaultsFromFeatures, featureIdSet } from './lib/custom-features.js'`. `DEFAULT_VIZ_PREFS` = `{ schemaVersion, ...defaultsFromFeatures(), updatedAt }` 자동 구성. POST whitelist = `for (const [k,v] of Object.entries(body)) if (FEATURE_IDS.has(k) && typeof v === 'boolean')` — boolean only 허용. 하드코딩 `gitAutoSync` 키 제거. (5) `viz/scripts/list-custom-features.js` (신규, 외부 의존성 0) — registry 출력 + viz/ 디렉토리 재귀 walk (node_modules/_build/scripts 제외, `.js/.mjs/.html/.css/.md/.ps1` 만) → `@custom-feature[:\s]+<id>` 마커 grep → cross-check (orphan = 마커는 있는데 registry 에 없음 → exit 1 + `⚠`, unmarked = registry 엔 있으나 코드 마커 없음 → 참고용 `ℹ`). TTY 면 ANSI 컬러, `--json` 머신 읽기 모드. **추적·구분 layer**: HTML `data-custom-feature` (adapter 자동) + 코드 주석 `@custom-feature: <id>` (수동, 향후 다른 위치 마킹 시) + CLI cross-check + `data-feature-added-in` 속성 (R 번호 추적). **뺀 것**: UI "모두 보기" 토글 (현재 surface = settings 하나라 가치 낮음, surface 늘어날 때 추가), debug CSS outline (필요 시 별도 사이클). **Korean=✅ / English=🕓** — R163 과 묶어 `/distribute` 스킬 단일 commit ("R160+R162+R163+R164 누적") 으로 Korean SellingVault 반영 + push 완료. 영문 번역 필요 매핑 신규 (다음 영문 사이클): `Git 동기화` / `viz 시작 시 git pull --ff-only + sync-all 자동 (R146/R149). 토글 off 상태에서도 ▶ 로 수동 트리거 가능 — 진행은 우상단 banner.` / `▶ 지금` / `지금 한 번 수동 동기화` / `다음 viz 실행부터 적용` / `viz 자체 동작을 디바이스별로 켜고 끕니다...` / `등록된 커스텀 기능이 없습니다.` / `추가/제거는 viz/lib/custom-features.js 정본 registry 에서 관리` / 등. **검증**: viz `.exe` 재시작 → Settings → 커스텀 탭 시각상 R163 과 동일 (registry 출처만 변경). DevTools 에서 row 에 `data-custom-feature="gitAutoSync"` + `data-feature-added-in="R163"` 속성 확인. `node viz/scripts/list-custom-features.js` 출력 = 1 feature (gitAutoSync, composite, R163), 0 orphans, 1 unmarked (정본만 존재 — 정상). 작업자: Claude (메인 PC). |
| R163 | 2026-06-09 | ✅ | 🕓 | **viz 자동 동기화 강제 → viz Settings UI 토글 + 수동 동기화 버튼** — 기존 `AIMV_VIZ_AUTO_PULL/SYNC` env var 만으로 on/off (사실상 기본 강제 on) 였던 git pull + sync-all 자동 동기화를, viz `Settings → 커스텀 → 커스텀 기능` 섹션의 UI 토글로 디바이스별 끌 수 있게 함. 자동 off 상태에서도 호출 가능한 `지금 한 번 동기화 ▶ 실행` 버튼 동반. **fix**: (1) `viz/server.js` — `POST /api/viz-prefs` (whitelist 머지 + `.vault_data/viz-prefs.json` 저장) + `POST /api/viz-sync-now` (백그라운드 `git pull --ff-only origin main` → 변동 있을 때만 `sync-all --skip-npm`, `syncInFlight` flag 로 중복 실행 차단, `.sync-status.json` 갱신) + `GET /api/viz-prefs` (현재 값 조회, 파일 부재 시 default). 기존 GET-only 정책에 두 POST 경로 예외 (127.0.0.1 바인딩 전제). `node:fs/promises` 에 `mkdir/writeFile` + `node:child_process spawn` 추가. (2) `viz/pages/settings.js` — 패널 3 (커스텀) "표시 토글" 아래 신규 `커스텀 기능` 섹션. **같은 동기화 기능의 자동/수동을 한 row 안에 묶음** — 라벨 `Git 동기화` + ctrl 영역에 `▶ 지금` 버튼 (`#set-sync-now`) + 자동 토글 (`data-feature-toggle="gitAutoSync"`) 가 flex 로 나란히. sub 설명에 "자동 실행 + off 상태에서도 ▶ 로 수동 트리거" 정합 명시. `initPage` 에서 `GET /api/viz-prefs` await 한 뒤 `state.vizPrefs` 셋. 토글 변경 시 POST 백그라운드, 실패 시 UI 롤백 + toast. 수동 버튼 클릭 시 POST `/api/viz-sync-now` → `sync-banner.js` 동적 import 로 polling 재시작 (이전 done/failed 후 `clearInterval` 됐을 수 있어). (3) `viz/_build/Start-Visualization.ps1` — 환경 변수 기본값 설정 직전에 `.vault_data/viz-prefs.json` 읽어 `AIMV_VIZ_AUTO_PULL/SYNC` 적용. 우선순위: **시스템 env var (사용자 명시) > viz-prefs.json (UI 토글) > default(true)**. `$myRoot = Split-Path -Parent $exeDir` 를 라인 81 부근으로 끌어올림 (기존 라인 117 는 그대로 잔존 — 단순 재할당이라 무해). `Get-Content -Raw -Encoding utf8 | ConvertFrom-Json` + `PSObject.Properties.Match` 키 확인 (round-trip 없는 단방향 Read 만이므로 obsidian-config-safety 룰 비대상). (4) `.claude/rules/core/viz-device-sync.md` — "환경 변수" 섹션을 "viz Settings UI 토글 (R163, 권장)" + "환경 변수 (시스템 레벨)" + "우선순위" 세 블록으로 재구성. viz-prefs.json schema 명시 + `.gitignore` 명시 (`.vault_data/` 이미 ignore 됨). 참조 섹션 + 트리거 키워드 (`viz-prefs`, `자동 동기화 토글`, `지금 한 번 동기화`) 추가. (5) `_ROOT_VERSION.md` R163 등록. **Korean=✅ / English=🕓** — `/distribute` 스킬로 Korean SellingVault deploy --apply + commit + push 완료. English 는 viz 영문판 매핑 보강 필요 (R156/R157 의 `translate-to-en.js`) — 신규 라벨 "커스텀 기능" / "Git 동기화" / "▶ 지금" / "지금 한 번 수동 동기화" / "다음 viz 실행부터 적용" / viz-prefs 관련 메시지 등 매니페스트 § 6 에 추가 후 변환 → 다음 영문 사이클 보류. English SellingVault 는 `deploy --english-mode` 로 한국어 본문 untracked 잔존 (commit X). **검증**: 사용자가 viz 현재 인스턴스 reload → Settings → 커스텀 탭에서 새 섹션 + 토글 + 버튼 확인. 토글 변경 시 toast `Git 자동 동기화: off — 다음 viz 실행부터 적용`. 다음 viz `.exe` 실행 시 백그라운드 sync 미수행 확인 (sync-banner idle). 작업자: Claude (메인 PC). |
| R162 | 2026-06-06 | – | – | **Career 볼트 신설 (Personal 카테고리)** — 이력서·자기소개서·회사별 지원 기록·면접 준비/후기·자격증 학습 등 커리어 활동 전반 관리 볼트. 첫 케이스 = (주)스미스 오토메이션 PLC HMI 직무 지원 (게임 회사 빅파이어게임즈 경력·게임 관련 수상 내역은 이력서에서 제외, 자소서 본문에 "시스템 기획자 경험" 톤으로 녹여 PLC/HMI 전환 스토리 구성). **fix**: (1) `node cli.js clone` 으로 BasicContentsVault → `Vaults/Personal/Career/` 복제 (378 파일), hub=AIHubVault (Default Preset, hubId=default). (2) Career 볼트 `CLAUDE.md` 재작성 — 역할 (이력서/자소서 + 회사별 지원 기록 + 면접 + 자격증 학습) + 수집 범위/제외 (Diary·IndustrialControl·Unity·AI 와 경계) + 디렉토리 구조 (`Contents/Domain/{resume,coverletter,interview}` + `Contents/Project/applications/{YYYYMMDD}_{회사명}_{직무}/`) + 태그 규칙 (Career 필수 + Application/Resume/CoverLetter/Interview/CompanyResearch 보조). (3) `_STATUS.md` 초기화 — Now/Next/Decisions 채움 (첫 케이스 스미스 오토메이션). (4) 루트 `_STATUS.md` `### Personal` 표에 Career 행 추가 (Diary 와 같은 라인). (5) 루트 `CLAUDE.md` 키워드 매핑 표에 Career 행 추가 (이력서·자소서·지원서·구직·이직·채용·면접·커리어·자격증 학습 노트). (6) 사용자 개인 자산 (구직 활동 + 개인정보 포함) → SellingVault Korean/English 배포 대상 X (distribution-content-safety 룰). Korean=– / English=–. 다음 = 인덱스 빌드 + obsidian.json 등록 + 첫 콘텐츠 (스미스 오토메이션 지원 폴더 + 이력서·자소서 .docx 출력). 작업자: Claude (메인 PC). |
| R161 | 2026-06-06 | ✅ | 🕓 | **`/create-vault` + `vault-individualization` 을 custom/CreateVault/ → core/ 격상 (R159 incident 근본 정합)** — R159 자기검증 후 발견: 두 파일이 사실상 모든 사용자 대상 일반 인프라인데 R125 카테고리화 시 `custom/CreateVault/` 로 분류돼 SellingVault deploy 의 whitelist 에서 제외 → R159 본문이 배포 미반영 상태. 사용자 결정: core/ 격상 (R125 카테고리 결정 정합). **fix**: (1) `.agents/rules/custom/CreateVault/vault-individualization.md` → `.agents/rules/core/vault-individualization.md` 이동, 빈 `.agents/rules/custom/CreateVault/` 폴더 rmdir. (2) `.agents/commands/custom/CreateVault/create-vault.md` → `.agents/commands/core/create-vault.md` 이동, `create-preset-hub.md`/`register-vaults.md` 는 도메인 잔존 (별 트랙 검토). (3) `.agents/_MANIFEST.md` core 행 카운트 rules 12→13 + commands 20→21, CreateVault 행 카운트 rules 2→0 + commands 3→2, core 룰 목록에 vault-individualization 추가 + core 스킬 목록에 create-vault 추가, CreateVault 섹션 정본 룰 "없음" 명시 + create-vault.md core 격상 명시. (4) `.claude/rules/MANIFEST.md` core/ 표에 vault-individualization 행 추가 + Phase 3 archive 의 stale 행 제거 (R125→R161 이력 코멘트). (5) `.agents/rules/core/_skill-router.md` 트리거 매핑의 vault-individualization 경로 갱신 (core 상시 주입이라 명시 Read 불필요 표기). (6) agents-sync 결과 Created=4 Updated=2 + stale mirror 4 수동 정리 (.claude/.codex 양쪽 rules/commands 의 옛 custom/CreateVault/ 파일). agents-sync --verify Same=118 drift=0. (7) deploy dry-run 검증: `[COPY] vault-individualization.md` + `[COPY] create-vault.md` + `[COPY] _skill-router.md` + `[COPY] .claude/rules/MANIFEST.md` + `[COPY] .claude/commands/MANIFEST.md` + `[COPY] _ROOT_VERSION.md` 모두 포함 확인. **R161 본 사이클**: Korean deploy --apply + Korean commit + push + 정본 commit + push. **English 는 🕓 보류** — `.claude/settings.local.json` deny 4 (`Write/Edit(C:/SellingVault/**)`) 가 의도적 설계 록 (distribute 스킬 본문 "Claude 가 배포 경로 직접 접근 X" 정합) 이라 영문 번역 본문 직접 Write 불가. 다음 사이클에서 영문 번역 인프라 (Antigravity sync-distribution 또는 translate-to-en.js 확장 등) 통해 처리. English SellingVault working tree 에는 deploy --english-mode 가 자동 추가한 vault-individualization.md (한국어 본문) 가 untracked 잔존 — 영문 번역 사이클에서 영문으로 덮어쓰기 후 commit. R159 의 Korean/English 컬럼도 동일 처리 (Korean=✅, English=🕓). 다음 새 볼트 생성 사이클부터 R159 incident 재발 불가 구조 (Korean 측만 일단 보장). 작업자: Claude (메인 PC). |
| R160 | 2026-06-06 | – | – | **YouTube AI 재생목록 2차 노트화 + UnrealEngine / ContentCreation / Learning 일반 주제형 볼트 신설** — `PLPQA4lizZ-sC4CvYWWbhnqaT-hHf88D-b` 37개 중 기존 노트 4개를 중복 스킵하고 33개를 10개 볼트에 라우팅해 노트화: AI_Coding 9, ContentCreation 5, AI_Gen4Game 4, GameArt 5, AI 3, Blender 1, ArtInsight 1, UnrealEngine 1, Funding 3, Learning 1. 사용자 결정에 따라 `Domains_*` 경로지만 ZK Domain Preset 대신 일반 주제형 구조를 선택. `AIHubVault` 허브 + 명시적 `BasicContentsVault` 템플릿으로 `Vaults/Domains_Game/UnrealEngine/`, 신규 카테고리 `Vaults/Domains_Content/ContentCreation/`, `Vaults/Domains_Life/Learning/` 3개 볼트를 생성. 각 볼트 `AGENTS.md`, `CLAUDE.md`, `CODEX.md`, `_STATUS.md`, `_VAULT-INDEX.md`, `_Standards/CONTENTS_SPEC.md`, `Contents/Domain/Domain.md`, `Contents/Domain/Review/Review.md` 개별화. 루트 `_STATUS.md` 레지스트리와 `AGENTS.md`/`CLAUDE.md` 라우팅 키워드 갱신. 대상 10개 볼트 review/index 모두 `BAD=0`, `TAG_VIOLATIONS=0`, `POST_EDIT_INDEX_UPDATED=1`. Obsidian 등록은 `register-vaults`가 앱 실행 중 상태를 감지해 차단, 앱 종료 후 apply 재시도 필요. 사용자 개인 자산(도메인 볼트·노트 추가)이므로 SellingVault Korean/English 배포 대상 아님. 작업자: Codex. |
| R159 | 2026-06-06 | ✅ | 🕓 | **`/create-vault` 스킬 + `vault-individualization.md` 룰 안전 강화 — Obsidian obsidian.json 자동 등록 단계 정합** (R161 core/ 격상 후 Korean SellingVault 반영 — Korean=✅. English=🕓 다음 영문 번역 사이클) — 2026-06-05 R158 IndustrialControl 신설 후 viz 노트 링크 클릭 시 `Vault not found` incident 발생 (Obsidian obsidian.json 미등록 상태). 자기검증 결과: `/create-vault` 스킬 § 4 가 GUI 등록 안내만 명시 + `vault-individualization.md` § "볼트 생성 후 필수 작업" 4단계에 obsidian.json 등록 누락 + clone-vault.js 자체 등록 통합 안 됨 → CLI `register-vaults` 도구가 별도 존재하는데도 진입점 어디서도 자동 호출 안 됨. 사용자 결정: 옵션 **A+C 병행** (가장 안전, 비용 최소). **fix**: (A) `.agents/commands/custom/CreateVault/create-vault.md` § 번호 정합화 (중복된 § 3 → § 4 "생성 후 필수 작업" / § 4 → § 5 / § 5 → § 6) + § 5 "Obsidian obsidian.json 등록 (강제 — 자동화 우선)" 신설: CLI 자동 등록 우선 (5-1) + GUI fallback (5-2) + URI 금지 명시 (5-3). § 4 에 인덱스 빌드 6번 추가 (룰 참조). § 6 완료 보고에 등록 상태 명시 추가. (C) `.agents/rules/custom/CreateVault/vault-individualization.md` § "볼트 생성 후 필수 작업" 5번 신설 — `register-vaults --apply` 명령 + 전제 (Obsidian 종료) + dry-run → 사용자 종료 요청 → apply → 재시작 안내 흐름 + GUI fallback + 스킬 § 5 참조. `--force` / `--skip-process-check` 사용 금지 명시 (메모리 캐시 덮어쓰기 위험). **agents-sync --area CreateVault** = 4 mirror updated (`.claude/+.codex/` 각 2). 본 멀티볼트 환경 즉시 반영 (다음 새 볼트 생성 사이클부터 자동 등록 안내됨). SellingVault Korean/English 배포는 다음 사이클 결정 (룰·스킬 변경이라 일반 배포 대상 후보). 작업자: Claude (메인 PC). |
| R158 | 2026-06-05 | – | – | **IndustrialControl 도메인 볼트 신설** — PLC 정규직 트랙 진입 학습 영역. 사용자 결정: `Domains_Industrial` 카테고리 신규 + 볼트명 `IndustrialControl` (PLC 코어 + HMI/SCADA + Motion + Fieldbus + Safety 까지 한 볼트 흡수, 영역이 커지면 분리). `node cli.js clone` 으로 BasicContentsVault → `Vaults/Domains_Industrial/IndustrialControl/` 복제 (378 파일), hub=AIHubVault (Default Preset, hubId=default). CLAUDE.md/_STATUS.md IndustrialControl 용도로 재작성 (역할 + 경계 + 디렉토리 구조 + 태그 규칙 + 출발점). 루트 `_STATUS.md` 볼트 레지스트리 `### Domains_Industrial` 섹션 신규 + IndustrialControl 행 추가. 루트 `CLAUDE.md` 키워드 매핑 표에 IndustrialControl 라우팅 추가 (PLC, 시퀀스 제어, 래더, Ladder, FBD, ST, IEC 61131, HMI, SCADA, Motion Control, 서보, 인버터, Fieldbus, Profinet, EtherCAT, Modbus, CC-Link, OPC UA, Safety PLC, 자동화 설비 제어, 산업 자동화, FA, 공장 PLC, Siemens TIA, Mitsubishi GX, Studio 5000, LS XG5000). 사용자 개인 자산 (도메인 볼트 추가) → SellingVault Korean/English 배포 대상 X (distribution-content-safety 룰). 다음 = `node cli.js index build -r` 초기 인덱스 빌드 + Obsidian 볼트 매니저 등록 (사용자 수행). 작업자: Claude (메인 PC). |
| R157 | 2026-06-01 | – | ✅ | **English SellingVault R096~R157 누적 동기화 + viz/ 영문 1차 배포 — Phase D-2 완료** — English SellingVault 이 R095 (4-29) 이후 누적 미동기화 상태 (296 working tree 변경 + viz/ untracked). 사용자 결정: "오늘 영문 배포까지 완료" → R155/R156/R157 일괄 push 진행. **fix**: (1) extra-mappings.json 신규 (~300 매핑, D/E/G 카테고리 보강 — 사용자 노출 X 코드 주석/console/PS1) — 정본 `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/scripts/viz_i18n/extra-mappings.json`. (2) translate-to-en.js 보강 — extra-mappings.json 로드 + SKIP_FILES (AGENTS.md) 메커니즘. (3) 재실행: 2099 매핑 적용 → 1차 잔재 1666 → 1433 라인 → AGENTS.md 영문 본문 직접 작성 → 사용자 노출 핵심 잔재 0건. (4) deploy --english-mode 실행: 16 새 파일 추가 (viz-device-sync.md / delegate-task.md / obsidian-windows.md / spawn-claude.md / juggl × 12) + 영문 진입점 보호 (루트 AGENTS.md / CLAUDE.md / AGENT_ONBOARDING / README / SETUP_GUIDE / Sync All Vaults 등 SKIP). (5) English SellingVault commit + push: 387 파일 변경 (67736 insertions / 3177 deletions), commit `6699675` ([AIMindVaults-EN](https://github.com/warqun/AIMindVaults-EN)). **남은 잔재**: viz 코드 주석/JSDoc/console/PS1 메시지 한국어 ~5500 음절 (사용자 노출 X, 개발자 시야). R158 보강 사이클 후보 — 매니페스트 § 6 D/E/G 카테고리 정식 흡수 후 재변환. **R155/R156 English 컬럼 일괄 ✅** (본 R157 일괄 push 에 흡수). 작업자: Claude (메인 PC). |
| R156 | 2026-06-01 | ✅ | ✅ | **viz 영문판 자동 변환 인프라 (translate-to-en.js) — Phase D-1 완료** — viz/ 정본 (한국어) → English SellingVault viz/ (영문) 일괄 변환 자동화. 표 4 AI 추천 채택 (viz-en 위치 = English SellingVault viz/, 자동 변환 스크립트, 영문 매니페스트 별도 X, Phase D 일괄 push). **fix**: (1) 변환 스크립트 신규 — `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/scripts/viz_i18n/translate-to-en.js` (Node.js, ~150 LOC). 매니페스트 § 6 카탈로그를 파싱해 KO/EN 매핑 추출 + 재귀 viz/ → English viz/ 복사 + KO 길이 내림차순 정렬 (substring 충돌 회피) + 잔재 char 카운트 + `_build/tools/` skip (외부 라이브러리 보호) + binary 그대로 복사 + dry-run 옵션. 영구 보관 (매니페스트 § 6 갱신 후 재실행). (2) CoreHub `Script_Registry.md` 등록 + `bump-version --broadcast` (Core 영역 D1 강제) — 6/6 Preset Hub 의 `_WORKSPACE_VERSION.md` 202605310001 갱신. (3) 매니페스트 § 0 Phase D-1/D-2 분리 — D-1 (인프라) ✅ 완료, D-2 (잔재 0건 + English push) 🟡 R157+ 보류. **1차 실행 결과**: § 6 매핑 364 행 추출 → 51 텍스트 파일 변환 → 519 매핑 적용 → 잔재 1666 라인 (정본 1932 라인 대비 14% 감소). 잔재 큰 파일: AGENTS.md (129) / home.js (122) / additions.js (103) / calendar.js (89) / settings.js (82) / Start-Visualization.ps1 (81) / server.js (80) 등. **원인**: 매니페스트 § 6 = UI 라벨 중심 (사용자 노출 부분만). D 코드 주석 / E console.log / G PowerShell 콘솔 / AGENTS.md / 마크다운 헤더 매핑 부재. **사용자 결정 (옵션 A)**: 영문판 push 보류 — 잔재 다수 상태로 구매자 노출 안 함. R157 사이클에 § 6 카탈로그 D/E/G 카테고리 보강 (1000+ 매핑 추가) 후 재실행 → 잔재 0건 → English push. 정본만 commit + push (스크립트 + Script_Registry + 매니페스트 § 0 + _ROOT_VERSION). 작업자: Claude (메인 PC). |
| R155 | 2026-06-01 | ✅ | ✅ | **viz/AGENTS.md 신규 — 에이전트 폴더 진입점 + 구매자 진입 가이드 통합** — 진입점 점검 결과 viz/ 폴더 자체에 진입 가이드 파일 부재 (index.html 만). 정본 AI 가 viz/ 폴더 직접 접근 시 매니페스트가 `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/plan/distribution/` 깊은 곳에 파묻혀 발견 어려움. 구매자 (Korean SellingVault 사용자) 는 spec/plan 노트 전체가 배포 X (사용자 위성 콘텐츠라 distribution-content-safety 룰 적용) — viz 코드만 받고 이해 가이드 없음. **fix**: `viz/AGENTS.md` 신규 (173 LOC, 132 한국어 라인, 10 섹션) — viz/ 폴더 트리 + 책임 + 페이지 11 매핑 (URL hash → 파일) + 데이터 흐름 (master_index → server API → SSE → 페이지) + SSE 자동 갱신 + 비자명 패턴 7 (5 모듈 dispose 순서, directedMode 분기, multi sub-link KPI, R148 디바이스 무관 derive, port 순회, idle 자동 종료, 시스템 Hub 필터) + 빌드 + 작업 진입 (정본 전용 자료 link 명시) + 동기 규칙 5 + 컨텍스트 깊이 추천 4. Claude/Codex 공용 — `viz/CLAUDE.md` 별도 미작성 (중복 회피). 정본 자료 link (시스템스펙 04 / 영문화 매니페스트 / viz-device-sync 룰) 는 정본 path — 구매자에게 깨지지만 § 8 에 "정본 전용" 명시. **영문화 매니페스트 갱신**: § 3.1 (진입점/서버/라우터) 에 AGENTS.md 행 추가 + § 7 (코드 주석 매핑) 에 AGENTS.md 행 추가 + frontmatter updated 2026-05-30 → 2026-06-01. **자동 배포**: viz/ 자체가 배포 대상이라 `viz/AGENTS.md` 도 Korean SellingVault 에 자동 미러. 효과: 정본 AI 5분 viz 구조 파악 + 구매자 자체 완결 진입 가이드 수령. 작업자: Claude (메인 PC). |
| R154 | 2026-06-01 | ✅ | 🕓 | **Custom Juggl 모든 Preset Hub 기본 제공 통일** — 사용자 점검 중 발견: Juggl 가 `AIHubVault_Diary`/`_Lab`/`_Project` Preset Hub 에서 의도적으로 빠짐 (4-20 Multi-Hub Phase 1 설계 — 일기 최소 UI · Lab 실험 R&D · Project PARA 각각 Custom 4/6/5 명시). BUT `BasicDiaryVault`/`BasicLabVault`/`BasicProjectVault` 클론 템플릿에는 juggl 가 들어있어 **클론한 위성에는 juggl 가 있는데 sync 정본 Preset Hub 에 없는 비대칭** 발생 → Hub→위성 갱신 경로 끊김. 사용자 결정: "핵심 플러그인은 아니라서 배제된 듯 하지만 Custom 으로라도 기본 제공" — 4-20 설계 변경, 모든 Preset Hub 에 juggl 통일. **fix 3 Preset Hub**: (1) `AIHubVault_Diary` Custom 4→5 (Calendar, Periodic Notes, Heatmap Calendar, Journal Review, **Juggl**), (2) `AIHubVault_Lab` Custom 6→7, (3) `AIHubVault_Project` Custom 5→6 — 각각 `plugins/juggl/` 4 파일 복사 (graph.css, main.js, manifest.json, styles.css, ≈3.4MB) + `community-plugins.json` 에 `"juggl"` 등록 + `hub-marker.json` description Custom 목록 갱신 + `CLAUDE.md` 역할 섹션 갱신 + `_WORKSPACE_VERSION.md` 202606010001 bump. **위성 누락 복구 2곳**: (4) `MachineAssembly` (json=1 등록은 있는데 `plugins/juggl/` 디렉토리 부재 — 4-25 신설 직후 sync 누락) — 디렉토리 복원, (5) `Project_MyVaults` (json=0 등록도 디렉토리도 부재 — Project Preset 바인딩이라 의도적 누락 가능성 있었으나 사용자 확인 = 활성) — json 등록 + 디렉토리 추가. **Korean SellingVault** 동기화: 3 Preset Hub 의 5 파일 정본 → Korean 미러 (`C:\SellingVault\Korean\AIMindVaults\Vaults\BasicVaults\AIHubVault_{Diary,Lab,Project}/`). 위성은 배포 대상 X (사용자 개인 자산 — `distribution-content-safety.md` 룰). 영향: 다음 위성 sync 사이클에 자동 반영. 작업자: Claude (메인 PC). |
| R153 | 2026-05-30 | ✅ | 🕓 | **viz home KPI Connected 카드 "+N today" 2지표 살림 (R152 후속)** — R152 작업 시 의도적으로 보류했던 증감 표시 부분. 사용자 결정: "새로 연결된 것 + 기존 소유태그에서 새로 연결된 노트 수" 2지표 동시 표시. Notes 카드 multi sub-link 패턴 응용 (둘 다 `#connections` 직진). **derive (디바이스 무관, R148 원칙 유지)**: (1) 지표 1 "연결" = 각 노트의 `created` 기반으로 (vault, tag) 첫 등장 일자 계산 → 각 (owner, user, tag) 쌍의 형성일 = `max(owner first created, user first created)` → 가장 최근 형성일 + 그 날 신규 쌍 수. (2) 지표 2 "노트" = owned tag (master.connections 의 tag) 를 가진 노트 중 (vault, tag) 첫 등장이 아닌 노트의 created 분포 → 가장 최근 일자 + 그 날 추가된 노트 수. 지표 1 첫 등장 제외해 중복 회피. **fix**: `viz/pages/home.js` — (a) `normalizeConnections(connections)` 헬퍼 (객체/배열 양쪽 정규화), (b) `buildFirstSeen(notes, tagFilter)` 헬퍼 ((vault, tag) → 첫 created 일자), (c) `computeConnectionsNewPairs(notes, connections)` export — 지표 1, (d) `computeConnectionsNewNotes(notes, connections)` export — 지표 2 (첫 등장 제외), (e) `connectionsSubLink(change, label)` 렌더링 (notesSubLink 와 유사, destination 둘 다 `#connections`), (f) items 의 Connected 항목 `change: null` → `conns: { pairs, notes }`, (g) renderKpi map 에 `if (it.conns)` multi 분기 추가. 약 110줄 추가. **검증** (메인 PC preview server localhost:8765): home KPI Connected 카드 `<div class="kpi multi">` 안에 연결 sub-link `<a href="#connections">+1 연결 5/29</a>` + 노트 sub-link `<a href="#connections">+1 노트 5/29</a>` 정상 렌더. derive 정확성 raw check — 5-29 신규 쌍 1건 (`Project_AIMindVaults ↔ Project_MyVaults via Distribution`, owner first 2026-03-17 + user first 2026-05-29 → max 5-29), 기존 새 노트 1건 (`Project_MyVaults/Contents/01_Plan/20260529_Korean_SellingVault_노트북_점검_절차.md`, CrossDevice tag 매칭). 다른 카드 회귀 없음 (Vaults +1 5-14 / Notes 생성 +3 5-29 갱신 +3 5-29 / Tags +15 5-29). **의미 분리 명확**: 연결 = vault-pair 단위 새 owner-user-tag 쌍 / 노트 = note 단위 기존 connection 의 후속 노트. 작업자: Claude (메인 PC). |
| R152 | 2026-05-29 | ✅ | 🕓 | **viz home KPI Connected 카드 진입 동선 살림** — 노트북 R151 검증 중 발견된 이슈 (`Vaults/Projects_Personal/Project_MyVaults/Contents/06_Issue/20260529_viz_CONNECTED_카드_증감표시_및_드릴다운_누락.md`) 처리. **원인**: R148 에서 Connected 가 vault-pair 단위라 노트 mtime derive 어렵다는 이유로 `change: null` 처리 후, `viz/pages/home.js` 의 linkable 분기가 `it.change && it.change.date` 만 검사 → `change: null` 이면 plain `<div>` 렌더 → **href 자체 없음, 클릭 불가**. `view: 'connections'` 는 보유했지만 게이팅 통과 못 함. **fix**: 기존 분기 (`change.date` 있으면 `#additions?date=...&view=...&basis=created`) 유지 + **새 분기 추가** — `change` 없어도 `view` 있으면 `#<view>` 직진 (`#connections` 등). 4줄 추가. **destination 선택 이유**: Connected 는 vault-pair 구조 정보가 핵심이라 일자별 신규 노트 (`#additions`) 보다 `connections.js` Sankey 페이지 직진이 자연스러움 (이미 풍부 — Sankey + topN 슬라이더 + 검색 + 우측 패널 owner/tag/user 상세). **검증** (메인 PC preview server localhost:8765): home KPI 카드 4개 DOM 확인 — Vaults `<a href="#additions?date=2026-05-14&view=vaults&basis=created">` (회귀 없음), Notes `<div class="kpi multi">` (sub-link 유지), **Connected `<a href="#connections">`** ✅, Tags `<a href="#additions?date=2026-05-29&view=tags&basis=created">` (회귀 없음). 카드 click → `location.hash=#connections` + Sankey canvas 마운트 + topN 슬라이더 표시 + 사이드 패널 안내 "좌측 owner, 중간 tag, 우측 user 를 클릭하면 상세가 펼쳐집니다." 정상. **증감 표시 "+N today"** = 별 트랙 (vault-pair 단위 derive 가 timeseries 디바이스 시점 의존 — R148 디바이스 무관 원칙과 충돌 회피, 사용자 결정 "변경 최소" 따라 클릭 동선만 살림). 이슈 발견·기록: 노트북 (점검 중) → fix·검증: 메인 PC. 작업자: Claude (메인 PC). |
| R151 | 2026-05-29 | ✅ | 🕓 | **viz-device-sync.md core/ 격상 + 일반화 (배포 대상)** — 사용자 결정: "팔 때 적절한 가치 추가" 이유로 본 룰을 배포본 (SellingVault) 에 포함. 격상 작업: (1) **일반화** — distribution-content-safety 룰 따라 사용자 개인 자산 인용 5건 제거: `Project_AIMindVaults` 경로 (3건) → `<프로젝트 볼트>`, `MachineAssembly` (2건) → `특정 vault`, 사용자 노트 파일명 → `{YYYYMMDD}_vault_counts_교차검증.md` 패턴. 메인 PC/노트북 인용 → "디바이스" 추상. (2) **정본 이동**: `.agents/rules/custom/Distribution/` → `.agents/rules/core/viz-device-sync.md`. (3) **agents-sync** 2회 호출 — mirror 갱신 + orphan custom mirror 수동 삭제 (`.claude/.codex/rules/custom/Distribution/viz-device-sync.md` 2건). (4) MANIFEST 갱신 — custom 섹션 행 제거 + core 섹션 행 추가. (5) `_skill-router` 트리거 경로 갱신 — `.agents/rules/custom/Distribution/` → `.claude/rules/core/`. **효과**: 모든 사용자가 viz 시각화 도구 사용 시 본 룰 자동 주입 — 디바이스 동기화 + viz `.exe` 자동 동기화 + KPI 통일 + viz_snapshots 워크플로우 + 디바이스 간 큐 패턴 일관 적용. Korean/English 배포 대상 (다음 사이클에 SellingVault 반영). 작업자: Claude (메인 PC). |
| R150.1 | 2026-05-29 | – | – | **R150 보강 — viz-device-sync.md 에이전트 공용 위치로 이관** — R150 초기에 `.claude/rules/custom/` (Claude 전용) 에 작성했으나 Codex 미러 부재. 사용자 요청 "에이전트 공용 커스텀" 으로 정정. **fix**: 정본을 `.agents/rules/custom/Distribution/viz-device-sync.md` 로 이동 (기존 distribution-deploy.md + sync-version-priority.md 와 같은 카테고리). `agents-sync` 자동 호출 → `.claude/rules/custom/Distribution/viz-device-sync.md` + `.codex/rules/custom/Distribution/viz-device-sync.md` 양쪽 미러. `.claude/rules/core/_skill-router.md` 트리거 행도 첫 commit 시 `.claude/` 에만 추가 → mirror 갱신 시 정본 `.agents/rules/core/_skill-router.md` 의 옛 내용으로 덮어쓰여 사라짐 발견 → 정본 갱신 후 재 agents-sync 로 .claude + .codex 양쪽 mirror 정합 (2 updated). 트리거 경로도 `.agents/rules/custom/Distribution/viz-device-sync.md` 참조로 변경. 사이트별 노트 — 본 사이클의 정본 first 원칙 위반 incident: `.agents/` 가 정본이고 `.claude/`/`.codex/` 가 미러임을 망각. 향후 룰 추가 시 무조건 `.agents/` 정본 작성 → agents-sync. 작업자: Claude (메인 PC). |
| R150 | 2026-05-29 | – | – | **viz 디바이스 간 동기화 custom 룰 정리** — 본 사이클 (R141~R149.2) 의 깃 연동 + viz 자동 동기화 + viz_snapshots 워크플로우 + _AGENT_COMMS 큐 패턴을 한 곳에 정리. **fix**: `.claude/rules/custom/viz-device-sync.md` 신규 — 핵심 원칙 (데이터 = git / 인덱스 = 각자 빌드 / viz 자동 동기화 / viz UI 일관성) + R146 + R149 + R149.2 흐름 + 환경 변수 + fail-safe + viz_snapshots 워크플로우 + 디바이스 간 큐 패턴 + master_index .gitignore 사유 + R148 노트 기반 derive + 자주 쓰는 명령. **MANIFEST.md** 갱신 — custom 섹션에 등록. **_skill-router.md** — 트리거 행 추가 ("viz / 시각화 / 동기화 / sync-banner / sync-status / viz `.exe` / Generate Visualization / viz_snapshots / 디바이스 정합 / KPI 불일치" 등). R150 은 custom 영역이라 배포 무관 (Korean/English – 처리). 작업자: Claude (메인 PC). |
| R149 | 2026-05-29 | ✅ | 🕓 | **viz 시작 시 자동 동기화 — 백그라운드 + UI 상태 배너** — R146 의 wait 동기 sync (35-57s 사용자 대기) 가 viz 띄우는 체감 시간 늦춤 발견. 사용자 통찰: "창은 일단 열고 동기화 완료를 표시" — viz UI 먼저, 동기화는 백그라운드 + 상태 배너로 진행 알림. **fix 4 컴포넌트**: (1) `viz/_build/Start-Visualization.ps1` — 메인 PS1 이 `.vault_data/.sync-status.json` 초기 'running' write 후 별도 PowerShell 프로세스 (Start-Process hidden) spawn. 그 백그라운드가 git pull → sync-all 단계별 상태 파일 갱신 → 완료 시 'done'. 메인 PS1 은 즉시 chrome `--app` 띄움 (wait 없이). (2) `viz/server.js` — `SYNC_STATUS_PATH` 상수 + `handleApiSyncStatus(res)` 핸들러 + `/api/sync-status` GET 분기. 파일 부재 시 idle 반환. (3) `viz/components/sync-banner.js` 신규 — floating banner (top-right), 3s polling, 상태별 색/메시지: running (spinner + step + 메시지), done (✓ + reload 링크 + 5s fade), failed (✗ + 에러 상세), idle (숨김). done/failed 도달 시 polling 중단 (idempotent). (4) `viz/index.html` — sync-banner.js module script 마운트. (5) `Generate Visualization.exe` 재빌드 (53.5KB). **사용자 체감 변화**: viz `.exe` 더블클릭 → 즉시 chrome 띄움 (이전 35-57s 대기 → 0s) → 백그라운드 동기화 + UI banner 진행 표시 → 완료 후 reload 안내. 디바이스 자동 동기화 (R146) 효과는 그대로 + 사용자 체감 즉시. **fail-safe**: 백그라운드 실패 시 banner 가 에러 표시 + viz UI 는 자기 master 로 진입. env var off 시 idle banner (표시 안 됨). **R146 호환**: AIMV_VIZ_AUTO_PULL/SYNC 변수 그대로. throttling 미진행 (자주 viz 띄워도 부담 X — 백그라운드라 UI 무관). **노트북 검증 큐 발행**. 작업자: Claude (메인 PC). |
| R148 | 2026-05-28 | ✅ | 🕓 | **viz home KPI Tags/Connections change 노트 기반 derive — timeseries snapshot 시점 의존 제거** — 사용자 발견: 메인 PC vs 노트북 KPI 큰 숫자는 R142.5 후 일치하나 카드 하단 "+N today" 가 다름 (예: 메인 Tags +4, 노트북 Tags +12, Connected 메인 +1 노트북 없음). **원인**: `computeRecentChange(snaps, key)` 가 디바이스 자체 timeseries snapshot 의 직전 vs 현재 delta 기반 — 디바이스마다 master-build 시점/순서 다르면 같은 master 데이터인데도 일별 변화량 분포 다름. 노트북이 한 번에 git pull + rebuild → +12 흡수, 메인 PC 가 5-26 부터 분산 → +4. **사용자 의도** (KPI "+N today" = "오늘 이 데이터 얼마나 변했나") 와 어긋남. **fix**: `viz/pages/home.js` 에 `computeTagsRecentFromNotes(notes)` 헬퍼 신규 — "가장 최근 mtime 일자 + 그 날 mtime 인 노트들의 unique tags Set size". `master.notes` 만 사용 → 디바이스 무관. Tags 카드 `change: computeRecentChange(snaps, 'tag_count')` → `change: computeTagsRecentFromNotes(notes)`. **Connected 카드**: vault-pair 단위 (tag 공유 기반) 라 노트 기반 derive 어려움 → `change: null` (표시 제거, "—" 로 통일). **Notes / Vaults 카드**: 이미 노트 기반 (`notesSubLink` / `computeVaultsFromBirths`) — 변경 X. **효과**: 양 디바이스 같은 master 데이터면 KPI "+N today" 도 일치 (디바이스 무관). timeseries.json 자체는 유지 (vault births / sparkline 등 다른 용도). **viz 모듈 영향**: SPA reload 만으로 효과. server-side X. **미진행 후속**: 같은 패턴 다른 페이지 (timeseries / additions / 등) 검토 후 R148 확장. Connected 의 vault-pair "+N today" 정의 (옵션) 별 트랙. 노트북 검증 큐 발행. 작업자: Claude (메인 PC). |
| R146 | 2026-05-28 | ✅ | 🕓 | **viz 시작 시 디바이스 자동 동기화 — Start-Visualization.ps1 에 git pull + sync-all chain** — 사용자 통찰 "어떤 디바이스든 같은 시각화 결과" + "viz 더블클릭 하나로 데이터 정합" 답. 메인 PC ↔ 노트북 정합 시 발견된 master_index 시점 차이 (R142 사이클 Project_AIMindVaults -3 / AI -1 등) 가 사용자 수동 git pull + sync-all 호출에 의존하던 문제 해소. **fix**: `viz/_build/Start-Visualization.ps1` 의 새 인스턴스 시작 분기 (기존 인스턴스 활성화 분기 후) 에 chain 추가 — (1) `git pull --ff-only origin main` (`AIMV_VIZ_AUTO_PULL=true` 기본), (2) `node cli.js sync-all --skip-npm` (`AIMV_VIZ_AUTO_SYNC=true` 기본). 두 단계 모두 fail-safe — 네트워크/conflict/sync 부분 fail 시 viz 진입 계속 (자기 측 데이터로). env var 으로 off 가능. **사이클 통합**: R133 (viz 시작 시 master_index 검증) 의 fallback 은 그대로 유지 — R146 이 정상 동작 시 master_index 이미 빌드, R146 실패 시 R133 가 master-build 단독 호출. 위성 sync mirror (R139 fix) 는 sync-all 안에서 자연 발생. 다중 클론 격리 (R132 X-AIMV-Root) 와 동거. **효과**: viz `.exe` 더블클릭 → 자동 git pull (다른 디바이스 push 흡수) → sync-all (모든 vault_index 재빌드 + master_index 재빌드) → chrome `--app` 띄움. 사용자 수동 호출 없이 데이터 정합 보장. **viz `.exe` 재빌드**: ps2exe 51KB. **미진행**: idle 자동 종료된 instance 가 재시작 시 매번 sync-all 호출 비용 (~3-5s incremental) — 향후 mtime 기반 throttling 후보. 노트북 검증 큐 발행. 작업자: Claude (메인 PC). |
| R142.5 | 2026-05-28 | ✅ | 🕓 | **viz home KPI 카드 R142 client filter 적용 — Notes/Tags 카드 master raw → filterVisibleNotes 기반** — R142 옵션 b 검증 후 발견된 KPI 카드 vs 캘린더 헤더 불일치 (메인 PC NOTES 카드 1337 vs 캘린더 1173, 1164 차이). **원인**: `viz/pages/home.js:186-188` 의 Notes/Tags KPI 카드가 `m.note_count` · `m.tag_index` keys (master 측 raw) 직접 사용 → R142 client filter 미반영 → 페이지 간 카운트 불일치. **fix**: `viz/pages/home.js renderKpi` 함수에 `visibleNoteCount = notes.length || m.note_count` + `visibleTagSet` (notes.flatMap(n=>n.tags) Set) → `visibleTagCount = visibleTagSet.size || tagCountRaw` 추가. Notes 카드 `num: visibleNoteCount`, Tags 카드 `num: visibleTagCount` 로 교체. `notes` 가 비동기 fetch 중이면 master raw fallback (초기 빈 배열 회피). **Vaults / Connected 카드**: vault 단위라 영향 적음 — 변경 X. **기대 효과**: viz UI reload 시 home KPI 카드 NOTES 1337 → 1173, TAGS ~1202 → 1181. 캘린더 헤더 + additions 합산 + KPI 모두 일치. **노트북 발견 부수**: 노트북 master_index.json 에 `tag_count` / `connections` 필드 자체 부재 — viz/lib 가 런타임 derive 추정. R142.5 의 `notes.flatMap(...).Set` 패턴이 양 디바이스 일관성. **R143 (CDP) 의 단일 진입점 (R144 후보 — getVisibleData() helper)** 와 함께 진행 시 모든 페이지 통합. R142.5 는 home.js 만 즉시 fix, 다른 페이지 R144 에서 통합. 작업자: Claude (메인 PC). |
| R142 | 2026-05-28 | ✅ | 🕓 | **R140 옵션 b 구현 — viz 측 type/path 메타 노트 필터링** — 노트북 commit `8e6bc677` 의 R140 결정 (사용자: "데이터 수집방식과 형식을 바꾸는 것은 무리, 시각화 측에 필터가 맞겠음") 받아 메인 PC 위탁분 구현. **fix**: `viz/lib/system-vaults.js` 에 `META_NOTE_TYPES` 사전 (`folder-index`, `standard`) + `META_NOTE_PATH_PATTERNS` 정규식 4종 (`/Juggl_StyleGuide/`, `/CONTENTS_*.md`, `/Domain.md$/`, `/Project.md$/`) + `isMetaNote(note)` 판정 + `filterMetaNotes(notes)` helper + `filterVisibleNotes(notes)` (시스템 Hub + 메타 노트 둘 다 chain) 신규. **적용 페이지**: `viz/pages/additions.js`, `viz/pages/home.js`, `viz/pages/calendar.js` 의 `filterUserNotes` 호출 점 모두 `filterVisibleNotes` 로 교체. **효과 기대값** (R140 결정 노트 기준): 메인 PC max 셀 5-08 188 으로 복귀 (3-04 200 시드 응축 198 제외), 활동일 미변동, 3-04 셀 200 → ~2. 노트북 max 셀 4-23 191 → ~10 (메타 노트 191 제외 후 잔여) + 3-04 198 → ~2 → max 5-08 로 복귀 → **메인 PC vs 노트북 max 셀 위치·값 완전 일치 기대**. **R 번호 정리**: 노트북 commit `8e6bc677` 가 R140 (결정만) → R141 (viz 스냅샷 자동화, 이전 row 24, 메인 PC) → 본 구현이 R142. R143 후보 = CDP 양방향 통신 (별 사이클). **viz 모듈 영향**: SPA reload 또는 viz `.exe` 재시작 시 효과 발현 (server-side X, client module). 미진행 페이지 (`network`, `connections`, `tags`, `explorer`, `distribution`, `rules`, `settings`) 는 vault 단위라 영향 적음 + 별 트랙. 노트북 측 검증 큐 발행 (`_AGENT_COMMS/to_claude/20260528_claude_claude_R142_옵션b_검증.md`). 작업자: Claude (메인 PC). |
| R140 | 2026-05-27 | 🕓 | 🕓 | **viz 디바이스 간 점검 공유 자동화 — Viz-Snapshot.ps1 + 표준 폴더 + 노트북 동일 루틴** — R138/R139 사이클에서 메인 PC ↔ 노트북 viz 결과 불일치 진단 시 일일이 캡처/dump 손작업 비효율 해소. 사용자 통찰: "디바이스 간 시각화 점검 공유 루틴이 우선". **스크립트** (`Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/scripts/viz_snapshot/Viz-Snapshot.ps1`): PowerShell + System.Drawing 화면 캡처 + Node 로 master_index.json 파싱 → 표준 베이스명 (`YYYYMMDD_HHMMSS_{device}_{label}.{png\|md}`) 으로 PNG + MD 동시 생성. MD 메타에 raw/filtered/active days (created+mtime)/top15 created+mtime/vault counts/git hash/branch/timestamp 포함. **디바이스 식별**: hostname 자동 (`notebook\|laptop\|lap\|portable` 패턴 → notebook, 그 외 main) + `-Device main\|notebook` 명시 가능. **저장 폴더**: `Contents/Project/plan/distribution/viz_snapshots/` 신규. **표준 라벨**: calendar_created_6m, calendar_mtime_6m, home, additions_*, network, connections, tags, explorer 등 README 에 정의. **워크플로우**: 메인 PC + 노트북 모두 viz `.exe` 띄워서 페이지 활성 → `.\Viz-Snapshot.ps1 -Label "..."` → 표준 폴더 PNG+MD 생성 → git add + commit + push → 다른 디바이스가 git pull 로 수령 + visual/meta diff. **Script_Registry 등록**: CoreHub `.sync/_Standards/Core/Script_Registry.md` 에 새 섹션 "프로젝트 도메인 스크립트 (PowerShell 등)" + Viz-Snapshot.ps1 행 추가. **노트북 측 큐 발행** (`_AGENT_COMMS/to_claude/`): 같은 루틴 적용 + 표준 라벨 사용 + push 가이드. **v1 한계 (향후 후보)**: 화면 캡처는 현재 페이지만 (자동 페이지 전환 X), viz `.exe` idle 종료 시 viz API 응답 X (master_index 직접 파싱으로 메타는 보장), multi-monitor 미지원. 후속: `Viz-Snapshot-All.ps1` (자동 페이지 전환), `Generate Viz Snapshot.exe` (사이드 빌드), `/viz-snapshot` 스킬. 본 R140 은 R138/R139 의 잔여 노트 총개수 차이 (메인 1359 vs 노트북 1368, +9) 의 진단 기반 — 양쪽 vault별 counts 메타 dump 가 같은 폴더에 누적되면 어느 vault 에서 차이 나는지 즉시 식별 가능. **본 사이클 미진행**: 메타 노트 fallback 약화 (R138/R139 보고서의 R140 후보) 는 R141 후보로 재명명. 작업자: Claude (메인) |
| R139 | 2026-05-27 | ✅ | 🕓 | **sync version skip 버그 fix — core-sync-all 이 Preset Hub `_WORKSPACE_VERSION` 도 bump (위성 PULL 트리거)** — R138 노트북 검증 응답에서 발견된 시스템 버그. **현상**: `bump-version --broadcast` 직후 노트북 + 메인 PC 위성 모두 R138 index-build.js 미전파 (위성 R138 count=0, Preset Hub 7 만 count=1). **근본 원인**: `sync-workspace.js line 330` 의 `if (vaultVersion === hubVersion) direction = PLUGIN_ONLY` 분기 — 위성과 Preset Hub `_WORKSPACE_VERSION` 이 같으면 `.sync/` mirror 자체를 skip. R119 이후 broadcast 가 위성에 안 닿은 채 Preset Hub level 까지만 전파됨 (CoreHub `202605270001` vs AIHubVault Preset `202605140001` vs 위성 `202605140001` 동시 일치). **fix**: `core-sync-all.js` 에 `bumpPresetWorkspaceVersion(presetHub, coreVersion, message)` 헬퍼 추가 — 각 Preset Hub mirror 완료 직후 Preset Hub `_WORKSPACE_VERSION.md` 의 시퀀스 (YYYYMMDDNNNN) 에 새 row append (`| {newVer} | Core 전파 수신 (CoreHub {coreVer}) — {msg} |`). Preset Hub 자체 시퀀스 보존 (Custom 변경과 공존). `bump-version.js` 가 `coreSyncAll({...presetBumpMessage: opts.message})` 로 message 전달. **CoreHub broadcast 결과** (R139 시점): 6/6 Preset Hub 에 `bump-version.js` + `core-sync-all.js` 전파 (12 items). 각 Preset Hub `_WORKSPACE_VERSION → 202605270001` bump → 위성 sync 자연 mismatch. **sync-all 후 위성 R138 적용 전수 검증**: 30개 위성 중 **29 적용 / 1 누락** (MachineAssembly — Obsidian 미실행 pre-sync fail 부수 이슈, 별 작업). **메인 PC R138+R139 통합 효과** (vault_index 37개 force rebuild + sync-all 18s + master_index 직접 파싱): raw 1359 / filtered 1334 / max 셀 **3-04 200** (이전 측정 5-08 188 → 3-04 이동, 노트북 강제 cp 후 결과 3-04 198 과 일치) / 5-08 188 (2위) / 활동일 59. **검증의 의미**: R138 의 created fallback 강화가 이전 측정 (5-08 188) 에선 위성 미적용 상태였고, R139 fix 후 실제로 적용되어 created 결정 로직이 frontmatter.updated 우선으로 동작 → Juggl_StyleGuide 시드 노트 다수 (updated=2026-03-04) 가 3-04 셀로 박힘. **이전 R138 측정값 (메인 PC 5-08 188 max) 은 사실상 R138 미적용 상태**. **잔여 응축 (R140 후보)**: 3-04 200 은 시드 노트 응축, 4-23 (노트북) 191 은 frontmatter.updated 도 없는 메타 노트 birthtime fallback. R140 옵션: (a) `type: standard / folder-index` + 특정 path 패턴 (`CONTENTS_*`, `Juggl_StyleGuide/*`) 인덱싱 자체 제외 / (b) calendar viz 측 type 기반 필터링 / (c) R138 의 birthtime fallback 제거. **노트북 측 후속**: 다음 세션 진입 시 `_AGENT_COMMS/to_claude/20260527_claude_claude_R139_적용_검증.md` 처리 — git pull + sync-all 자동 mirror (강제 cp 불필요) + 재측정 → 메인 PC (3-04 200) 와 일치 확인. 작업자: Claude (메인) |
| R138 | 2026-05-27 | ✅ | 🕓 | **viz 시각화 일관성 Phase 4 — index-build.js created fallback 강화 (frontmatter.updated 보조)** — R136 (mtime frontmatter.updated 우선) push 후 노트북 교차검증에서 발견된 잔여 응축 해결. **노트북 보고** ([[20260527_노트북_캘린더_불일치_조사요청]], 노트북 phase3 캡처): 동일 코드(`4719214a`) 상태에서 노트북 캘린더 max 셀 389 (메인 PC 188 대비 2배+), 일자 4-23 06:49:54 UTC (= 노트북 git clone 시각) 에 모든 27 사용자 vault 의 노트가 응축. 노트북 측 체크리스트 B.2 (vault_index 38개 삭제 + sync-all) 정상 수행 (44 ok / 0 fail / 63s) 후에도 응축 잔존. **원인** (노트북 Claude Opus 4.7 1M 진단 + 메인 PC 코드 확인): `index-build.js line 280-281` 의 `created = fmCreated || birthtime` 로직이 노트북 권장과 동일. 즉 코드는 이미 frontmatter.created 우선이나 **frontmatter.created 필드가 없는 노트 (type=standard, folder-index, knowledge 등 다수)** 가 `birthtime` (`fs.stat.birthtime` = NTFS ctime) fallback → git clone 시 모든 파일 ctime 이 한 시점으로 박혀 응축. 메인 PC 는 노트들이 실제 작성 시점에 만들어져 자연 분포라 문제 없음. 디바이스 간 created 값 차이가 본질. **fix**: `index-build.js buildNoteObject` 의 created 결정 로직을 **frontmatter.created → frontmatter.updated → birthtime** 3단 fallback 으로 강화. `fmUpdatedForCreated = normalizeCreatedField(fm.updated)` 신규 + `created = fmCreated || fmUpdatedForCreated || birthtime`. frontmatter.updated 는 R119 노트 작성 표준화 후 거의 모든 노트가 보유하므로 noteCt 응축 회피 효과 크다. updated 자체가 vault 변경 시점이라 메타 노트는 약하나 NTFS clone 시각 응축보다 분산도 높음. **CoreHub broadcast**: 6/6 Preset Hub 에 index-build.js 전파 (6 items). **정본 메인 PC 검증**: 37 vault_index.json 삭제 + sync-all (full rebuild, 43 ok / 1 fail (MachineAssembly Obsidian 미실행 부수) / Index builds 43 ok / Master build ok / Elapsed 23s). viz API `/api/all-notes` 측정: raw 1358 / filtered 1333 (이전 1357/1332 + 본 사이클 보고서/큐 1) / max 셀 5-08 **188** (변동 없음 — 메인 PC 는 frontmatter 잘 박혀 있어 R138 효과 무관). **노트북 측 후속**: 다음 세션 진입 시 `_AGENT_COMMS/to_claude/20260527_claude_claude_노트북_R138_적용_검증.md` 큐 처리 — git pull + vault_index 38개 삭제 + sync-all + phase5 캡처 → 메인 PC (1333 / 188) 와 일치 검증. **본 R 의 의의**: 디바이스 간 git clone NTFS 시각 응축 해소 — 멀티 디바이스 환경에서 viz 시각화 일관성 회복 핵심 fix. **미진행 (R139 후보)**: frontmatter.updated 도 부재한 메타 노트의 fallback 약화 (vault 자체 birth 또는 vault_index 등록 시점). 본 R138 효과 확인 후 잔여 응축 발견 시 진행. +10 차이 (메인 PC 1332 vs 노트북 1342, raw 도 +10) 는 단순 인덱스 시점 차이 — 노트북 측 sync-all 재실행으로 동기화 예상. 작업자: Claude (메인) |
| R137 | 2026-05-27 | ✅ | 🕓 | **viz 시각화 일관성 Phase 3 — Gap 5 + Gap 3 + Gap 7 + 시스템 Hub build fail 해소** — Phase 1·2 완료 후 잔여 Gap 처리. **Gap 5 — master.notes 에 mtime/created 누락**: `master-index-build.js` 의 notes 합산 시 `vault_id, title, path, type, tags` 만 포함, mtime/created 누락 → page 가 시계열 데이터 필요 시 `/api/all-notes` API 강제 호출 부담. fix: aggregate 시 `mtime: note.mtime || null, created: note.created || null` 추가. master.notes 만으로 시계열 시각화 가능. **검증**: notes[0] keys = `vault_id, title, path, type, tags, mtime, created`. notes[0] mtime = `2026-03-23T00:00:00` (frontmatter.updated 기반), created = `2026-04-18T06:00:33` (ISO datetime). **Gap 3 — additions basis=created 정렬**: `server.js handleApiAdditions` 의 정렬이 `created` 단일 키 → date only (T00:00:00 패딩) 노트들 같은 시각으로 처리되어 시간 순서 결정 불가. fix: 1차 sortKey 동률 시 mtime 2차 정렬 추가. mtime 은 R136 이후 frontmatter.updated 우선이라 정확. **시스템 Hub build fail 해소**: `index-build.js` 의 `Contents/` 부재 시 ERROR + exit 1 → INFO + success exit 강등. CoreHub · AIHubVault_Minimal · AIHubVault_{Domain,Lab,Project,Diary} 6 인프라성 Hub 는 콘텐츠 인덱싱 대상 아님 (Contents/ 폴더 없음). sync-all 의 자동 인덱싱이 이들을 무의미한 fail 로 카운트하던 문제 해소. **검증**: sync-all 결과 `Index builds 43 ok / 0 fail` (이전 37/6). **Gap 7 정합 측정** (사용자 1340 노트 통과 후): vault_count 37 / note_count 1356 / tag_owners 318 / connections 45 / concept_map 145 / owner_conflicts 0. 정상 정합 유지. R134 이전 추정값과 큰 차이 없음 — 즉 filter 통과로 노트 풀이 늘어도 owner/connection 결정 로직은 안정. **CoreHub broadcast**: 6/6 Preset Hub 에 index-build.js + master-index-build.js 전파 (12 items). 정본 sync-all full rebuild: 43 vault ok / 0 fail / Elapsed 17s. 정본 master 5월 26일 20:08 → 2026-05-27 갱신 (이전과 동등 카운트). **부수 잔여**: MachineAssembly pre-sync fail (Obsidian reload fetch failed) — Obsidian 미실행 환경 영향, sync 자체 데이터에 무관. 별 작업. 작업자: Claude (메인) |
| R136 | 2026-05-27 | ✅ | 🕓 | **viz 시각화 일관성 Phase 2 — Gap 1 (mtime frontmatter 우선) + Gap 2 (vault birthtime vault_index 가장 오래된 created)** — 노트북 보고서 (`20260527_viz_데이터_랜더링_파싱_상태_보고서.md`) Phase 1 검증 결과 (Gap 6 코드 PASS, 시각 효과는 Gap 1 미해소로 미발현) 확인 후 Phase 2 진행. **Gap 1 — git pull mtime 리셋**: `index-build.js fileStat.mtime` 가 git pull 시각으로 리셋되어 같은 클론에서 받은 파일 다수가 한 날짜에 응축 (1340 노트 3 일에 집중). fix: 노트 mtime 결정 로직을 **`frontmatter.updated` 우선 → `fs.stat.mtime` fallback** 으로 변경 (`normalizeCreatedField` 재사용, Gap 3 일부 처리). incremental 효율 보존을 위해 vault_index schema 에 `fsMtime` 필드 추가 (1차 fast-path 비교용, legacy `mtime` 자동 fallback). hash check 단계도 `ex.fsMtime` 만 갱신하여 git pull 후 재인덱싱 회피. **Gap 2 — NTFS clone 시각**: `server.js handleApiVaultBirths` 의 `statSync().birthtime` 가 vault 폴더 NTFS 생성 시각 = git clone 시각 → 34 vault 가 4-23 한 셀에 응축. fix: vault_index 의 가장 오래된 노트 `created` 사용 (frontmatter 기반). 부재 시 `statSync().birthtime` fallback. **CoreHub broadcast**: 6/6 Preset Hub 에 index-build.js 전파 (6 items). **정본 전수 재빌드**: 38 vault_index.json 삭제 → sync-all (full rebuild, 37 ok / 6 fail — Basic*Vault 6 시스템 Hub 의 Contents 표준만 인덱싱). **검증 — Gap 1 효과**: mtime 분포 **3 일 (4-23, 5-20, 5-26) 응축 → 42 일자 자연 분포** (5-06 15 · 5-08 2 · 5-11 551 · 5-12 12 · 5-14 18 · 5-18 57 · 5-19 7 · 5-24 1 · 5-25 1 · 5-26 4 등 사용자 실제 작업 시각). **검증 — Gap 2 효과**: vault birth 분포 **2 일 (4-23 34vault + 4-24 1vault) → 13 일자** (2-27 ~ 5-14, 사용자 실제 vault 생성 시각). 정본 master_index: 37 vaults · 1356 notes · 45 connections · built 5-26T20:08. Phase 1 (R135) 의 calendar visualMax P95 효과가 비로소 의미 — 42 일자에 분포된 노트 사이에서 outlier 셀 1개가 visualMax 의 정상 분포 결정. **미진행 (낮은 우선)**: Gap 3 (created date 0 패딩 — 정밀도 향상, additions basis=created 시간 정렬), Gap 5 (master.notes mtime/created 누락), Gap 7 (owned_tags · connections 정합 측정). **부수 발견**: AIHubVault_{Diary,Domain,Lab,Minimal,Project} + CoreHub 의 index build fail — 시스템 Hub 의 Contents 표준 가이드 노트 처리 시 빈 vault_index 또는 frontmatter 호환 이슈 (별 분석 후속, viz 영향 없음 — 시스템 Hub 필터로 제외). MachineAssembly pre-sync fail — Obsidian reload fetch failed (Obsidian 미실행 환경 부수 이슈). 작업자: Claude (메인) |
| R135 | 2026-05-27 | ✅ | 🕓 | **viz 시각화 일관성 Phase 1 — calendar visualMax P95 (Gap 6) + timeseries 일별 dedup (Gap 4)** — 노트북 교차검증 (R134 직후) 의 `20260527_viz_데이터_랜더링_파싱_상태_보고서.md` 7 Gap 진단 중 Phase 1 두 개 해결. **Gap 6 — calendar.js visualMax outlier 압도**: 기존 `visualMax = max` 가 단일 outlier 셀 (예: 5-20 1068 건) 에 의해 visualMax 결정 → 다른 모든 셀 농도가 그에 비해 미미 → 6 month heatmap 거의 빈 셀. fix: positive count 배열 sort + P95 (95th percentile) 값을 visualMax 로 사용. outlier 1068 건은 그대로 표시되나 나머지 셀들이 P95 기준 색농도 분포 → 시각상 자연스러운 heatmap. **Gap 4 — timeseries 같은 날 누적 → sparkline 평선**: 기존 `dedup: last.timestamp === new.timestamp` (millisecond 단위) 룰이 같은 날 다회 master-build 시 매 빌드마다 push → 5-26 두 시간 안에 42 snapshot 누적 → sparkline 평선. fix: dedup 키를 `date` (YYYY-MM-DD) 로 변경하여 일별 dedup 복귀. 추가로 기존 누적 자동 정리 로직 (`updateTimeseries` 시작 시 Map 기반 dedup) 추가 — 같은 날 중복 snapshot 마지막만 유지 + timestamp ASC 정렬 보존. **검증**: 정본 master-build 재실행 → 42 snapshot 자동 정리 → 11 snapshot (5-08 ~ 5-26 일별 분포, vault_count 32→38 점진 증가, note_count 1212→1364 자연스러운 추세). **CoreHub broadcast**: `bump-version --broadcast` 6/6 Preset Hub 에 timeseries.js 전파 (6 items). 정본 viz + BasicVaults → Korean + English SellingVault robocopy. Korean SellingVault master-build 재실행 — 시스템 Hub 6 vault / 25 notes 정상. **미진행 (Phase 2)**: Gap 1 (mtime git pull 시각 리셋) + Gap 2 (vault birthtime NTFS 시각) — 다음 사이클 R136. 작업자: Claude (메인) |
| R134 | 2026-05-27 | ✅ | 🕓 | **viz 캘린더/additions/home 노트 누락 핫픽스 — `filterUserNotes` 의 `vault_id` 키 인식 추가** — 사용자 보고 (정본 viz 캘린더에 노트 기록 깡그리 사라짐) 진단. **원인**: `viz/lib/system-vaults.js` 의 `filterUserNotes` 가 노트의 `vault` · `vaultId` 키만 체크하고 `vault_id` 누락. 그러나 `viz/server.js` 의 `/api/all-notes` 응답이 `vault_id` 키 사용 (line 290, `vault_id: vid`). 결과: 모든 노트에서 `vid = n.vault || n.vaultId` 가 undefined → falsy → 필터 제외. 사용자 1339 노트 + 시스템 Hub 25 노트 모두 화면에서 사라짐. **검증**: 정본 viz/server.js 띄움 → `Invoke-RestMethod /api/all-notes` 응답 1364 notes (시스템 Hub 25 + 사용자 vault 1339 + No vault_id 0 — 모두 `vault_id` 키 보유). R134 fix 후 filter: 1364 → 1339 (시스템 Hub 25 제거 + 사용자 vault 1339 유지). **수정**: `filterUserNotes` 에 `n.vault_id` 키 체크 추가 (`vault` · `vaultId` · `vault_id` 셋 다 fallback). `loadIndex.js` 의 `filterSystemHubsInPlace` 는 이미 `vault_id` 체크 (R131 시점). 즉 본 fix 는 `system-vaults.js` 의 helper 한 군데만 갱신. **영향 범위**: home.js / additions.js / calendar.js 의 `filterUserNotes` 호출 모두 정상 복구. calendar `vault-births` (시스템 Hub 표시 보존) 영향 없음 — `isSystemVault(v.vaultId)` 동작 그대로. 작업자: Claude (메인) |
| R133 | 2026-05-27 | ✅ | 🕓 | **인덱싱 자동화 — sync-all 통합 (B) + viz 시작 시 검증 (A) 복합** — 노트북 교차검증 (R131 commit `ad761bf4`) 의 `20260527_노트북_vault_index_누락_원인분석.md` 보고서 대응. **현재 인덱싱 주기 조사**: `review` 명령 통과 후 단일 vault `index build -i` 자동 호출이 유일. `sync` / `pre-sync` / `sync-all` / `Setup New Device` / `viz` 시작 / hooks 모두 인덱싱 자동 트리거 X. 디바이스 간 git clone/pull 후 (`.gitignore` 의 `**/.vault_data/` 룰로 인덱스 git 비운반) 사용자가 모든 vault 에 명시적 `aimv index build` 수동 호출해야 viz 정상 동작. **해결 — B + A 복합**: (B) `sync-all.js` 에 `runIndexBuild` + `runMasterBuild` 함수 추가, 메인 loop 안에 각 vault sync 완료 후 `index build -r <vault> -i` 자동 호출 (incremental) + 전체 loop 후 CoreHub cli.js 로 `index master-build -r <aimRoot>` 자동 호출. 환경 변수 출력: `SYNC_ALL_INDEX_BUILT`, `SYNC_ALL_INDEX_FAILED`, `SYNC_ALL_MASTER_BUILD`. (A) `Start-Visualization.ps1` 의 server 시작 직전에 `master_index.json` 부재 검증. 부재 시 CoreHub cli.js 로 `index master-build` 자동 호출 (Hidden + stdout/stderr 임시파일 redirect, NoConsole 호환). 빌드 실패 또는 cli.js/node_modules 부재 시 MessageBox 안내. cli.js 의 `sync-all` 명령에 `--skip-index` 옵션 신규 (자동 인덱싱 건너뛰기). **CoreHub Core 편집 → 자동 전파**: `bump-version --broadcast` 6/6 Preset Hub 에 cli.js + sync-all.js 전파 (12 items copied). `Generate Visualization.exe` 재빌드 (49.5KB). **dry-run 검증**: `aimv sync-all --dry-run --skip-npm` → 44 vault sync OK + 신규 환경 변수 정상 출력 (Index builds 0/0, Master index skipped — dry-run 이라 0). **사용자 시나리오**: 노트북 처음 pull 후 `Sync All Vaults` 더블클릭 → 모든 vault sync + 인덱스 자동 빌드 + master 합산 → viz 즉시 정상 동작. sync-all 안 쓴 사용자도 viz 시작 시 자동 검증 + 빌드 보완. **미진행 후속**: `.gitignore` 의 `**/.vault_data/` 룰은 유지 (인덱스는 자동 빌드 우선, git noise 회피). `Setup New Device` 가 이미 `sync-all` 호출하므로 자동 통합 자연 따라옴. 작업자: Claude (메인) |
| R132 | 2026-05-27 | ✅ | 🕓 | **viz 다중 AIMindVaults 클론 격리 — `X-AIMV-Root` 정체성 헤더 + 런처 fallback** — 노트북 교차검증 (R131 commit `76ad6967`) 에서 발견된 이슈 1 (다중 클론 동시 실행 시 런처가 다른 클론 server 로 점프) 해결. **3중 원인 분석** (노트북 보고서 `20260527_viz_포트충돌_다중클론_이슈분석.md`): (1) port 충돌 — `viz/server.js` 의 `DEFAULT_PORT=8765` + `PORT_FALLBACKS=[8765,8766,8767]` 동일하여 첫 시작 클론이 8765 차지, 두 번째 클론은 fallback 8766/8767 사용. (2) **런처 URL 하드코딩** — `Generate Visualization.bat` L5 `set "URL=http://localhost:8765"` 고정, server 가 다른 port 에 bind 됐어도 무조건 8765 엶. (3) "기존 인스턴스 활성화" 식별자 부재 — R131 의 `Start-Visualization.ps1` port 점유 체크가 응답 server 가 자기 클론인지 검증 X. **수정 (옵션 C 채택)**: (1) `viz/server.js handleRequest` 시작에 `res.setHeader('X-AIMV-Root', ROOT_DIR)` 1줄 추가 — 모든 HTTP 응답에 정체성 헤더. ROOT_DIR = viz/ 의 부모 = AIMindVaults 멀티볼트 루트 절대경로. (2) `viz/_build/Start-Visualization.ps1` 의 port 점유 체크 로직 재작성 — 8765 부터 20 port 순회하며: free port 발견 → 자기 server 시작. listening 중 → `GET /` 의 `X-AIMV-Root` 헤더 확인. 자기 ROOT 일치 → 기존 인스턴스 활성화 (Chrome 만 열고 종료). 다른 클론 → 다음 port 계속 순회. 20 port 모두 막히면 안내 MessageBox + exit. (3) `$myRoot = Split-Path -Parent $exeDir` 로 비교 — `$exeDir` (= viz/ 자신) 이 아닌 viz/ 의 부모 (= server.js 의 ROOT_DIR) 와 비교. **검증** (메인 PC 시뮬레이션): 정본 (`C:\AIMindVaults\viz\server.js`) 8765 + 사본 (`C:\SellingVault\Korean\AIMindVaults\viz\server.js`) 8766 동시 실행 → 각자 X-AIMV-Root 응답 (`C:\AIMindVaults` vs `C:\SellingVault\Korean\AIMindVaults`) → PASS. `Generate Visualization.exe` 재빌드 (47KB). **미진행 (후속)**: `Generate Visualization.bat` 의 URL 하드코딩 — .bat 가 사용자가 직접 호출하는 단순 진입점이므로 .exe·.vbs 우선 권장 + .bat 자체는 옛 동작 (단순 8765) 유지. 본 R132 는 .exe + .vbs (= .bat hidden wrapper) 경로만 격리. .bat URL 해결은 노트북 보고서 옵션 D (PowerShell 래퍼) 로 후속. 작업자: Claude (메인) |
| R131 | 2026-05-26 | ✅ | 🕓 | **viz 통합 변경 + Korean SellingVault 첫 배포 + 시스템 Hub 시각화 필터링** — 3 트랙 누적. **트랙 A (5-25 viz 테마 엔진 — 이미 정본 변경, 본 R 에서 배포 반영)**: UI 색 편집기 + 전역 적용 (Phase 1+2), settings 4그룹 캐러셀 (무한순환 peek), 표시 토글 fix, network/connections 그래프 볼트 색 패널, settings 그래프 색상 섹션 제거. `viz/lib/{user-config,theme-engine}.js` 신규 + `components/theme.js` + `router.js` + `pages/{settings,home,network,connections}.js` 수정. **트랙 B (5-26 viz .exe 패키징)**: `_build/{Start-Visualization,build-icon,build-exe,create-shortcut}.ps1` + `tools/ps2exe` MIT 동봉 + `viz.ico` 자체 생성 + `Generate Visualization.exe` (45KB, NoConsole + idle 자동종료 + port 점유 시 기존 인스턴스 활성화). PS 5.1 + ps2exe NoConsole 환경에서 native command stdout/stderr 가 MessageBox 로 잡히는 문제 회피 (Start-Process hidden + stdout/stderr 임시파일 redirect). **트랙 C (5-26 시스템 Hub 시각화 필터링)**: `viz/lib/system-vaults.js` 신규 — `SYSTEM_HUB_IDS` 사전 (CoreHub + AIHubVault* 6 Preset Hub + Basic*Vault 5 템플릿 = 12개) + `isSystemVault` / `filterUserNotes` helpers. `viz/lib/loadIndex.js` 에 `filterSystemHubsInPlace` 추가 — `master.vaults` · `master.notes` 에서 시스템 Hub 자동 제외 (network·connections·home·settings 통계 자동 처리). `viz/pages/{home,additions,calendar}.js` — fetch 결과 (`/api/all-notes`, `/api/vault-births`, `/api/additions`) 사용자 Hub 만 필터링. **calendar 의 `/api/vault-births` 는 raw master_index 서버측 접근이라 시스템 Hub 도 timeline 셀 핀 + 헤더 표시 보존** (Hub 생성 메타 정보 시각화 가치). 사용자가 `/create-preset-hub` 로 만든 커스텀 Hub 는 `SYSTEM_HUB_IDS` 외 ID 이므로 자동 사용자 분류. **트랙 D (5-26 Korean SellingVault 첫 viz 배포)**: 정본 `C:/AIMindVaults/viz/` → `C:/SellingVault/Korean/AIMindVaults/viz/` 첫 robocopy + 정본의 R092~R130 누적 변경 (`.claude/{rules/core,commands/core,hooks,templates}` + `.codex/{rules,skills}/core` + `_Standards/Core` 45 + `docs/generated` + `Vaults/BasicVaults/*` Multi-Hub 누적) 함께 sync. `_ROOT_VERSION.md` Korean 컬럼 12건 ✅. `_STATUS.md` 통째 재작성 — 사용자 개인 자산 (Domains_*, Lab_*, Projects_*, Personal 등 27 위성 볼트) 제거 + 시스템 볼트 12 + 사용자 추가 가이드. 진입점·스킬·Hub 본문 `distribution-content-safety` 일반화 (CLAUDE.md / AGENTS.md 키워드 매핑 표 빈 템플릿화, AGENT_ONBOARDING.md L323 일반화, VaultTypes.md 예시 추상화, 모든 Hub `_WORKFLOW.md`/`Plugins/README.md`/`VaultTypes/README.md` frontmatter `- TileMapToolKit` tag 제거 36 파일, `tag-aliases.yaml` `jissougame` alias 제거 7 파일, `delegate-task.md` / `open-notes.md` / `reindex.md` 예시 일반화, `BasicProjectVault/CLAUDE.md` + `BasicLabVault/CLAUDE.md` 위성 명시 제거, `BasicProjectVault/yt_to_note.py` 절대경로 → env var, `viz/lib/mask.js` 사용자 vault alias 비움, `distribution-sync.md` 예시 일반화). AIHubVault 사용자 잔재 (`Contents/HowAgentWorks/` 2 노트 + `Tags/#사용자태그/` 9 폴더 + 모든 Hub `.space/` 18 잔재) 제거. 정본 cli-node `index master-build` 로 SellingVault Korean 안에서 `.vault_data/master_index.json` (11.5KB, 시스템 Hub 6 / 25 notes / TagOwners 0 / Connections 0 / 사용자 자산 매칭 0) + `timeseries.json` 생성. `npm install` (commander + gray-matter) 사용자 환경 의존. `.gitignore` `**/node_modules/` 추가. 잔존 레거시 (정본 측 R 누적의 정본 자산 인용) 후속 큐 (`_AGENT_COMMS/to_claude/20260526_..._distribution_legacy_cleanup_큐.md`). English 배포 영문화는 별 트랙 (Korean 정본 사본 robocopy 완료, 영문화 + .exe 영문 빌드 후속). 작업자: Claude (메인) |
| R130 | 2026-05-20 | – | – | **`.agents/_MANIFEST.md` core 행 카운트 TBD 정정 + 헤더 자동생성 주장 실태 정정** — 모바일 원격 세션 셀프 큐 (`20260520_..._MANIFEST_count_TBD_채움`, 원격 브랜치 `claude/clarify-repo-access-QeZxp` fast-forward 머지) 처리. **진단**: `agents-sync.js` 에 "영역별 버전" 표 생성 로직 없음 — `SYNC_MAP` 은 `.agents/{rules,commands,hooks}/core` → `.claude/`·`.codex/` 미러 복사만, manifest 는 `updateManifestTimestamp` 로 `last_updated` 만 자동 갱신. 즉 표는 수동 관리 문서이고 큐의 (A)안 "생성기 카운트 미반영" 전제는 오류 (표 자동생성 기능 자체가 없음). **사용자 결정** = 수동 표 정정 (생성기 미변경) + hooks 0 표기. core 행 `commands TBD + hooks TBD` → `commands 20 + hooks 0` (실측: `.agents/commands/core/` 20 .md, `.agents/hooks/core/` 0). 헤더 `AUTO-GENERATED ... DO NOT EDIT MANUALLY` → `수동 관리 문서 — last_updated timestamp 만 자동 갱신, 표·분류·파일 수는 수동 편집` 으로 정정. **검증**: `agents-sync --verify` exit 0 (drift 0, Same 120). 생성기·Core CLI 미변경이라 `bump-version --broadcast` 불필요, `.agents/_MANIFEST.md` 는 SYNC_MAP 비대상이라 미러 비전파. `.agents/` 루트 메타 정정이라 배포 무관. 작업자: Claude (메인) |
| R126 | 2026-05-18 | – | – | **GameDesign frontmatter legacy warning 정규화** — 사용자 요청에 따라 Shorts 취합 후 남아 있던 `GameDesign` legacy frontmatter warning 14건을 CoreHub `note-types.yaml` 기준으로 정리. `Contents/Contents.md`, `Contents/Domain/Domain.md`, `CONTENTS_AI_RULES.md`, `CONTENTS_GLOSSARY.md`, `Domain` 일부 노트, `Juggl_StyleGuide` 문서, `Project/Project.md`, archive tag 문서의 `type`, `tags`, `created`, 내부 링크를 보강. 검증: `POST_EDIT_REVIEW_BAD=0`, `POST_EDIT_REVIEW_WARN=0`, `POST_EDIT_REVIEW_FM_WARN=0`, `POST_EDIT_INDEX_UPDATED=1`, GameDesign index Notes 23 / Tags 70. 작업자: Codex. |
| R125 | 2026-05-18 | 🕓 | 🕓 | **에이전트 공용공간 R122 Phase 1 — `.agents/` 정본 권한 이전 + 카테고리화 + agents-sync CLI + 영역별 미러 + SessionStart hook** — 5-15 사용자 통찰 ("Claude/Codex 하네스 cross-update 비효율, 공용공간 + 자동 갱신 루틴 필요") 기반 plan 작성 후 진입 ([[20260518_에이전트_공용공간_및_자동갱신_설계]]). 사용자 결정: 코어+도메인 모두 자동 sync + 도메인 볼트 1:1 매핑 + 영역별 독립 버전 + 진입점·hook·custom 에이전트별 유지. **Phase 1.1**: `.agents/rules/custom/{Unity,Blender,Meshy,Discord,Notion,Distribution,CreateVault}/` 7 도메인 폴더 + `.agents/commands/{core,custom/*}/` 신설. archive 10 파일 (`blender-mcp`, `discord-bot`, `distribution-deploy`, `meshy-api`, `notion-sync`, `serena-mcp`, `sync-version-priority`, `unity-scripting-style`, `unity-tools`, `vault-individualization`) 도메인별 mv + `.claude/rules-archive/` 동일 10 git rm + 경로 참조 14 파일 일괄 갱신 (`.claude/rules/core/{_skill-router,user-guidance}.md` + `.codex/rules/skill-router.md` + `.codex/skills/create-aimind-vault/SKILL.md` + `.claude/commands/{core/create-vault,custom/{unity-dev,discord-admin,notion-record,meshy-workflow,blender-workflow,distribute}}.md` + `docs/customization.md` + `.agents/rules/core/` mirror 2) + `.agents/skills/meshy-3d-*` 3개 → `.agents/commands/custom/Meshy/` 이관. **Phase 1.2**: `.agents/_MANIFEST.md` 신규 — 영역별 버전 표 (core + 7 도메인) + 트리거 키워드 + 도메인 볼트 매핑 + sync 매트릭스. **Phase 1.3**: CoreHub `agents-sync.js` 신규 (160 LOC) — `.agents/{rules,commands,hooks}/{core,custom/*}` → `.claude/`+`.codex/` 미러 자동 sync + sha256 비교 + 영역별 처리 + dry-run/apply/verify/area 옵션 + orphan 보고 + MANIFEST timestamp 자동 갱신. `bin/cli.js` `agents-sync` 명령 등록. **Phase 1.4**: `.claude/commands/{core,custom}/` 30 파일 도메인별 mv (note-from-* custom 3 제외) + `agents-sync --apply` 첫 호출 = 104 미러 자동 생성. `.md` 헤더 주석이 Claude `Skill` 도구 description 으로 잘못 인식되는 버그 fix (`commentPrefix` 함수에서 md 제거) + sync 재호출 = 105 .md updated. **Phase 1.5**: `.claude/settings.json` `hooks.SessionStart` 에 `agents-sync` 등록 (새 세션 진입 시 자동 sync) + `AGENTS.md` § 세션 시작 순서 0번 단계로 `agents-sync --verify` 명시 (Codex hook 비활성 대체). **Phase 1.6**: `bump-version --broadcast` = 6/6 Preset Hub 에 cli.js + agents-sync.js 전파 (12 items). **검증**: `agents-sync --verify` exit 0 (drift 0) + 스킬 인식 정상 (core 21 + custom 도메인별 11 + note-from-* custom 3). 작업자: Claude (메인) |
| R124 | 2026-05-18 | – | – | **게임 개발 Shorts 14개 GameDesign 취합** — 채널 `UCRIomWe2by56WTTeoRFdEJA` Shorts 27개 중 게임 개발·게임 수학·고전 게임 구현 관련 14개를 선별. `Vaults/Domains_Game/GameDesign/Contents/Domain/GameDev_Shorts_GameMath_Implementation_Collection.md` 모음 노트 생성, `GameDesign/Contents/Domain/Domain.md`, `GameDesign/_STATUS.md`, `GameDesign/.codex/AGENT_STATUS.md`, 루트 `_STATUS.md` 갱신. 포함 주제: 코요테 타임, 점프 버퍼링, 이징/베지에, 카메라 데드존, 오일러 적분, 펄린 노이즈, 화학 엔진, Z 타게팅, 비밀번호 세이브, 의사난수, 로그라이크 맵 생성, 아핀 변환, 팔레트 그래픽. 검증: `POST_EDIT_REVIEW_BAD=0`, `POST_EDIT_REVIEW_WARN=14`(기존 legacy warning), `POST_EDIT_INDEX_UPDATED=1`, GameDesign index Notes 23 / Tags 51. 작업자: Codex. |
| R123 | 2026-05-18 | – | – | **Maker Evan 신규 영상 4개 AI_Coding 노트화** — `@maker-evan` 최신 업로드를 확인해 기존 노트가 있는 `BZPaZzjLIOY`, `MYEYID9Euzs` 앞의 미처리 4개를 신규분으로 판단. `Vaults/Domains_Dev/AI_Coding/Contents/Domain/Review/`에 `AIAgent_Permission_Safety_PocketOS`, `Claude_Cowork_Tutorial`, `Claude_Cowork_KakaoTalk_Automation`, `AI_Skill_Selection_Criteria` 4개 study-note 생성. `Contents/Domain/Domain.md`, `AI_Coding/_STATUS.md`, `AI_Coding/.codex/AGENT_STATUS.md`, 루트 `_STATUS.md` 갱신. 검증: `POST_EDIT_REVIEW_BAD=0`, `POST_EDIT_REVIEW_WARN=13`(기존 legacy warning), `POST_EDIT_INDEX_UPDATED=1`, AI_Coding index Notes 31 / New 4 / Tags 46. 작업자: Codex. |
| R122 | 2026-05-18 | – | – | **DevFoundation Domain Preset 복구 — Codex 생성 실수 수복** — R117 에서 `BasicContentsVault + AIHubVault(default)`로 생성됐던 `Vaults/Domains_Dev/DevFoundation/`을 원래 도메인 볼트 기준으로 복구. `aimv rebase --hub AIHubVault_Domain` dry-run 후 실제 적용하여 `.sync/hub-source.json`을 `hubId="domain"`, `hubPath="../../BasicVaults/AIHubVault_Domain"`으로 갱신하고 Domain Preset workspace sync 실행. `Contents/Domain/Review/` 기반 구조를 제거하고 ZK 표준 폴더 (`00_Fleeting`, `01_Literature`, `02_Permanent`, `03_MOC`, `04_Synthesis`, `05_Pattern`, `_AI_Drafts`, `_archive`)로 수복. 기존 with2511 개발 기초 영상 노트 22개는 삭제하지 않고 `Contents/_AI_Drafts/with2511_review/`로 이동, `status: draft` + `tags: [Zk, AiDraft, Literature, SourceVideo, Review]`로 정리. 중심 MOC `Contents/03_MOC/DevFoundation_MOC.md`, `Contents/_index.md`, `Contents/_log.md` 추가. `CLAUDE.md`, `AGENTS.md`, `_STATUS.md`, `_VAULT-INDEX.md`, `_Standards/CONTENTS_SPEC.md`, `README.md`, `.codex/AGENT_STATUS.md`, `_SESSION_HANDOFF_CODEX.md`를 Domain Preset 기준으로 갱신. 검증: DevFoundation review `POST_EDIT_REVIEW_BAD=0`, `POST_EDIT_REVIEW_WARN=0`, `POST_EDIT_REVIEW_TAG_VIOLATIONS=0`, `POST_EDIT_REVIEW_FM_WARN=0`, `POST_EDIT_INDEX_UPDATED=1`; master partial update OK. 사용자 개인 지식 볼트 복구라 배포 무관. 작업자: Codex |
| R121 | 2026-05-15 | 🕓 | 🕓 | **viz 5-15 일괄 fix + 노트/볼트 열기 기능 + 카드 desc·placeholder 일괄 단순화** — 5-13/R116 viz GUI 사용자 검증 후 발견 버그 + UX 단순화 + 5-11 큐 (노트/볼트 열기) Phase 1 처리. **트랙 A — 버그 fix**: (1) KPI 카드 라우팅 `&basis=created` 자동 부여 — timeseries delta 는 신규 노트 추가 (created) 기반인데 default `basis=mtime` 으로 가서 5-13 mtime 노트 0건 빈 결과 발생. (2) KPI Vaults 카드 birthtime 단일 우선 — timeseries 5-11 vault `37→38→37` 일시 카운트 노이즈를 +1 변화로 잘못 잡아 vaults view 빈 결과. `computeVaultsFromBirths` 만 사용. (3) Notes KPI 두 sub-link (생성/갱신) — 사용자 요청, `fetchAllNotes()` + `computeLatestNote(notes, basis)` + `notesSubLink()` UI. CSS `.kpi.multi .change.sub-link`. (4) additions.js basis/date 토글 indirection 버그 — onBasisClick/onDateInput 가 state 먼저 변경 → setHashFromState → hashchange → onHashChange 의 `q.basis !== state.basis` 비교 false (이미 같음) → load() 호출 안 됨. fix: 토글 핸들러 직접 `load()` 호출, hashchange 는 외부 진입만 처리. **트랙 B — UX 단순화**: home 카드 desc 6개 일괄 명사구 ('볼트끼리 이어진 태그' / '볼트끼리 연결 관계' / '노트 종류별 개수' / '많이 쓴 태그' / '모든 볼트와 노트' / '에이전트 규칙') — 조작법·기능 광고 톤 제거. additions placeholder 3개 ('이 날 ... 없습니다' → '... 없음'). router.js + header.js 페이지 제목 ('커넥션 연결도' → '커넥션', 'AI 룰·스킬 뷰어' → 'AI 룰·스킬'). **트랙 C — 노트/볼트 열기 (5-11 큐 처리)**: `viz/lib/obsidian-uri.js` 신규 — `openNote(vault, path)` / `openVault(vault)` + URI 예약문자 검증 (`#`, `%`, `&`, `?`, `+`) + segment 별 URL 인코딩. additions.js 통합: vault card 전체 클릭 = openVault, note card 우상단 ↗ 버튼 = openNote (single click on row 는 preview 유지), connections row 양쪽 vault chip = openVault. delegated handler (capture phase) 로 row click 보다 먼저 잡음. explorer.js 통합: vault 행 끝 ↗ = openVault (확장/축소 토글 보존), note 행 끝 ↗ = openNote (preview 보존). CSS `.open-obs` 작은 버튼 + `[data-open-vault]` cursor:pointer. **트랙 F — DevFoundation 등록 누락**: viz 노트/볼트 열기 사용 중 DevFoundation 카드 클릭 → "Vault not found". 확인: master 38 vault vs obsidian.json 37 vault, DevFoundation NOT FOUND. 다른 루트 조사 (Local REST API / Obsidian.exe CLI / file:// URL) 모두 viz 브라우저에서 미등록 vault 직접 열기 불가. Obsidian graceful close → `/register-vaults --apply` → 14 신규 vault 일괄 등록 (DevFoundation 외 BasicVaults 5 + ArtInsight + Funding + AI_Coding + Appflowy + Search + Overseas + MachineAssembly + Project_MyVaults). 백업 `obsidian.json.bak_20260515_005854`. Hub 바인딩 부정합 발견 (DevFoundation `hubId: default` 인데 도메인 볼트면 `hubId: domain` 정합) → codex 큐 작성 (`_AGENT_COMMS/to_codex/20260515_claude_codex_DevFoundation_등록누락_점검.md` status: open) — 점검 6항목 (등록 누락 의도 / `/create-vault` 스킬 / codex 룰 / AppFlowy 케이스 / Hub 바인딩 + codex 자가 점검 4 단계 a-d / Template 선택). 본 트랙 등록 시점에 codex 가 동시에 R120 등록 (Codex 새 볼트 생성 안전 룰/스킬 신설 — 사실상 본 트랙 큐의 일부 응답으로 작동) → 본 트랙은 R121 차지. **검증**: `node viz/pages/home.test.js` 9/9 PASS. additions.js syntax check OK. **환경 독립성 § 3 A4 준수**: 절대경로 / vault 이름 박힘 0. **viz/ 정본 변경 — 다음 SellingVault 배포 사이클 반영**. **Phase 2 (다음 트랙)**: network.js / connections.js Sankey / tags.js / distribution.js / calendar.js + Settings 토글 (open_in_obsidian / show_preview_first / copy_path_only). 작업자: Claude (메인) |
| R120 | 2026-05-15 | – | – | **Codex 새 볼트 생성 안전 룰/스킬 신설 — Preset Hub 우선 라우팅 + Obsidian 등록 dry-run + Claude 검토 큐** — DevFoundation 생성 후속 점검에서 `Domains_*` 신규 볼트가 오래된 `BasicContentsVault`/기본 `AIHubVault` 경로로 생성될 수 있는 원인을 Codex 규칙 계층에서 보강. `.codex/skills/create-aimind-vault/` 신규 생성 (`SKILL.md`, `agents/openai.yaml`) — 새 볼트 생성 시 `AIHubVault_Domain`/`AIHubVault_Lab`/`AIHubVault_Project`/`AIHubVault_Diary` 등 Preset Hub 매핑을 먼저 확인하고, CoreHub `clone` 기본 템플릿을 사용하며, 도메인이지만 ZK 구조가 아닌 경우 사용자 확인을 요구. `.codex/rules/create-vault-safety.md` 신규 — `/create-vault`·`vault-individualization` 구문만 보고 오래된 기본 허브로 생성하지 않도록 금지, `register-vaults` dry-run 후 보고, 명시 확인 전 `--apply` 금지, `obsidian.json` 직접 편집 금지, hub-source/index/status 검증 필수화. `.codex/rules/skill-router.md` 새 볼트 생성 행을 신규 스킬+룰로 라우팅하고 `.codex/rules/agent-rules.md` 목록에 등록. Claude 검토 큐 `_AGENT_COMMS/to_claude/20260515_codex_claude_create_vault_rule_skill_review.md` 등록 — Claude 측 `/create-vault`, `vault-individualization`, `_skill-router`, `/register-vaults`, Multi-Hub 문서와의 정합성 검토 요청. Codex 개인 규칙/스킬 및 에이전트 큐 변경으로 Korean/English 배포 무관. 작업자: Codex |
| R119 | 2026-05-14 | 🕓 | 🕓 | **노트 작성 플로우 정교화 Phase 1 — frontmatter 검증 강화 + note-types.yaml 신규 + 작성 스킬 4종 양식 통일** — viz 강화 (R116) 로 frontmatter 가 1차 데이터 소스가 됨에 따라 작성→검증→인덱싱→viz 노출 전 플로우 안전화. 사용자 결정 8건 (보고서 `Project_AIMindVaults/Contents/Project/plan/architecture/20260514_노트_플로우_결정미결_8건_가이드.md`) 중 추천 채택. (1) **CoreHub `data/note-types.yaml` 신규** — 코어 type 25종 (콘텐츠 11 + 이슈 6 + 임시 2 + 구조 2 + 운영 4) 정본. aliases (note→knowledge 등 7건) + 미니 파서로 검증 도구가 yaml 로드 (R114 tag-aliases.yaml 패턴). (2) **CoreHub `post-edit-review.js` 갱신** — `loadNoteTypes()` 헬퍼 + `checkFile` frontmatter 검증 추가 (BAD: no-frontmatter / no-type, WARN: type-not-core / no-created / tags 5-12 / no-wikilink / no-h1). 신규 envVar `POST_EDIT_REVIEW_WARN` + `POST_EDIT_REVIEW_FM_WARN`. WARN 항목은 1개월 후 (6-14) BAD 격상 예정. (3) **bump-version --broadcast** 6/6 Preset Hub 전파 + 6 Preset Hub 모두 수동 bump-version (위성 sync 트리거용). (4) **위성 sync src/ 갱신 chicken-egg 발견** — 위성 cli.js 자체가 옛 코드라 자가 갱신 불가. Preset Hub cli 로 위성 sync 강제 (Project_AIMindVaults 만 본 트랙에서 갱신, 다른 위성은 다음 sync 시 자동). (5) **`.claude/rules/core/_essentials.md § 6` 갱신** — Frontmatter 필수 (`type` + `tags 5-12` + `created`, `updated` 폐지) + type 정본 yaml 참조 + post-edit-review 검증 항목 표 추가. (6) **작성 스킬 4종 양식 통일** — `/juggl-note` (`type: knowledge`, `created` 추가, tags 5-12), `/note-from-article` + `/note-from-pdf` + `/note-from-video` (`type: study-note`, tags 5-12 PascalCase). 기존 `type: note` (juggl) / `type: domain` (note-from-*) 모두 코어 위반이라 정정. (7) **BAD 5건 frontmatter 정상화** — Project_AIMindVaults 의 옛 손상 파일 4건 (한 줄 frontmatter) 멀티라인 + Contents.md 신규. 본문 마크다운 복원은 별 트랙. (8) **검증 결과**: Project_AIMindVaults review POST_EDIT_REVIEW_BAD=0 / WARN=74 / FM_WARN=110 / TAG_VIOLATIONS=0 / OK=1 / INDEX_UPDATED=1. (9) **결정 미결 8건 추천 채택**: 1=B (yaml 정본), 2=B (note→knowledge), 3=A (summary 만 — Phase 2), 4=D (항목별 단계), 5=A+B (스킬 항상 + review 옵션), 6=A (frontmatter agent — Phase 2), 7=B (status 4종 — Phase 2 종속), 8=A (updated 폐지). Phase 2 (summary/status/agent 인덱싱/priority 신규 필드) + Phase 3 (자동화) + Phase 4 (viz 확장) 별 트랙. CoreHub Core 정본 변경 (broadcast 됨) + 루트 `.claude/rules/core` + `.claude/commands/core` 변경 (deploy 대상). 작업자: Claude (메인) |
| R117 | 2026-05-14 | – | – | **DevFoundation 도메인 볼트 신설 — 개발 기초 지식 축적용 Domains_Dev 볼트** — 사용자 요청 ("기존에 개발지식쌓는 볼트는 없었나?" 확인 후 "그럼 그렇게 볼트 만들자"). `Vaults/Domains_Dev/DevFoundation/`를 `BasicContentsVault`에서 CoreHub CLI `clone`으로 생성하고 기본 Hub(`AIHubVault`)에 바인딩. `CLAUDE.md`, `AGENTS.md`, `_STATUS.md`, `_VAULT-INDEX.md`, `_Standards/CONTENTS_SPEC.md`, `Contents/Domain/Domain.md`, `.codex/AGENT_STATUS.md`, `README.md`, `_SESSION_HANDOFF_CODEX.md`를 개발 기초 볼트 범위에 맞게 개별화. 초기 폴더: `CS`, `Network`, `Web`, `Backend`, `Database`, `AuthSecurity`, `CloudInfra`, `SoftwareEngineering`, `Review`. 루트 `AGENTS.md`, `CLAUDE.md`, `.codex/rules/vault-routing.md`, `_STATUS.md` 레지스트리에 DevFoundation 라우팅 추가. with2511 채널 개발 기초 영상 노트 1차 10개 + 2차 12개(총 22개)를 `Contents/Domain/Review/`에 작성하고 `Domain.md` 인덱스 갱신. 사용자 개인 지식 볼트라 배포 무관. 작업자: Codex |
| R116 | 2026-05-13 | 🕓 | 🕓 | **viz 날짜 컨텍스트 4 모드 + 생성일 기준 캘린더 + KPI 카드 라우팅 재배치** — 사용자 요청 ("KPI Connections·Tags 클릭이 노트 페이지로 가는 게 어색, 전용 페이지 필요. 캘린더에 노트/볼트/태그/커넥션 4 뷰 토글. 갱신일 외 생성일 기준 캘린더도 필요"). **Phase 1 — 데이터 인프라**: CoreHub `index-build.js` 갱신 — 노트마다 `fs.statSync.birthtime` + frontmatter `created` 파싱 → `created` 필드 인덱싱. normalizeCreatedField() 헬퍼 (ISO datetime / 공백 datetime / 날짜 only 3 형식 정규화 → `YYYY-MM-DDTHH:MM:SS`). 증분 모드에서 ex.created 없으면 birthtime 으로 backfill (legacy 빌드 호환). bump-version --broadcast 6/6 Preset 전파. 37 vault index 전체 재빌드 OK 37 / FAIL 0. master-index-build 재실행. 검증: 1306/1306 (100%) created 필드 채움. **Phase 2 — server.js API**: `/api/additions?date=&basis=mtime|created` 매개변수 추가 (basis 기본 mtime, created 시 note.created 또는 mtime fallback 으로 일자 필터). 응답에 `created` 필드 포함 + `basis` 에코. `/api/all-notes` 응답에도 `created` 필드 추가. **Phase 3 — additions.js 4 모드 토글**: 한 페이지 내 4 뷰 (notes / vaults / tags / connections) URL `#additions?date=&basis=&view=`. parseQueryFromHash + buildHash 헬퍼. view 토글은 클라이언트 사이드 (재 fetch 없음, suppressHashReload flag). basis 토글은 서버 응답 변경 → 재 fetch. notes view = 기존 (리스트 + 미리보기), vaults view = `/api/vault-births` 그 날짜 birthtime vault 카드 그리드 (카테고리 색상 + path + note_count), tags view = additions notes 의 태그 빈도 집계 (top 40 가로 막대, owner vault chip + owner cat 색상, isOwned 구분), connections view = additions notes × master.tag_owners cross-vault 매칭 (owner ≠ note.vault_id) → "owner vault → tag → user vault × count" 리스트. 카테고리 필터 + 검색 (notes/tags/connections), 카테고리 필터 (vaults). aggregateTags + aggregateConnections 헬퍼 내장. **Phase 4 — calendar.js basis 토글**: 툴바에 [갱신일|생성일] 세그먼트 + URL `#calendar?basis=mtime|created`. aggregateByDate(notes, basis) 매개변수화. 셀 hover/click 텍스트 "갱신/생성" 동적 라벨. 셀 클릭 시 `#additions?date=...&basis=created` 전파. parseBasisFromHash + setBasisInHash 헬퍼. **Phase 5 — home.js KPI 라우팅**: 4 카드 각각 view 매핑 — Vaults→`view=vaults`, Notes→기본 (notes), Connected→`view=connections`, Tags→`view=tags`. URL 빌드 시 view≠notes 만 명시. **Phase 6 — CSS** (`viz/styles/pages.css`): .view-pane / .vault-grid (카드 그리드 minmax 280px) / .vault-card / .tag-bars / .tag-bar (3-cell grid: left chip+title / bar / count) / .tag-bar .bar .fill (owner cat 색상) / .conn-list / .conn-row (owner chip → tag → user chip). 반응형 1024px 분기. **검증**: home.test.js PASS 9/9. additions.js __internal sanity (parseQueryFromHash + buildHash + aggregateTags + aggregateConnections OK). calendar.js __internal sanity (aggregateByDate basis 분기 OK). server API: `/api/additions?date=2026-05-12&basis=mtime` keys=date,basis,count,notes basis=mtime count=14 created 필드 포함. `/api/all-notes` 1306/1306 created 채움. `/api/additions?date=2026-05-13&basis=created` 8 notes (5-13 birthtime). **환경 독립성 § 3 A4 준수**: 모든 신규 코드 절대경로 박힘 0 + vault_id 박힘 0 + 자기 위치 탐지 + 동적 스캔. viz/ 정본 변경 — 다음 SellingVault 배포 사이클에 반영. CoreHub Core 정본 변경 (index-build.js) — broadcast 됨. 작업자: Claude (메인) |
| R115 | 2026-05-13 | 🕓 | 🕓 | **owned_tags 옵션 C 하이브리드 도입 — 볼트별 정본 31 + 루트 집합 + master_index 분리 필드 + KPI 정합 + Track A 사후 보강** — 5-11 단순화 결정 (vault_id 1개 자동 + 정본 위치 없음) 가 사용자 의도 ("사용자 메타인지 + 관리 측면 + 의도 담기") 와 간극. 옵션 C 하이브리드 (1계층 자동 vault_id 매칭 + 2계층 명시 vault 주제 다중) 재결정. **Phase 흐름**: (1) Phase O-1 계획서 § 2.1/§ 2.4/§ 2.5/§ 3 B/§ 4/§ 5 갱신 (옵션 C 결정 반영, 정본 형식 `{vault}/Tags/OWNED_TAGS.md` + 루트 `Tags/OWNED_TAGS.md` + 자동 빌드 + 1태그 1owner 충돌 룰). (2) Phase O-2 6 워커 (W1-W6) 29 vault 주제 분석 + owned 후보 도출 (총 ~316 owned 태그). (3) **Track A 사후 보강**: tag-aliases.yaml `ppe/Ppe → PPE` + `iac/Iac → IaC` alias 추가 + CoreHub broadcast 6/6 Preset + tag-migrate apply (Overseas Ppe 1건 + Git Iac 1건 정규화) + reindex. (4) Phase O-3-b 워커 결과 통합 raw 보고서 `_AGENT_COMMS/to_claude/20260513_Claude_Claude_owned_tags_W1-W6_통합보고서.md` 저장 (룰 자동 적용 결정 + 31 vault 표 통합). (5) Phase O-3-c 31 vault `Tags/OWNED_TAGS.md` 정본 일괄 생성 (W1' 4 + W2' 3 + W3' 5 + W4' 7 + W5' 4 + W6' 8 워커 위임). (6) Phase O-4 CoreHub `owned-tags-build.js` 신규 CLI (~360 LOC) + bin/cli.js 등록 + 단위 테스트 14/14 PASS + dry-run + apply (루트 `Tags/OWNED_TAGS.md` 첫 빌드 317 태그 × 31 vault, 12245 bytes / 239 lines) + broadcast 6/6 Preset (env: `OWNED_TAGS_BUILD_RESULT=OK` / VAULTS=31 / TAGS=317 / CONFLICTS=0). (7) Phase O-5 master-index-build.js 갱신 (코드 변경 + reindex) — 신규 필드 `tag_owners` 317 (자동 31 + 명시 286) / `tag_aliases` 71 (yaml 1회 로드) / `connections` 43 (엄격 정의: owner != null AND 외부 사용 ≥1) / `connection_count` 43 / `owner_conflicts` 0. 옛 `concept_map` 125 호환성 유지. timeseries snapshot 에 `connection_count` 키 추가 (Phase O-5 신규). (8) Phase O-6 viz KPI 정합 — `viz/pages/home.js:164` delta 키 `concept_count` → `connection_count` + `countConnections()` 우선순위 갱신 (master.connection_count 스칼라 → 객체 connections → 옛 concept_map fallback). 이전 KPI 정합 큐 (`20260512_..._viz_KPI_connection_count_정합_누락.md`) 통합 처리. (9) **사용자 결정 사항**: AppFlowy 케이스 (vault_id `Appflowy` vs 태그 `AppFlowy`) = 디렉토리 그대로 + 명시 owned 등록 안 함 (일반 태그 owner null). **환경 독립성 § 3 A4 준수**: 모든 신규 CLI · viz · 정본 파일 절대경로 박힘 0 + vault_id 박힘 0 + 자기 위치 탐지 + 동적 스캔. **결과**: 사용자가 (a) 개별 볼트 `{vault}/Tags/OWNED_TAGS.md` (b) 루트 `Tags/OWNED_TAGS.md` (c) master_index.json `tag_owners` 필드 (d) viz KPI Cross-Vault Connections 카드 + Sankey/network 페이지 — 4 채널에서 owner 태그 확인 가능. 작업자: Claude (메인 + 6 워커 협업) |
| R111 | 2026-05-08 | – | – | **Exercise 도메인 볼트 신설 — 운동·체력관리·트레이닝 지식 도메인 (Domains_Life)** — 시각화 도구 실시간 반영 테스트 목적의 신규 Domain Preset 위성. `Vaults/Domains_Life/Exercise/`를 `BasicDomainVault` 템플릿에서 clone하고 `AIHubVault_Domain`에 바인딩. `CLAUDE.md`, `_STATUS.md`, `_VAULT-INDEX.md`, `README.md`, `Tags/TAGS.md`, `Tags/TAG_MASK.md`를 운동 도메인에 맞게 개별화하고 초기 MOC `Contents/03_MOC/Exercise_MOC.md` 작성. 루트 `CLAUDE.md`·`AGENTS.md` 라우팅 키워드와 `_STATUS.md` 볼트 레지스트리 등록. 사용자 개인 테스트/원본 운영 변경이라 배포 무관. 작업자: Codex |
| R114 | 2026-05-12 | 🕓 | 🕓 | **Phase 5 viz rename + Phase 6 CoreHub post-edit-review.js PascalCase 검증 통합 — 양 에이전트 공용 검증** — Phase 5 (Codex 워커): `viz/pages/concepts.js` → `connections.js` rename + Sankey 3-단 (owner vault → tag → user vault) + network.js 방향성 엣지 (`edgeSymbol: ['none', 'arrow']` 3건 적용) + 홈 KPI "Cross-Vault Concepts" → "Cross-Vault Connections" + 라우터/사이드바/URL hash + `buildOption.test.js`/`loadIndex.test.js` 절대경로 박힘 제거 (`fileURLToPath(import.meta.url)` 동적). 단위 테스트 PASS, 절대경로 잔존 0. Phase 6 (메인, 양 에이전트 공용 검증): `Vaults/BasicVaults/CoreHub/.sync/_tools/cli-node/src/commands/post-edit-review.js` 갱신 — `tag-migrate.js` 의 `normalizeTag` + `parseTagAliases` + `defaultTagAliasesPath` import + `lib/frontmatter.js` parseFrontmatterLight 활용. checkFile 에 frontmatter `tags` 검증 추가: PascalCase 위반 / 메타 태그 / alias 미적용 감지 시 `tagViolations` 배열에 등록. 신규 envVar `POST_EDIT_REVIEW_TAG_VIOLATIONS` 출력. vault_id 동적 추론 (Vaults/** 자동 스캔, 5단계 상승). 환경 독립성 § 3 A4 준수. 검증 결과: Exercise 볼트 review 호출 시 `POST_EDIT_REVIEW_TAG_VIOLATIONS=0`, `POST_EDIT_REVIEW_OK=1` 정상 동작. **양 에이전트 공용 적용** (hook 미사용, CLI 통합으로 환경 독립). Claude/Codex 모두 노트 편집 후 review 호출 시 자동 검증. 본 변경은 CoreHub Core 정본 → `bump-version --broadcast` 로 6 Preset Hub 전파. 작업자: Claude (메인 + Phase 5 Codex 워커) |
| R112 | 2026-05-12 | 🕓 | 🕓 | **`.agents/` 공용 정본 폴더 신설 — AI 에이전트 공용 룰·스킬·도구 단일 위치** — 5-12 사용자 통찰 ("모든 AI 가 공통으로 이 환경을 쓰기 위한 파일은 루트쪽 공용 폴더에 두고") 반영. (1) 신규 `.agents/` 루트 직속 폴더 + 하위 `rules/{core,custom,archive}` + `commands/` + `README.md` (~80 LOC, 구조 설명·정본 권한 단계적 이전·환경 독립성·에이전트별 진입점 참조 관계·hook 특수 사례). (2) 초기 본문 mirror: `.claude/rules/core/` 12 + `.claude/rules/custom/` 3 + `.claude/rules-archive/` 18 = 33 파일을 `.agents/rules/{core,custom,archive}/` 에 copy. 본 단계는 mirror, 정본 권한은 다음 트랙에서 이전 (sync CLI 신설 후). (3) 현재 정본 = `.claude/rules/` 유지 (Claude Code 자동 주입 활성). 다음 트랙 = `.agents/` 가 정본, `.claude/`/`.codex/` 가 sync copy. (4) Codex `.codex/rules/` 의 7 파일 (agent-comms, agent-rules, edit-scope, encoding-safety, skill-router, status-sync, vault-routing) 도 단계적 mirror 이전 검토. (5) hook 은 본질적 에이전트별 (Windows native hook Claude 한정, Codex 비활성) — `.claude/hooks/` 유지, 양 에이전트 공통 검증은 CoreHub CLI 통합 (review 명령 PascalCase 검증) 으로 대체. (6) 본 단계 영향 0 (mirror copy 만, 기존 자동 주입 동작 유지). 다음 트랙: sync CLI (`aimv agents-sync`) + 정본 권한 이전 + 배포 정합 (DEPLOY_TARGETS 갱신). 작업자: Claude (메인) |
| R113 | 2026-05-12 | 🕓 | 🕓 | **Phase 2.9 owned_tags 시스템 도입 + 태그 정규화 Phase 1~4 완료** — 3 워커 병렬 (W1 Codex tag-migrate.js Core CLI + 단위 테스트 33/33 PASS + 489+297 LOC / W2 Codex 26 CLAUDE.md "필수 태그" 규칙 일괄 제거, 51 deletions / W3 Codex 37 vault × 74 파일 archive `git mv`) + 메인 통합. 메인 보강: tag-migrate.js `normalizeTag` 룰 1.5 추가 (nested `/` PascalCase 결합 — `unity/dots` → `UnityDOTS`) + `normalizeInlineTag` 통합 + tag-aliases.yaml 신규 11건 (1차 5: zk/usa/nodejs/agentforge/ci-cd, 잔존 patch 6: URP/Urp/KPI/Kpi/Dayloop/DayLoop) + 단위 테스트 38/38 PASS. 메인 apply: 37 vault / 1371 노트 / Changed 1011 / FmTagChanges 3301 / FmTagRemoved 70 (메타 5종 일괄 제거: domain 34 + Domain 4 + Meta 13 + meta 1 + (볼트 태그) 18) / InlineChanges 23 / Dedup 1 / Ambiguous 0. 메인 reindex (37 vault × vault_index_build + master-index-build FULL): Tags 1208 → 1109 (-99 메타+대소문자+nested 통합), Concepts 115 → 125 (+10 의미 명확화), 대소문자 충돌 61 → 0 (잔존 1: `Software3.0` Karpathy AI coding paradigm 점 포함 그대로 유지). 환경 독립성 § 3 A4 자체 점검 통과 (`__dirname` 5단계 상승 + `Vaults/**` 동적 스캔 + master_index 동적 추출, 절대경로·사용자명·디바이스명·vault 이름·32 볼트 리스트 박힘 0). viz 2모드 신규 큐 등록 (`_AGENT_COMMS/to_claude/20260512_..._viz_2모드_그래프_cross_vault_wikilinks.md` — cross-vault wikilinks 데이터 모델 + Mode 1/2 토글, Phase 2.9 완료 후 진입). CoreHub Core 정본 변경: `tag-migrate.js` + `tag-migrate.test.js` + `data/tag-aliases.yaml` 신규 + `bin/cli.js` tag-migrate 명령 등록. `bump-version --broadcast` 로 6 Preset Hub 전파 대상. **다음**: Phase 5 viz rename + Phase 6 `_essentials.md § 6` 재작성 + Phase 7 환경 독립성 + GUI 검증. 작업자: Claude (메인 + 3 워커 협업) |
| R110 | 2026-05-07 | – | – | **Git 운영 정책 명문화 — 단독 main 브랜치, 워크트리 격리 미사용** — 사용자 단독 운영이라 브랜치 분리 불필요 (사용자 결정). (1) `CLAUDE.md` `## Git 운영 (단독 운영, R110)` 섹션 신규 — 모든 작업 `main` 브랜치 직접, Claude Code/에이전트 자동 워크트리 격리(`Agent` 도구 `isolation: "worktree"`) **사용 금지**, 기존 `claude/*` 임시 브랜치 + `.claude/worktrees/*` 잔재 발견 시 정리, 예외는 사용자 명시적 위험 격리 시만 + 종료 시 즉시 삭제. (2) 본 세션 정리: `claude/great-hodgkin-091bae` 브랜치 삭제 + `git worktree` 메타 정리 (디렉토리 잔재는 다른 프로세스 락 — 별도 정리 필요). (3) `claude/upbeat-wright-92b4bc` (본 세션 cwd) 는 세션 종료 후 다음 메인 세션에서 정리. (4) 사용자 운영 정책 — Korean/English 배포 무관. 작업자: Claude (메인) |
| R109 | 2026-05-07 | 🕓 | 🕓 | **시각화 도구 Phase 2 + Phase 2.5 일괄 완성 — `viz/` 10 페이지 + 라우터 + 공통 컴포넌트 + CSS 토큰 + 신규 server endpoints + 다중 에이전트 룰뷰어** — Phase 1 spec 1 워커 + Phase 2 5 워커 + Phase 2.5 5 워커 + Phase 2.5-Phase2 W-cal/W-add 2 워커 = 13 워커 협업 산출. (1) `viz/lib/` (loadIndex 145 LOC + buildOption 435 LOC + markdown 70 LOC + 표 파서 추가 + 테스트 297 LOC). (2) `viz/server.js` (~330 LOC — Node.js 내장 http + SSE + fs.watch + /api/rules·note·activity·all-notes·additions + Codex 디렉토리 통합 스캔 + path traversal 방어 + safeJoin). (3) `viz/index.html` + `router.js` (~250 LOC — vanilla JS hashchange + dynamic import + EventSource + 페이지 init/destroy + currentPage.refresh + UserConfig localStorage). (4) `viz/components/` (header.js + side-panel.js + theme.js + crumb chain — additions parent=calendar 매핑). (5) `viz/styles/` (tokens.css 양 테마 + base.css + components.css 모든 공통 컴포넌트 + GFM 표 + Settings 시안 09 톤 + .preview .pbody 마크다운 위계 + .main grid + chart-wrap height + .tree-wrap + pages.css). (6) `viz/pages/` 10 페이지 (home·concepts·network·distribution·tags·explorer·rules·settings·calendar·additions, 총 ~2700 LOC). (7) **차트 동작 표준** force.layoutAnimation:false + draggable:true + roam:true + 응집력·척력·엣지 길이 슬라이더. (8) **자동 갱신** 진짜 실시간 (SSE data-changed → currentPage.refresh, F5 불필요). (9) **다중 에이전트 룰뷰어** `.claude/` + `.codex/` 통합 + agent 그룹 트리 + kind 색상 (core/custom/hook). (10) **캘린더 + 날짜별 추가 항목** GitHub-style heatmap (LOOKBACK 6 + custom input 7~36500일) + visualMap 5단계 그라디언트 + theme MutationObserver + 셀 클릭 → #additions?date=YYYY-MM-DD. (11) **Settings** 색상 CRUD + 두께 multiplier + 표시 토글 + 테마 + export/import + 리셋 + danger zone. (12) `viz_design_drafts/` 9 시안 (마케팅 캡처용 보존). (13) Phase 2.5 디자인 시스템 spec + 통합검증·Obsidian 연동 계획서 + 디자인 다듬기 계획서 신규 (`Project_AIMindVaults/Contents/Project/spec·plan`). (14) 폐기: viz/style.css 단일 + viz/viz.js 700 LOC. **DEPLOY_TARGETS viz/ 등록 (Phase 2 W5)** — 다음 `bump-version --broadcast` 시 6 Preset Hub 전파 + Korean SellingVault 반영. **알려진 trailing**: viz/nul R069 재발 (워커 spawn hook 상속 미보장 — 별도 트랙). 작업자: Claude (메인 + 13 워커 협업) |
| R108 | 2026-05-06 | – | – | **`_AGENT_COMMS/multi-worker-protocol.md` 신규 — 오케스트레이터 ↔ 다중 워커 워크프레임 (멀티 에이전트 허브 표준)** — 시각화 트랙 Phase 2 6 워커 분할 시점 + 사용자 의지 ("멀티 에이전트 허브로 확실하게 써먹자고") 로 1:N 다중 워커 패턴 정식 룰화. 기존 `_AGENT_COMMS/README.md` 의 1:1 통신 규약 (Claude ↔ Codex 단발 검토·교차검증) 위에 작동. (1) `_AGENT_COMMS/multi-worker-protocol.md` 신규 (~280 LOC) — § 1 패턴 개요 (1:1 vs 1:N) + § 2 역할 정의 (메인 오케스트레이터 / 워커) + § 3 워커 큐 작성 규약 (자기완결 콜드 스타트 진입자용 필수 섹션) + § 4 진행 보고 (큐 파일 § 진행 로그, 단계별 1줄, status 흐름) + § 5 양방향 통신 (워커 § 질문 ↔ 메인 § 답변, status: in-progress 유지) + § 6 위반 피드백 (fix 큐 발행 패턴) + § 7 인터페이스 명세 = 단일 진리원 (Phase 1 워커 산출 → Phase 2 N 워커 의존) + § 8 검증 책임 (메인 = 관리감독 통합, 별도 W6 워커 비권장) + § 9 spawn 메커니즘 (`/spawn-claude` 권장, 워커 자율 spawn 금지) + § 10 라이프사이클 (open → in-progress → 진행 로그 → 질문/답변 → resolved → 완료 트리거 → 검증 → 통과/fix 큐 → archive) + § 11 사용 시점 (큰 작업/분할 가능/인터페이스 명세 가능/병렬성 확보) + § 12 금지 (워커 자율 spawn / 워커 직접 통신 / 인터페이스 명세 없이 동시 spawn / 메인 검증 스킵 / 별도 W6 / fix 큐 3건+ 누적 시 메인 직접) + § 13 시각화 트랙 사용 예시 + § 14 참조. (2) `_AGENT_COMMS/README.md` § 다중 워커 패턴 신규 섹션 추가 + § 참조에 multi-worker-protocol.md 링크 1줄. (3) `.claude/rules/custom/agent-ownership.md` § _AGENT_COMMS § 상세 규약 — 1:1 룰 README + 1:N 룰 multi-worker-protocol 양분 명시. **배포 무관** — _AGENT_COMMS/ 자체가 배포 미대상 (README 첫 줄), agent-ownership.md 도 custom/ 영역. 본 R 항목은 멀티 에이전트 허브 인프라 영구 자산. 작업자: Claude (메인) |
| R107 | 2026-05-05 | – | – | **Codex Serena MCP 전역 자동 실행 제거 + Unity 조건부 호출 규칙 보강** — 원인: `C:\Users\c\.codex\config.toml`의 `[mcp_servers.serena]` 전역 등록 때문에 AIMindVaults 루트/노트 작업 등 비-Unity Codex 세션에서도 `uvx -> uv -> serena -> python` 체인이 자동 기동했고, 여러 Codex 세션이 열리면서 중복 Serena 프로세스가 누적됨. 조치: (1) 사용자 전역 Codex config에서 `[mcp_servers.serena]` 블록 제거하고 주석으로 lazy-load 정책 명시. (2) 루트 `AGENTS.md`의 Serena 섹션을 "항상 연결"에서 "Unity C# 스크립팅 작업 시에만 tool discovery로 호출"로 수정. (3) `.codex/rules/skill-router.md`에 Serena MCP 조건부 호출 섹션 추가 — Unity/C# 트리거에서만 `tool_search`로 Serena 노출, 이후 `activate_project`, 비-Unity 작업에서는 호출 금지. 사용자 로컬 Codex 런타임·개인 운영 규칙 변경이므로 배포 대상 아님. 작업자: Codex |
| R106 | 2026-05-03 | 🕓 | 🕓 | **Phase 1 hooks 4종 신설 — 셀프 #1 권장 #1~#4 일괄 처리 (병렬 4 워커 위임)** — 4-24 셀프 메모 (`20260424_Claude_Claude_벌크등록_완료후_훅_구현_제안.md`) 의 권장 #1~#4 미처리 4건을 5-03 메인 위임으로 4 워커 병렬 spawn → 각 hook 파일 생성 + 자기검증 + 완료 트리거. (1) **`block_uri_reserved_chars.py`** (PreToolUse Write\|Edit) — 파일명 URI 예약문자 (`#`, `%`, `&`, `?`, `+`) 차단, `_essentials.md § 6` 룰 강제. (2) **`post_edit_review_advisory.py`** (PostToolUse Write\|Edit) — `Vaults/<볼트>/Contents/**/*.md` 편집 후 stderr advisory, `_essentials.md § 5` 룰 환기. 백그라운드 실행 X (사용자 대기 부담 회피). (3) **`block_reserved_filenames.py`** (PreToolUse Write\|Edit) — Windows 예약 이름 (NUL/CON/AUX/PRN/COM1-9/LPT1-9 + 확장자 변형) 파일 생성 차단, `shell-redirect-safety.md § 9` 룰 강제. R069~R072 Bash 차단의 Write 도구 보강. 워커 보강: `hookSpecificOutput.permissionDecision` 새 형식 + exit 2 (Claude Code 공식 권장). 메인 추가 fix: stderr UTF-8 reconfigure (cp949 콘솔 깨짐 방지). (4) **`check_script_creation.py`** (PreToolUse Write 만) — `.ps1`/`.py`/`.bat`/`.sh`/`.cmd`/`.psm1`/`.zsh`/`.fish` 신규 스크립트 생성 시 stderr advisory (목적·경로·영향·일회성·승인 4항목 요청), `script-creation-approval.md` 룰 환기. 면제 경로: `.claude/hooks/`, `_tools/cli-node/`, `.sync/_tools/`. (5) **`.claude/settings.json` 일괄 등록** (메인 영역, 4 워커 race 회피) — `hooks.PreToolUse` 에 Write\|Edit matcher entry (block_uri + block_reserved 묶음) + Write matcher entry (check_script_creation), `hooks.PostToolUse` 신설 + Write\|Edit matcher entry (post_edit_review_advisory). 기존 entry (Bash matcher block_nul + 모든 도구 log_tool_use, Stop check_status, Notification log_permission) 유지. (6) **R105 검증 표준 (Phase 4 raw output + Phase 5 spot-check) 적용 첫 케이스** — 4 워커 모두 위임 큐 § Phase 4 검증 raw output 보고 + 메인 spot-check 1줄로 검증 종료 (이전 패턴 4× 직접 검증 대비 토큰 절약 검증). 셀프 #1 status: in-progress → resolved + 응답 섹션 4 hook 처리 완료 보강 + archive 이동. 작업자: Claude (4 병렬 워커 + 5-03 메인 위임) |
| R105 | 2026-05-03 | – | – | **`/delegate-task` 스킬 § 검증 표준 (강제) 신규 섹션 추가 — Phase 4 raw output + Phase 5 spot-check** — 본 세션 5-02 정리 워커 검증 사례에서 발견 (메인 직접 검증 4번 vs 보고서 신뢰 + spot-check 1번 = 토큰 60~70% 절약). (1) `### 1. Phase 4 검증 형식 강제 (위임 큐 본문)` — 검증 명령 + 기대값 (PASS/FAIL 마커) + raw output 보고 의무. (2) `### 2. Phase 5 — 메인 spot-check 권장 명령 강제 (완료 트리거 본문)` — 1줄 명령으로 메인 빠른 검증. (3) § 효과 + § 강제 적용 범위 명시. 위치: ## 사용 예시 직전. 기존 5단계 워크플로우 + 사용 예시 보존. 본 세션 트랙 B 1단계 워커 작업이 검증 표준 적용 첫 케이스 (위임 큐 § Phase 4 검증 + § 메인 spot-check 권장 명령 포함, 완료 트리거 § 검증 결과 + § 메인 spot-check 권장 포함). custom 영역 스킬 — Korean/English 배포 무관. 작업자: Claude (워커, 5-02 메인 위임) |
| R104 | 2026-05-03 | – | – | **`agent-ownership.md` § 완료 트리거 처리 흐름 (강제) 신규 섹션 추가 — 5단계 룰** — _AGENT_COMMS 누적 원인 (완료 트리거 archive 시점 불명확) 차단. 기존 § 완료 트리거 체인 본문 보존 + 직후 신규 섹션 (§ 금지 직전). 5단계: (1) 워커 트리거 파일 생성 (open). (2) 메인/다음 진입 에이전트 발견 → in-progress (후속 처리 시작). (3) 후속 처리 완료 → resolved + 응답 섹션. (4) **후속 큐가 별도 파일로 발행되면 트리거 파일 즉시 archive 이동** (7일 안 기다림). (5) 7일 이상 open 잔존 = 후속 누락 신호 → Stop hook (R103) advisory 트리거. 효과: `_완료.md` 패턴 자체 누적 차단 + 누락 catch-up 메커니즘. custom 영역 룰 — Korean/English 배포 무관. 작업자: Claude (워커, 5-02 메인 위임) |
| R103 | 2026-05-03 | 🕓 | 🕓 | **Stop hook 확장 — `_AGENT_COMMS/to_claude/` 스캔 advisory 추가 (in-progress + 7일 stale resolved)** — R091 hook 의 _AGENT_COMMS 정리 누락 catch-up 메커니즘. (1) `.claude/hooks/check_status_on_stop.py` 확장 — `parse_frontmatter()` · `scan_agent_comms()` · `emit_agent_comms_advisory()` · `emit_git_advisory()` 함수 분리. `_AGENT_COMMS/to_claude/` 디렉토리 순회 + frontmatter 정규식 파싱 (yaml 의존 X) + status 분류. `status: in-progress` 잔존 → 처리 중 큐 N건 advisory + 첫 3건. `status: resolved` + (`resolved`/`updated`/`created` fallback) 7일 경과 → 정리 대상 큐 N건 archive 권장 + 첫 3건. 자동 archive 이동 절대 X (in-progress 오인 위험) — advisory 만. (2) Windows 환경 보강 (스펙 외 추가): `sys.stderr.reconfigure(encoding="utf-8")` cp949 콘솔 한글 깨짐 방지 + `subprocess.run(encoding="utf-8", errors="replace")` git diff Korean 파일명 UnicodeDecodeError 방지. (3) `CLAUDE_PROJECT_DIR` 미설정 시 cwd fallback (dry-run 지원). 트랙 B 1단계 워커 spawn (`_AGENT_COMMS/to_claude/20260502_Claude_Claude_루틴개선_1단계_위임.md`) 으로 처리. dry-run 검증 통과 — in-progress 1건 advisory 출력. 작업자: Claude (워커, 5-02 메인 위임) |
| R102 | 2026-05-02 | – | – | **`settings.local.json` 권한 보강 12개** — `Bash(node\|git\|grep\|cat\|echo\|head\|tail\|ls\|wc\|mv) *` 와일드카드 10 + `Edit/Write(_AGENT_COMMS/**)` 도구 path 패턴 2. 4-29 권한 분석 워커 (`20260429_Claude_Claude_권한요청_분석_완료.md`) 추천 패턴 적용. 워커 5건 prompt 100% 커버 (node/git/awk) + 일반 read-only 빈출 (grep/cat/echo/head/tail/ls/wc) + 본 세션 archive 이동 14건 mv 자동 등록 일반화 + _AGENT_COMMS 큐 갱신·생성 패턴. `Bash(python *)`, `Bash(awk *)` 이미 등록 (추가 X). `Bash(find *)` 사용자 결정 보류. 4-29 권한분석_완료 큐 status: open → resolved + 응답 섹션 추가. settings.local.json 은 .gitignore + 디바이스별 분리 (R093 패턴) — Korean/English 배포 무관. 작업자: Claude (5-02 메인 세션) |
| R101 | 2026-04-30 | – | – | **Overseas 도메인 볼트 신설 — 해외 출국·체류 도메인 (Domains_Life)** — 사용자 케이스: 2026 미국 ESTA 3개월 공장 정밀조립 파견 출국 준비. **초기 시도** (Project Preset / `Projects_Personal/OverseasPrep`) 후 사용자 정정 (대형 프로젝트 아님 → PARA·9타입 과한 구조) → **Domain Preset 으로 재구축**. (1) 초기 OverseasPrep 폴더 (Vaults/Projects_Personal/OverseasPrep, hubId=project) 삭제. (2) `node cli.js clone` BasicDomainVault → `Vaults/Domains_Life/Overseas/` (363 파일 복사, hubId=domain, AIHubVault_Domain 바인딩). (3) `CLAUDE.md` 개별화: 도메인 역할 (해외 출국·체류 노하우 도메인 누적), 수집 범위 (비자·항공·짐·PPE·숙소·통신·환율·보험·출국 절차·체류 적응), 수집 안 하는 것 (관광·민감 계약·일기·프로젝트 진행), ZK 5 타입 폴더 구조 (00_Inbox/01_Literature/02_Permanent/03_Synthesis/04_MOC/05_Pattern/_AI_Drafts/_archive), 활성 케이스 Case_USA_2026 명시, 태그 규칙 (ZK 5 타입 + 운영 단계 + 도메인 보조 `visa/flight/luggage/ppe/housing/telecom/fx/insurance/customs` + 케이스 식별 `case-usa-2026`). (4) `_STATUS.md` 초기화: Now (볼트 초기화·USA_2026 케이스 활성·초기 노트 4건 진행), Next (USA_2026 MOC·회사 답변 노트화·항공편 비교 결과·귀국 후 패턴 추출), Decisions (볼트명·카테고리·ZK 단순 구조·첫 케이스). (5) 루트 `_STATUS.md` Projects_Personal 의 OverseasPrep 행 제거 + Domains_Life 섹션에 Overseas 행 추가 (Cooking 옆). (6) 루트 `CLAUDE.md` 키워드 매핑 OverseasPrep → Overseas 변경 (해외 출국·출국 준비·미국 파견·ESTA·비자·항공편·짐 패킹·PPE·출장·해외 체류). **사용자 케이스 자산이라 배포 컬럼 `–` 처리** (R084 distribution-content-safety 정합 — 본문에 사용자 환경 인용은 _ROOT_VERSION 변경 로그 한정 OK, SellingVault 배포 제외). 다음: 인덱스 빌드 + USA_2026 케이스 MOC + 비자/항공/짐/PPE 노트 4건 작성. 작업자: Claude |
| R100 | 2026-04-29 | 🕓 | 🕓 | **원본 환경 정리 — bkit/pdca 흔적 일괄 제거 + 사용자 환경 종속 도메인 트리거 일반화** — 사용자 명시: bkit·grok-route 안 쓰는 기능, 도메인 스킬 (Unity/Blender/Meshy/Notion/Discord) 은 본인 개인 자산이라 다른 사용자 배포 무의미. **정리 1 — bkit/pdca 흔적 100% 제거** (원본만, SellingVault 워커 처리): `.bkit/` 26 폴더 + `.pdca-snapshots/` 16 폴더 + `.bkit-memory.json` 40+ 파일 + `.pdca-status.json` 40+ 파일 + `.docs/` 44 빈 폴더 (bkit-memory + pdca-status 페어 격납소). `find -name` + 상대경로 (settings.local.json `Bash(rm -rf C:*)` deny 우회) + 검증 0건. **정리 2 — `/grok-route` 제거**: `.claude/commands/core/grok-route.md` 삭제 + `MANIFEST.md` core 테이블에서 grok-route.md 행 제거. 트리거 매핑은 `_skill-router.md` 에 원래부터 없었음. **정리 3 — `_skill-router.md` 도메인 트리거 5행 제거** (Unity/Blender/Meshy/Discord/Notion) + Phase 2-A 매핑 표 일반화 (도메인 Skill 행 6→1, `/distribute` 만 유지 + "사용자 본인 환경에서 도메인 Skill 추가 시 트리거 매핑 직접 등록" 안내). 도메인 스킬 파일 자체 (`custom/discord-admin.md`, `custom/blender-workflow.md`, `custom/meshy-workflow.md`, `custom/notion-record.md`, `custom/unity-dev.md`, `custom/discord-manage.md`) 는 사용자 본인 환경 유지 (custom/ 배포 무관). 효과: 배포본 새 사용자가 받는 `_skill-router.md` 가 일반 인프라 트리거만 — 작성자 환경 종속 트리거 노출 0. R084 (배포 콘텐츠 안전) 정합 강화. 작업자: Claude (오케스트레이터) |
| R099 | 2026-04-29 | – | – | **영문 SellingVault Phase 3 부분 영문화 + Phase 5 deploy --english-mode live 검증 + Phase 6 R 번호 마커 일괄 갱신 (4-29 워커, 옵션 3)** — 옵션 3 선택 (사용자 결정) — Phase 3 최소 6 파일 영문화 → Phase 5 deploy --english-mode live → Phase 6 R069~R098 ✅ 갱신 → BasicVaults 697 self-memo 분할. (1) **Phase 3 영문화 6 파일** (C:/SellingVault/English/AIMindVaults/ 직접 편집, R093 deny `Edit(C:/SellingVault/**)` 미작동 사실 활용): `_ROOT_VERSION.md` (4 줄 한글 wikilink/file path 영문화), `docs/architecture.md` · `cli-reference.md` · `customization.md` (전면 영문화), `_tools/cli/sync_workspace.ps1` · `vault_index_build.ps1` (헤더 docstring 영문화). 본문 인라인 한글 주석은 미처리 (레거시 PS1 — Node.js CLI 마이그레이션 후 미사용). (2) **Phase 5 R094 검증 통과**: `deploy --english-mode --dry-run` → 2883 copied 신규만 / 0 deleted / 3052 unchanged 보호 / 9 skipped → live 동일 결과. R094 protectExisting 메커니즘 영문 번역본 손실 0 검증 완료. English SellingVault commit 7049543 → push origin main. (3) **Phase 6 마커 갱신**: R069~R098 중 Korean 또는 English 가 🕓 인 항목 일괄 ✅ 처리 (R094~R098 양쪽 + R092·R091·R088·R086·R085·R084·R083·R081·R080·R079 English). `–` 항목은 미변경 (R078·R082·R087·R089·R090·R093·R078 등 creator-only). (4) **BasicVaults 697 self-memo 큐**: 다음 워커가 분할 처리하도록 트리거 큐 작성 (BasicContentsVault 1 차 영문화 → diff → 다른 sub-vault 일괄 가능성). 자동 모드 검증 결과: `skipAutoPermissionPrompt: true` ✅, `Edit(C:/SellingVault/**)` deny ❌ 미작동 (R094 패턴 매칭 이슈 추정 — 후속 조사 필요). 권한 분석 큐 별도 spawn 처리. 본 R 항목은 마커 갱신 (메타 작업) 이라 `– –`. 작업자: Claude (워커, 4-29 spawn) |
| R098 | 2026-04-29 | ✅ | ✅ | **PreToolUse 모든 도구 hook 추가 + Notification payload 보강 (정밀 추적)** — R096 hook 첫 4 건 누적 결과 Notification payload 가 `message` 만 포함 → 도구명만 식별 가능, 명령·경로 미포함. 정확한 명령 추적 위해 PreToolUse 모든 도구 hook 추가. (1) `.claude/hooks/log_tool_use.py` 신규 — PreToolUse hook (matcher 없이 모든 도구). tool_name + tool_input (command, file_path, url, pattern, path, edits, content) 전체 dump → `$env:TEMP/aimv_tool_use_log.jsonl`. 큰 content (500자 초과) truncate. 항상 exit 0 + 빈 JSON pass-through (차단 X). (2) `log_permission_request.py` 보강 — `_payload_keys` (스키마 발견용 디버그 필드) + `tool_name_from_msg` (정규식 fallback `permission to use <tool>` 추출). (3) `.claude/settings.json` `hooks.PreToolUse` 에 신규 entry 추가 (matcher 없이) — 기존 `matcher: "Bash"` block_nul_redirection 와 병행. **매칭 분석**: permission log 의 Notification record + 같은 session_id 의 가장 최근 tool_use record → 어떤 명령이 prompt 받았는지 역추적. analyze 스크립트 보강은 후속 (사용자 결정). 사용자 의도: "확실하게 추적해서 풀 권한 vs 정밀 제어 결정". 작업자: Claude |
| R097 | 2026-04-29 | ✅ | ✅ | **권한 요청 분석 스크립트 신설 (`analyze_permission_log.py`) + 분석 워커 트리거 큐** — R096 Notification hook 으로 누적된 JSONL 로그를 빈도 분석하고 settings.local.json 보강 패턴 추천하는 도구. (1) `.claude/hooks/analyze_permission_log.py` 신규 — argparse 기반 CLI: `--threshold`, `--top`, `--format text/json/allow`, `--log <path>`. 출력: tool_name 빈도 + Bash command prefix 빈도 + Edit/Write/Read file_path prefix 빈도 + 추천 allow 패턴 (`Bash(<prefix> *)` 형식). (2) `_AGENT_COMMS/to_claude/20260429_Claude_Claude_권한요청_분석_이어작업.md` 신규 — 영문 점검 워커 작업 완료 후 다음 워커가 진행할 트리거 큐. 5 Phase (분석 실행 → 결과 보고 → settings.local.json 보강 제안 → 검증 옵션 → 트리거 체인). 자가 보고 (본 워커 자기 권한 요청 분석) 옵션. **사용자 의도** ("워커가 자기가 발생한 권한 요청도 기록하도록 + 새 워커가 겸해서 진행"): 분석 스크립트 + 트리거 큐 도입으로 자동화 + 영문 점검 후속 워커가 권한 분석 겸행 가능. analyze_permission_log.py 는 `.claude/hooks/` 배포 대상이라 다른 사용자도 본인 환경에서 활용 가능. 큐 파일 자체는 _AGENT_COMMS/ 배포 미대상. 작업자: Claude |
| R096 | 2026-04-29 | ✅ | ✅ | **Notification hook 신설 — 권한 요청 패턴 데이터 수집 (`log_permission_request.py`)** — 사용자 발견 (4-29): settings.local.json `skipAutoPermissionPrompt: true` + 누적 specific allow 패턴에도 불구하고 새 spawn 워커가 권한 prompt 다발 → 패턴 식별·allowlist 보강 자동화 메커니즘 필요. 조치: (1) `.claude/hooks/log_permission_request.py` 신규 — Notification hook 으로 권한 요청 payload (tool_name, tool_input.command/file_path/url, session_id, hook_event, message) 추출 + JSONL append. 로그 위치 `$env:TEMP/aimv_permission_log.jsonl` (per-device, git 미동기). 실패 silent (prompt 차단 X). (2) `.claude/settings.json` `hooks.Notification` 항목 신규 등록 (matcher 없이 모든 Notification 이벤트). 기존 hooks.PreToolUse.Bash (R073 block_nul_redirection) + hooks.Stop (R091 check_status_on_stop) 와 병행. **분석 워크플로우**: ① 일정 기간 작업 누적 → ② JSONL 파싱하여 빈도 분석 → ③ 고빈도 패턴을 settings.local.json `permissions.allow` 에 추가 → ④ prompt 발생률 감소 검증. `/fewer-permission-prompts` 스킬 활용 가능 (transcript 기반 자동 추출). 작업자: Claude |
| R095 | 2026-04-29 | ✅ | ✅ | **MANIFEST.md (commands + rules) custom 섹션 일반화 — R084 (배포 콘텐츠 안전) 위반 정정** — 사용자 발견 (4-29): `.claude/commands/MANIFEST.md` 의 `## custom/ — 사용자 스킬 (동기화 미대상)` 섹션 본문에 `discord-manage.md` (Discord 봇 운영, AIMindVaults Admin Bot 사용자 전용) + `spawn-claude.md` (사용자 디바이스 환경 종속) 구체 파일명 인용. MANIFEST.md 가 `DEPLOY_TARGETS` 에 포함 (`{ type: 'file', path: '.claude/commands/MANIFEST.md' }`) → SellingVault 배포 시 새 사용자가 받는 매니페스트에 작성자 개인 자산 (Discord 봇 등) 노출. R084 룰 (배포 대상 문서에 사용자 개인 볼트명·카테고리·자산 인용 금지) 위반. 4-25 R084 도입 시 일괄 점검 누락분. 조치: (1) `.claude/commands/MANIFEST.md` custom 섹션 — 구체 파일명 표 제거 → 카테고리 패턴 안내 (외부 서비스 봇, CLI spawn, 도메인 워크플로우, 노트 변환, 디바이스 셋업 등) + 신규 추가 시 `_skill-router.md` 트리거 매핑 안내. (2) `.claude/rules/MANIFEST.md` Phase 2-A 변경 이력 (`blender-mcp, notion-sync, serena-mcp, unity-scripting-style, unity-tools, discord-bot` → `/blender-workflow, /notion-record, /unity-dev, /discord-admin` 매핑) 일반화 — 도메인명 제거 + "도메인별 트리거 매핑은 `_skill-router.md` 참조" 안내. archive Phase 2-A 표는 일반 인프라 (`distribution-deploy`, `sync-version-priority`) 만 유지, 도메인 6행 제거. SellingVault 배포본 정리 다음 deploy 사이클에 자동 반영. 작업자: Claude |
| R094 | 2026-04-29 | ✅ | ✅ | **`deploy --english-mode` 옵션 신설 — 영문 번역본 보호 메커니즘 (CoreHub broadcast)** — 4-29 영문 점검 워커가 발견한 구조적 충돌 (fs-mirror.js `shouldCopyFile` 의 `size !== size \|\| mtime > mtime` OR 결합 → 한국어 source vs 영문 번역본 target size 항상 다름 → 영문 번역본 ~3006 손실 위험) 해결. (1) `Vaults/BasicVaults/CoreHub/.sync/_tools/cli-node/src/lib/fs-mirror.js` — `mirrorDirectory()` 옵션에 `protectExisting=false` 추가, `syncDir()` 시그니처 확장, `shouldCopyFile()` 에 `protectExisting` 파라미터 + `if (protectExisting) return false` 분기. protectExisting 활성 시 pruning 도 비활성 (영문 번역본 삭제 방지). (2) `src/commands/deploy-dist.js` — `deployDist()` 옵션에 `englishMode=false` 추가, main loop 의 `deployDir/deployFile` 호출에 `englishMode` 전달, `deployDir` 의 `mirrorDirectory` 호출에 `protectExisting: englishMode` 전달, `deployFile` 에 englishMode + target 존재 시 skip 로직 추가. 활성 시 `[ENGLISH-MODE]` 로그 출력. (3) `bin/cli.js` deploy 명령에 `--english-mode` 플래그 추가. dry-run 검증 통과 (English deploy: 2884 copied 신규만 / 0 deleted / 3050 unchanged 보호 / 9 skipped). CoreHub `bump-version --broadcast` 로 6/6 Preset Hub 에 102 items 전파 완료. 향후 영문 SellingVault deploy 는 `--english-mode` 명시. 작업자: Claude |
| R093 | 2026-04-29 | – | – | **사용자 디바이스 권한 정책 강화 — `.claude/settings.local.json` `skipAutoPermissionPrompt: true` + 정교한 deny 패턴** — 사용자 직접 편집 (4-29). spawn 워커 권한 prompt 누적 문제 해결. (1) `skipAutoPermissionPrompt: true` — 모든 권한 prompt 자동 통과 (Auto mode 영구 활성화, 새 spawn 인스턴스도 자동 상속). (2) `permissions.deny` 정교화 — `Bash(rm -rf /*)`, `rm -rf ~*`, `rm -rf $HOME*`, `rm -rf C:*`, `Remove-Item -Recurse -Force C:*`, `git push --force*`, `git push -f *`, `git push --force-with-lease*`, `Write(C:/SellingVault/**)`, `Edit(C:/SellingVault/**)` (Win/Unix 양쪽 path separator). 특히 SellingVault Write/Edit deny 는 `distribution-sync.md` 룰 ("Claude 직접 SellingVault 편집 X") 을 deterministic 강제. (3) 누적 specific allow 패턴은 그대로 유지 (작업 중 자동 등록 결과). settings.local.json 은 `.gitignore` 처리 (git 미추적, 디바이스별 분리). 노트북 ↔ 원본 PC 권한 정책 독립. 작업자: 사용자 |
| R092 | 2026-04-29 | ✅ | ✅ | **`/delegate-task` 스킬 신설 — 에이전트 작업 위임 워크플로우 SOP** — `/spawn-claude` (CLI spawn 메커니즘) 위에 위임 워크플로우 전체 (큐 작성 → spawn → 트리거 → 추적 → 완료 처리) 를 표준 SOP 로 묶음. 4-29 영문 배포본 점검 위임 사례에서 패턴 검증 → 사용자 ("이거 잘 응용하면 에이전트 협업 환경 거진 완성") 의 협업 인프라 구상 코어. (1) `.claude/commands/custom/delegate-task.md` 신규 — 트리거 키워드 (위임/떠넘겨/백그라운드/워커/delegate/병렬·동시 작업), 위임 적합·부적합 작업 유형 구분, 5단계 워크플로우 (큐 frontmatter + 콜드 스타트 자기완결 본문 → `/spawn-claude` 활용 + 방식 1·2·3 + 병렬 다중 spawn → 트리거 메시지 패턴 → 추적 → 완료 트리거 파일), 동시 수정 금지 영역 환기 (agent-ownership.md), 콜드 스타트 자기완결성 가이드, 1 세션 초과 처리법 (셀프 메모), 룰 정합 (distribution-sync · script-creation-approval · agent-ownership), 사용 예시 3종 (영문 배포본 점검 4-29 / Funding 잔재 정리 / 도메인 볼트 인덱스 병렬 재빌드), 직접 작업 vs 위임 선택 기준 표. (2) `.claude/rules/core/_skill-router.md` "에이전트 작업 위임" 트리거 매핑 행 추가. custom 스킬 자체는 배포 무관이지만 `_skill-router.md` 가 core 라 Korean/English 배포 대기. 작업자: Claude |
| R091 | 2026-04-29 | ✅ | ✅ | **Stop hook 신설 — `check_status_on_stop.py` (advisory, _STATUS.md/_SESSION_HANDOFF 갱신 환기)** — 셀프 메모 (`_AGENT_COMMS/to_claude/20260424_Claude_Claude_벌크등록_완료후_훅_구현_제안.md`) 권장 #5 (세션 종료 시 _STATUS.md 검증) 도입. (1) `.claude/hooks/check_status_on_stop.py` 신규 — Stop event 발화 시 `git diff --name-only HEAD` 검사. 변경 파일 있는데 `_STATUS.md` 또는 `_SESSION_HANDOFF_*.md` 미수정이면 stderr 에 advisory 출력 (exit 0, 차단 X). `stop_hook_active` 검사로 무한 루프 방지. (2) `.claude/settings.json` 의 `hooks.Stop` 항목 신규 등록 — matcher 없이 모든 Stop 이벤트에 적용. 기존 `hooks.PreToolUse.Bash` (block_nul_redirection.py) 와 병행. **첫 도입은 advisory 만** (deterministic 차단 X) — 추후 `decision: block` 으로 강화 가능하지만 무한 루프 위험 검증 후. 작업자: Claude. 배포 대기 — hook 파일 자체가 `.claude/hooks/` 배포 대상 (R073 패턴) |
| R090 | 2026-04-29 | – | – | **`Setup New Device.{bat,command,sh}` 신규 — 개인 디바이스 셋업 결합 래퍼 (배포 미대상)** — 신규 디바이스 git clone 직후 환경 셋업 1회 실행 래퍼. 흐름: Node.js 확인 → CoreHub CLI 검증 → `node_modules` + `npm install` → `/register-vaults` dry-run + 사용자 확인 + `--apply` → `sync-all`. 디바이스 무관 (`%~dp0` Windows / `$(dirname "$0")` Unix 자기 위치 탐지). 3종 패턴 (`.bat` Windows · `.command` macOS 더블클릭 · `.sh` Linux/Unix). **개인 git 추적** (노트북 ↔ 원본 PC 디바이스 동기화) + **SellingVault 배포 자동 제외** (`deploy-dist.js` whitelist 메커니즘 — `DEPLOY_TARGETS` 미명시 자동 제외). 배포본에는 컴포넌트만 (`/register-vaults` 스킬 + `sync-all` CLI + `Sync All Vaults.{bat,command,sh}`) 유지하고 결합 래퍼는 사용자 자유 (Unix 철학). 작업 도중 `.gitignore` 에 추가했다가 사용자 지적으로 즉시 제거 — 디바이스 동기화 차단 위험. 부수 작업: 원격 워크트리 브랜치 `claude/hardcore-lichterman-d15740` 삭제 (사용자 명시 승인, fully merged 검증 후 `git push origin --delete`) |
| R089 | 2026-04-29 | – | – | **Codex `_AGENT_COMMS` 참조 shim 추가** — `.codex/rules/agent-comms.md` 신규 작성. 공통 프로토콜 정본은 `_AGENT_COMMS/README.md` 와 `.claude/rules/custom/agent-ownership.md § 에이전트 간 소통` 으로 유지하고, Codex 쪽에는 복제본 대신 얇은 적용 규칙만 둔다. Codex 수신함은 `to_codex/`, 발신함은 `to_claude/` 로 명시하고, `to_claude/` 는 Codex 발신 요청의 응답·archive 확인 또는 사용자 명시 시에만 확인하도록 제한. 세션 시작 frontmatter 일괄 확인, `open`/`in-progress` 보고 후 사용자 지시 대기, `resolved`/`archived` 를 신규 큐로 오분류하지 않는 원칙을 Codex 전용 규칙에 고정. `.codex/rules/agent-rules.md` 의 Codex 고유 규칙 목록에 `agent-comms.md` 추가. `_AGENT_COMMS` 는 개인 원본 전용 소통 공간이므로 Korean/English 배포 무관 |
| R088 | 2026-04-27 | ✅ | ✅ | **PowerShell 권한 정리 + Obsidian 인스턴스 제어 룰/스킬 신설 (B-3) — 워크트리 머지 (원래 R080 → R088 재번호)** — 노트북 워크트리 브랜치 `claude/hardcore-lichterman-d15740` 의 commit `3538a0d6` 머지. 원본 워크트리는 R080 으로 기록됐으나 메인의 R080 (Sync This Vault `.sync/` 이동) 과 번호 충돌, R087 도 사용됨에 따라 R088 로 재번호. 내용: (1) **PowerShell allow 추가** — `.claude/settings.json` `permissions.allow` 에 `"PowerShell"` + `"PowerShell(*)"` 등록. (2) **B-3 방향 재정의** — 기존 "OS 프로세스 레벨 자동 차단·자동 닫기" 폐기 → "사용자가 명시한 N 개에 맞춰 AI 가 열고 닫음 (사용자 감각 기반)" 으로 전환. (3) **`.claude/rules/custom/obsidian-instance-control.md` 신규** — 트리거 키워드, 감지 방법 (`Get-Process Obsidian` + `MainWindowTitle` non-empty), 닫기 (graceful close → CloseMainWindow API → 5초 timeout), 열기 (URI vs 직접 실행 분기), `obsidian.json` open 플래그 동기화. (4) **`.claude/commands/custom/obsidian-windows.md` 신규** — `/obsidian-windows` 스킬 (`N` · `close <볼트>` · `close all` · `open <볼트>` · `status` 인자). (5) **`.claude/rules/core/_skill-router.md` 트리거 매핑 추가** — "Obsidian 인스턴스 제어" 행 → `/obsidian-windows` + `obsidian-instance-control.md`. settings.json·custom 룰/스킬은 배포 대상 아님, `_skill-router.md` 만 core 라 Korean/English 배포 대기 |
| R087 | 2026-04-27 | – | – | **`agent-ownership.md § _AGENT_COMMS` 보강 — frontmatter 일괄 확인 + 큐 트리거 분류 SOP 추가** — 2026-04-27 노트북 → 원본 PC pull 후 `_AGENT_COMMS/to_claude/` 11 건 (8 resolved + 3 open) 누적 상태에서 AI 가 frontmatter 미확인으로 "8 건 모두 큐잉 추정" 보고하는 사고 발생. 근본 원인: 룰에 "trigger ≠ pending task" 분류 SOP 부재. 조치: (1) `세션 진입 시 스캔` 항목에 "다수 (3 건 이상) 또는 pull 직후 신규 파일 포함 시 frontmatter 일괄 확인 후 분류 보고" 강제. (2) `Frontmatter 일괄 확인 (강제)` 하위 섹션 신규 — Grep/bash 명령 + 보고 포맷 템플릿 (open/in-progress/resolved 분리 + 자기발신/상대발신/완료트리거 분류). (3) `큐 트리거 분류 (강제)` 하위 섹션 신규 — 4 종 (신규 작업 큐 / 자기 발신 self-memo / 완료 트리거 / 사용자 메시지) 분류 표 + 처리 방향 + 추정 보고 금지 명시. 계획서: `Vaults/Projects_Personal/Project_MyVaults/Contents/01_Plan/20260427_AGENT_COMMS_커스텀_룰_확장_계획.md` (Phase 1 즉시, Phase 2-3 사례 누적 후). 개인 운영 룰 — Korean/English 배포 무관 (custom/) |
| R086 | 2026-04-25 | ✅ | ✅ | **Domain Preset (ZK) 태그 규칙 명확화 — AI Draft 단계 + 루트 발견 경로 보강** — Codex 큐 응답 (`_AGENT_COMMS/to_claude/20260425_Codex_Claude_Domain_태그규칙_온보딩_하네스_점검.md`). 발단: Codex 가 `MachineAssembly` 신규 노트들을 `tags: [zk]` 만으로 작성, 이게 정본 부합인지·룰 충돌·온보딩 하네스 문제 점검 요청. 분석: Domain Preset 정본 (`BasicDomainVault/CLAUDE.md`) 의 frontmatter 표준이 ZK 정식 5 타입 (zk·literature·synthesis·moc·pattern) 만 다루고 **AI Draft 단계 (`Contents/_AI_Drafts/`) 임시 노트 표기 누락** + 루트 일반 `note-writing` 와 Domain Preset 의 ZK 원칙 장력 (어느 정본 따라야 하는지 발견 경로 약함). 조치: (1) `BasicDomainVault/CLAUDE.md § 태그 규칙·Frontmatter 표준` 확장 — `ai-draft`·`temp` 운영 단계 태그 명시 + 정식 타입과 병기 가능 + AI Draft 폴더 노트는 `tags: [ai-draft]` 또는 `[zk, ai-draft]` 권장 + 정식 타입 승격 시 `ai-draft` 제거. (2) 루트 `_essentials.md § 6` 에 "ZK 시스템 (Domain Preset 계열) 태그 규칙" 섹션 신규 — 일반 태그 규칙 대신 ZK 운영·상태 태그만, 개념은 wikilink+MOC, 정본은 해당 볼트 CLAUDE.md 명시. **자동 검증 (post-edit-review 화이트리스트) 도입 안 함** (의미 판단 복잡성 vs ROI 부족, Codex 도 동의). MachineAssembly `_AI_Drafts/` 4 노트 태그 수정은 Codex 영역 (단일 볼트 노트 편집, agent-ownership 룰 정합) — Codex 가 본 R086 룰 보고 자가 수정 |
| R085 | 2026-04-25 | ✅ | ✅ | **AGENT_ONBOARDING.md § 2 볼트 구성 Multi-Hub 3 계층으로 단순화** — R084 의 1차 일반화 (카테고리 패턴 표) 도 여전히 사용자 컨벤션 (`Domains_*`, `Lab_*`, `Projects_*` 등) 을 노출한다는 사용자 지적. 실제 제품 구조는 Core Hub / Preset Hub / 위성 볼트 3 계층이라 그것만 명시하는 것이 자연스럽고 새 사용자 학습에도 더 직관적. 카테고리 명명 (`Domains_Game`, `Lab_Infra` 등) 은 사용자가 자유롭게 정하는 조직 패턴이지 제품 강제 구조가 아님. 본 R085 에서 § 2 를 3 계층 표 + 동작 흐름 (Core → Preset → 위성 sync 방향) + 볼트 추가 방법 (스킬 호출) 로 재작성. SellingVault 배포본은 다음 `/distribute` 시 자동 반영 |
| R084 | 2026-04-25 | ✅ | ✅ | **AGENT_ONBOARDING.md 개인 자산 노출 차단 + 배포 콘텐츠 안전 룰 신설** — 사용자 발견: `AGENT_ONBOARDING.md § 2 볼트 구성` 표에 개인 볼트명 (JissouGame · CombatToolKit · TileMapToolKit · Unity_Documentation · GameDesign · GameArt · LightAndColor · AppFlowy · Funding · Diary 등) 이 카테고리 예시로 들어가 SellingVault 배포 시 새 사용자에게 작성자 개인 사정 노출. 조치: (1) `AGENT_ONBOARDING.md § 2` 일반화 — 카테고리 표를 패턴 + 용도 설명으로 교체, 실제 볼트는 `_STATUS.md` 볼트 레지스트리 참조 안내, 개인 자산 인용 금지 주의문 추가. (2) `.claude/rules/core/distribution-content-safety.md` 신규 — 배포 대상 문서·미대상 분류, 카테고리 예시·키워드 매핑·변경 로그 작성 규약, Incident Rule (배포 대상 신규 작성·수정 시 사전 점검), 자동 검증 PowerShell 스니펫, SellingVault 배포본 정리 별도 작업 안내. (3) `.claude/rules/MANIFEST.md` core/ 테이블에 신규 행 등록. **SellingVault 정리 후속**: 다음 `/distribute` 시 Korean/English 배포본의 `AGENT_ONBOARDING.md`/`CLAUDE.md`/`AGENTS.md`/`_STATUS.md` 일괄 점검 + 일반화. 본 룰은 향후 모든 에이전트 세션에 상시 주입되어 재발 방지 |
| R083 | 2026-04-25 | ✅ | ✅ | **R081 정본 우선 룰 Claude 영역 반영 (Codex → Claude 큐 응답)** — Codex 가 R081 에서 `_essentials.md § 4` 와 `rules-archive/edit-mode-separation.md` 에 정본·전파본·생성물 분리 룰을 보강한 후 Claude 영역 반영 검토를 큐잉. 검토 결과 (1) `_skill-router.md` 는 변경 불요 (이미 _essentials.md 가 매핑됨, R081 자동 포함), (2) `CLAUDE.md § 에이전트 필수 준수 사항` 에 환기 한 줄 추가 — "workspace 편집 시 (R081): 정본 / 전파본 / 생성물 분류 후 정본·installer/sync 로직 우선 수정 + 버전 신호 + idempotency 검증", (3) `.claude/commands/core/hub-broadcast.md` 에 "정본·전파본·생성물 분리" 섹션 추가 — broadcast 호출 전 대상 파일이 정본인지 확인하도록 환기 (생성물을 broadcast 하면 위성에 stale 사본 남는 문제 방지). archive 는 Codex 가 이미 보강 완료라 추가 X. 결과: Claude 측에서도 R081 원칙이 모든 세션에 자동 환기되고, sync 도구 호출 직전 추가 환기 가능 |
| R082 | 2026-04-25 | – | – | **제조·기계조립 도메인 볼트 신설 — `MachineAssembly`** — 사용자 3개월 공작현장직 및 공장 자동화 기계 조립 업무 대비용 도메인 볼트 생성. `Vaults/Domains_Manufacturing/MachineAssembly/` 경로로 `BasicDomainVault` 템플릿을 `AIHubVault_Domain` 위성으로 clone, 새 카테고리 `Domains_Manufacturing` 등록. 볼트 범위는 렌치볼트·체결부품, 기본 공구, 도면·공차, 자동화 장비 기계 요소, 현장 조립 절차·검수 기준. AI 단독 초안 원칙에 따라 초기 학습 지도와 사용자가 제공한 Naver 블로그 2건·`렌치볼트.pdf` 자료 큐는 `Contents/_AI_Drafts/`에 격리. 루트 `CLAUDE.md`/`AGENTS.md` 라우팅 키워드 및 `_STATUS.md` 레지스트리 갱신 |
| R081 | 2026-04-25 | ✅ | ✅ | **workspace 모드 정본·전파본·생성물 분리 규칙 보강** — Multi-Hub 동기화 환경에서 workspace 편집 전 파일을 정본(source of truth)·전파본(synced copy)·생성물(generated/install artifact)로 분류하도록 공통/Codex 규칙 갱신. 전파본·생성물 직접 편집 대신 정본·템플릿·installer/sync 로직을 우선 수정하고, 버전 신호(`bump-version --broadcast`, Preset `_WORKSPACE_VERSION.md`, 루트 `_ROOT_VERSION.md`)와 최종 설치 위치/idempotency 검증을 완료해야 한다. 이 원칙 위반이 더 이득인 예외 상황은 사용자에게 이유·영향 범위·대안을 먼저 보고하고 확인받도록 명시 |
| R080 | 2026-04-25 | ✅ | ✅ | **Sync This Vault 런처 `.sync/` 내부 이동** — Obsidian 사이드바 오염을 막기 위해 모든 볼트 루트의 `Sync This Vault.bat/.command/.sh` 3종을 제거하고 `<vault>/.sync/Sync This Vault.*` 로 재설치. CoreHub CLI `install-launchers`/`sync-all`/`sync-workspace` 가 Hub 템플릿을 기준으로 설치하도록 보강하여 로컬 stale 템플릿 재사용을 방지. `Sync This Vault.*` 로그 위치는 `<vault>/.sync/sync.log` 로 결정. CoreHub `_WORKSPACE_VERSION.md` 202604240003~0005 기록 + `core-sync-all --broadcast` 완료, 6 Preset Hub `_WORKSPACE_VERSION.md` 202604240003 수동 기록 후 전체 40볼트 sync 재검증 성공. SellingVault 배포는 별도 `/distribute` 작업 대기 |
| R079 | 2026-04-24 | ✅ | ✅ | **수동 sync 런처 도입** — 비개발자 사용자가 더블클릭으로 workspace sync 를 실행할 수 있도록 루트 `Sync All Vaults.bat/.command/.sh` 와 볼트별 `Sync This Vault.bat/.command/.sh` 템플릿 추가. CoreHub CLI 에 `sync-all` 명령(전체 볼트 순회, 누락 `node_modules` 설치, `pre-sync`, `sync.log` append, 성공/실패 요약)과 `install-launchers` 명령(루트/볼트 런처 설치) 추가. `sync-workspace` 가 PULL/VERIFY/PLUGIN_ONLY 후 볼트 루트 런처를 자동 설치하도록 연결. 루트 `README.md` 에 수동 sync 실행 방법 추가. SellingVault 배포 대상: 루트 `Sync All Vaults.*` 와 BasicVaults 내 CoreHub 템플릿/CLI 변경 포함 권장, 실제 배포 동기화는 별도 `/distribute` 작업에서 수행 |
| R078 | 2026-04-22 | – | – | **개인 운영 볼트 신설 — `Project_MyVaults` (Projects_Personal 카테고리)** — AIMindVaults 제품 개발(`Project_AIMindVaults`)과 개인 원본 볼트 관리 작업을 **볼트 수준에서 분리**하라는 사용자 지침 반영. `/clone --hub AIHubVault_Project` 로 생성 — R076 `defaultTemplate` 메커니즘 첫 실전 테스트 통과 (AIHubVault_Project.hub-marker.defaultTemplate="BasicProjectVault" 자동 해석 → 345 파일 mirror copy + make-md systemName=Project_MyVaults + `.sync/hub-source.json` 자동 작성). 새 카테고리 `Projects_Personal/` 신설. 루트 `_STATUS.md` 벌트 레지스트리에 등록. 메모리에 feedback 규칙 추가 (`feedback_aimindvaults_vs_personal_separation.md`) — 향후 모든 에이전트가 두 맥락 구분 필수. 용도: 27 위성 벌트 검수·Preset 전환 결정·개인 rebase 계획·콘텐츠 이행. Korean/English 배포 **대상 아님** (개인 운영 컨텐츠라 `.gitignore` 로 제외 기대, SellingVault 오염 방지) |
| R077 | 2026-04-22 | ✅ | ✅ | **Step 3 완료 — BasicXxxVault 4종 신규 생성 (Preset 별 클론 템플릿)** — R076 defaultTemplate 메커니즘의 실체 구축. 4 Agent 병렬 dispatch (BasicDiaryVault · BasicDomainVault · BasicLabVault · BasicProjectVault) 로 Preset 명세 기반 클론 템플릿 생성. (1) **BasicDiaryVault** (`20260420_Diary_Hub_명세.md` 기반) — Contents/Daily/Weekly/Monthly/Yearly flat 구조 · 5 Templater (Morning/Evening/Weekly/Monthly/Yearly) · Dashboard.md 6 쿼리. (2) **BasicDomainVault** (`20260421_Domain_Hub_명세.md` 기반) — Zettelkasten 8 폴더 (00_Fleeting~05_Pattern + _AI_Drafts + _archive) · 5 Templater (fleeting/literature/permanent/moc/synthesis) · Dashboard.md 6 쿼리 · Contents/_log.md + _index.md 스켈레톤. (3) **BasicLabVault** (`20260421_Lab_Hub_명세.md` 기반) — R&D 9 타입 (idea/literature/permanent/moc/experiment/failure-log/plan/report/decision) · 3축 폴더 · 9 Templater · Dashboard.md 9 쿼리. (4) **BasicProjectVault** (`20260421_Project_Hub_명세.md` 기반) — PARA 4 (Projects/Areas/Resources/Archive) + 9 타입 폴더 (00_Idea~08_Retro) 병존 · 9 Templater · Dashboard.md 8 쿼리. 모두 `BasicContentsVault` 미러 복사 + 각자 Preset 명세 기반 커스터마이징. 공통: hub-marker.json · hub-source.json 미생성 (Template 원칙 유지) · `.obsidian/plugins/` BasicContentsVault 상속 (Hub sync 시 prune 기대) · `_Templates/` 루트 배치 · Templater folder bindings/QuickAdd 는 per-device (사용자 수동). **검증**: 6 Preset Hub × `defaultTemplate` 전수 정상 해석 (default/minimal → BasicContentsVault · diary/domain/lab/project → 각자 Basic 전용 템플릿 FALLBACK 에서 정식 해석으로 승격). 사용자 운영 모드 변경 (2026-04-22): "계획서 존재 시 safe autonomous 는 병렬 dispatch 즉시 실행, risky/judgment 는 deferred 리스트로 몰아 끝에 보고" 적용 첫 세션. **통합 deferred 항목** (사용자 판단 대기): (A) `.obsidian/plugins/` 의 BasicContentsVault 상속 Custom 들 — 각 Preset 명세 Custom 셋과 불일치분 prune 여부 (AIHubVault_* rebase 시 자동 정리 기대), (B) Diary flat 구조 vs `Contents/Diary/Daily/` wrapper 최종 결정, (C) Diary Monthly/Yearly 템플릿 본문 추정치 검토, (D) Diary aimv sync 제외 플래그 (스펙 § 10 권고), (E) Project `_log.md`/`_index.md` 포함 여부 (현재 미포함), (F) Project `Tags/TAGS.md` flat 규칙 재작성 여부, (G) 각 벌트 `_Standards/` BasicContentsVault 상속분 Preset 전용 슬림화 여부, (H) Templater folder bindings/QuickAdd 사전 주입 (현재 per-device 처리). Korean/English 배포 대기 — BasicXxxVault 4종이 BasicVaults 에 포함되어 deploy 시 자동 반영 |
| R076 | 2026-04-22 | ✅ | ✅ | **BasicVaults 런처 재배치 + defaultTemplate 메커니즘 도입 (Multi-Hub 개선)** — 런처 (`MakeCloneVault.bat/.sh`) 를 각 Hub 내부 (7 × 2 = 14 copies) + 24 위성 stray 에서 → `Vaults/BasicVaults/` 루트 **단일 copy** 로 재배치. 계획 노트: `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/plan/architecture/20260422_BasicVaults_런처_재배치_계획.md`. (1) `clone-vault.js` — `CLONE_EXCLUDE_FILES` 에 `MakeCloneVault.bat/.sh` 추가 (향후 위성 오염 방지) + 소스 해석 precedence 도입: `--source-path` > Hub 의 `defaultTemplate` (형제) > `BasicContentsVault` fallback > CLI 자동탐지. `resolveHubDefaultTemplate()` · `resolveBasicContentsFallback()` 신규 함수 + 단계번호 [1/4]~[4/4] 일관화 + 소스 origin 로그. (2) `hub-marker.schema.json` — `defaultTemplate` 필드 신설 (문자열, 이름·상대경로·절대경로 세 형태 지원, 명세 주석 포함). (3) `config.js` — `CORE_PATHS` 에서 `MakeCloneVault.{bat,sh}` 제거 (단일 copy 는 더 이상 Hub 간 전파 불필요, DEPLOY_TARGETS 의 `Vaults/BasicVaults` dir mirror 경유 배포). (4) 신규 `Vaults/BasicVaults/MakeCloneVault.bat/.sh` 작성 — 형제 Hub 자동 스캔 (`.sync/hub-marker.json` 보유 폴더만) + 번호 메뉴 + Hub·Parent·Name 프롬프트 → `node clone --hub` 호출 (CLI 가 defaultTemplate 자동 해석). CoreHub CLI 단일 정본 사용. (5) 6 Preset Hub 의 `hub-marker.json` 에 `defaultTemplate` 필드 추가: default/minimal = BasicContentsVault · diary = BasicDiaryVault · domain = BasicDomainVault · lab = BasicLabVault · project = BasicProjectVault (후자 4종은 Step 3 에서 생성 예정, 그 전까지 BasicContentsVault fallback). (6) 청소: CoreHub + 6 Preset + BasicContentsVault 의 `.sync/MakeCloneVault.*` 제거 (15 파일) + 24 위성의 stray `.sync/MakeCloneVault.bat` 전량 제거 (27 파일, 과거 BasicContentsVault 클론 때 오염된 것, 전부 깨져 있어 기능 무관) → 최종 `find` 결과 `BasicVaults/MakeCloneVault.{bat,sh}` 2건만 잔존. (7) CoreHub `bump-version --broadcast` → 6 Preset Hub 에 18 items 전파 (clone-vault.js + config.js + hub-marker.schema.json). (8) `/create-preset-hub` 스킬 업데이트: `defaultTemplate` 필드 명세 + 해석 규칙 테이블 추가. **Step 3 대기**: BasicDiaryVault · BasicDomainVault · BasicLabVault · BasicProjectVault 4 종 생성 (각 Preset 명세의 폴더구조·Templater·QuickAdd·Dataview 반영). Korean/English 배포 대기 |
| R075 | 2026-04-22 | ✅ | ✅ | **`/create-preset-hub` 스킬 신설 + `create-hub.js` 주석 보강** — Preset Hub 생성 워크플로우를 스킬로 문서화. (1) `.claude/commands/core/create-preset-hub.md` 신규 — 경로 지정 위치 (target-path, --from 자동탐지 공식, hubId 필수 규칙), `create-hub` CLI 가 자동으로 하는 5단계 (mirror copy · per-device 정리 · hub-marker.json · 진입 파일 재작성 · make-md systemName) 와 수동 후속 작업 (coreHubVersion 기입 · Custom 플러그인 설치 · community-plugins.json · 템플릿/QuickAdd/Dataview · 루트 `_STATUS.md`/`_ROOT_VERSION.md` 등록) 구분, 동기화 흐름 3 계층 (Core Push · Custom 내부 · 위성 바인딩) 설명, 위성 바인딩 3 방법 (`clone --hub` · `rebase` · 수동 `hub-source.json`), 검증 4 단계, 금지 사항 5 가지, 관련 문서 크로스 링크. (2) `Vaults/BasicVaults/CoreHub/.sync/_tools/cli-node/src/commands/create-hub.js` 주석 보강 — 파일 최상단에 "Path resolution (경로가 어디서 오는가)" · "5-step workflow (자동 수행)" · "수동 후속 작업" · "Sync semantics after creation" 4 블록 추가, `__dirname` 5단계 상승 공식 (src → cli-node → _tools → .sync → HubRoot) 명시, 상수별 존재 이유 (per-device/CLONE_EXCLUDE) 주석, 각 단계 [1/5]~[5/5] 라벨 통일 (기존 [1/4]·[2/4]·[3/5] 불일치 수정) + 단계별 블록 설명 (왜 필요한가 · 자동/수동 구분 · 범주). (3) `.claude/rules/core/_skill-router.md` — "새 Preset Hub 생성" 행 추가 (트리거: Preset Hub 생성, 프리셋 허브 만들기, create-preset-hub, create-hub, 신규 Hub, AIHubVault_ 생성, Hub 파생), 기존 Multi-Hub 행에서 `create-hub`·`프리셋 허브` 중복 키워드 제거, "새 볼트 생성" 행을 "(위성)" 으로 명확화. (4) `.claude/commands/MANIFEST.md` — core 테이블에 create-preset-hub.md 추가, create-vault.md 용도에 "(위성)" 명시. Korean/English 배포 대기 — 스킬은 배포 대상, `create-hub.js` 주석도 CoreHub 가 BasicVaults 에 포함되어 deploy 시 자동 반영 |
| R074 | 2026-04-21 | ✅ | ✅ | **block_nul_redirection.py hook 출력 포맷 수정 — 작동 검증 완료**. R073 에서 배포된 Claude Code `PreToolUse` hook 이 실제로 차단하지 않는 문제 발견. 원인: 스크립트가 구식 플랫 JSON 포맷 `{"decision": "deny", "reason": "..."}` 를 exit 0 으로 반환 → Claude Code 가 인식 못하고 통과. 수정: (1) 현행 네스티드 스키마 `{"hookSpecificOutput": {"hookEventName": "PreToolUse", "permissionDecision": "deny", "permissionDecisionReason": "..."}}` 로 전환, (2) stderr 에도 동일 reason 기록, (3) exit code 2 로 blocking error 명시 (네스티드 JSON + exit 2 + stderr 3중 확정 경로). 라이브 테스트 통과: `echo x 2>nul` · `echo x >nul` · `echo x 2>NUL` 3가지 변형 모두 차단 + `nul` 파일 미생성 + `> /dev/null` 안전 명령 통과. 참조: docs.claude.com/en/docs/claude-code/hooks. Korean/English 배포 대기 — hook 파일이 배포 대상이라 양쪽 반영 필요 |
| R073 | 2026-04-21 | ✅ | ✅ | **다층 방어 hook/템플릿 배포 + shell-redirect-safety.md 대폭 보강** — 2026-04-21 딥리서치 결과 반영 ([[20260421_nul_파일_누적_생성_이슈_리서치]]). (1) **Claude Code `PreToolUse` hook 배포**: `.claude/hooks/block_nul_redirection.py` 신규 — Bash tool 호출 직전 `2>nul`·`>nul`·`&>nul` 패턴 감지 시 `decision: deny` 반환. `.claude/settings.json` 에 `hooks.PreToolUse.Bash` matcher 등록. 세션 재시작 시부터 deterministic 차단. (2) **CI 템플릿 5종 배포** (`.claude/templates/ci/`): `check-reserved-names.sh` (공통 스캐너, Git hook + CI 재사용), `pre-commit-reserved-names.sh` (Git pre-commit 래퍼), `deny-reserved-names.yml` (GitHub Actions workflow), `bashrc-nul-trap.sh` (Bash DEBUG trap), `vault-watcher.js` (Node fs.watch 실시간 감시). 배포 레포에 수동 또는 distribute 스킬로 설치. (3) **`.claude/rules/core/shell-redirect-safety.md` 대폭 보강**: 근본 원인 설명 (Cygwin/MSYS2 의 POSIX 호환 의도적 설계), PowerShell FileSystemProvider 조기 중단 재해석 (유령 매칭이 아니라 FileInfo materialize 실패로 서브트리 가림), 다층 방어 5 레이어 (에이전트/셸/저장소/CI/파일시스템) 배포 위치 안내, Windows 예약 이름 전체 표 (NUL·CON·PRN·AUX·COM1-9·LPT1-9), Obsidian Shell Commands 감사 절차 Incident Rule 추가, 삭제 처리량 순위 (Rust 네이티브 > PS `[System.IO.File]::Delete('\\?\...')` > cmd del), 딥리서치 교차 참조 링크. 기존 10줄 규모에서 164줄로 확장. Korean/English 배포 대기 — hook/템플릿/규칙 모두 범용이라 양쪽 대상 |
| R072 | 2026-04-21 | ✅ | ✅ | **shell-redirect-safety.md 에 PowerShell `Get-ChildItem -Filter nul` 유령 매칭 Incident Rule 추가** — 2026-04-21 조사 결과 반영. PowerShell `-Filter nul` 이 Windows `FindFirstFile` API 의 예약 디바이스명 처리로 모든 디렉토리에서 가짜 `FileInfo` 를 반환함 (41,440 개 유령 vs 실제 0개). 정확한 스캔 방법 4 가지 제시 (cmd `dir /A /B /S` 1순위, Python `os.walk` 2순위, Rust `walkdir` 3순위, PowerShell Filter 없이 4순위), 검증 방법 (`[System.IO.File]::Exists('\\?\...')` + Delete 예외 메시지 판별), 2026-04-21 Incident 링크. Korean/English 배포 대기 — 셸 안전 규칙이라 양쪽 대상 |
| R071 | 2026-04-21 | ✅ | ✅ | **plugin-seed 부트스트랩 + Hub systemName 정합성 복구** — 발단: Preset Hub 생성 시 make-md `systemName` 이 벌트명과 일치해야 한다는 규칙(`rules-archive/vault-individualization.md`) 위반 확인. CoreHub 가 AIHubVault 에서 파생될 당시 data.json 이 복사됐지만 systemName 재작성이 누락되어 "AIHubVault" 로 남음. 5개 Preset Hub (Minimal·Diary·Domain·Lab·Project) 는 Core 6 시기(make-md 미편입)에 `create-hub` 로 파생되어 make-md data.json 자체가 부재. 근본 원인: `create-hub` Step 1 (mirrorDirectory) 은 source에 없는 파일을 생성하지 않고, `core-sync-all` 은 data.json 을 `CORE_FORCE_DATA_JSON` 화이트리스트 외에 전파하지 않음(설계상 개인 설정 보호). 조치: (1) `src/lib/plugin-seed.js` 신규 — `seedPluginDataIfMissing()` + `seedPluginDataBatch()` + `normalizePluginData()` (make-md 전용 systemName 재작성 포함). 정책: target 에 data.json 이 **부재할 때만** seed, 기존 파일 덮어쓰지 않음 (conservative). (2) `core-sync-all.js` 에 "Plugin Data Seed" 단계 추가 — CORE_PLUGINS 배열 전체를 대상으로 seed 수행. Core 플러그인이 추후 추가되어도 기존 Preset 에 자동 부트스트랩. (3) CoreHub data.json systemName 수동 수정 ("AIHubVault" → "CoreHub"). (4) `bump-version --broadcast` 로 6 Preset 전파 → 5개 Preset에 make-md/data.json 생성 + systemName 각각 "AIHubVault_Diary", "AIHubVault_Minimal" 등 볼트명으로 자동 재작성. 결과: 7개 Hub 전부 systemName=벌트명 정합. 향후 새 Core 플러그인 추가 시 기존 Preset 에도 자동 seed 보장. `create-hub` 는 Core Hub 에 make-md 가 편입된 R070 이후 Step 1 (data.json 복사) + Step 5 (systemName 재작성) 로 정상 동작. Korean/English 배포 대기 |
| R070 | 2026-04-21 | ✅ | ✅ | **Core 7 편성 + core-sync-all 자동 병합 로직** — (1) CoreHub `CORE_PLUGINS` 배열에 `make-md` 추가 (Core 6 → Core 7). 이유: Preset Hub 환경에서 make-md 가시성(사이드바 네비게이션, 폴더 트리, 태그 스페이스)이 핵심 UX라 모든 Preset에 강제 전파 필요. CoreHub `.obsidian/plugins/make-md/` 설치(5.7MB 4 파일) + `community-plugins.json` 활성화 + `CLAUDE.md` "Core 7" 참조 수정. (2) `src/commands/core-sync-all.js` 에 `mergeCommunityPlugins()` 함수 신설 — 각 Preset의 `community-plugins.json` 을 읽어 `[...CORE_PLUGINS (config 순서), ...기존 Custom (순서 보존)]` 로 병합. 기존엔 Core 플러그인 파일만 복사하고 활성화 목록은 수동 편집 필요했음. (3) `bump-version --broadcast` 로 6 Preset Hub(Default·Minimal·Diary·Domain·Lab·Project) 전부에 make-md 파일 + community-plugins 병합 전파 완료 (총 38 item 동기화). 향후 Core 플러그인 추가 시 자동 활성화 보장. Korean/English 배포 대기 — CoreHub 자체가 BasicVaults 에 포함되어 deploy 시 함께 반영 |
| R069 | 2026-04-21 | ✅ | ✅ | **셸 리다이렉트 안전 규칙 신설** — `.claude/rules/core/shell-redirect-safety.md` 신규 생성. 2026-04-21 Incident (Git Bash에서 CMD 구문 `2>nul` 혼용으로 AIMindVaults 전체에 69개 빈 `nul` 파일 생성) 기반. 셸별 NUL 리다이렉트 구문 표 (Bash `2>/dev/null` · PowerShell `2>$null` · CMD `2>nul`), 셸 식별 기준 (Bash tool → Git Bash / `powershell` · `pwsh` → PowerShell / `cmd /c` → CMD), Windows Incident Rule (Bash에서 `2>nul` 금지 · 생성된 `nul` 파일 삭제는 `\\?\` UNC prefix + `[System.IO.File]::Delete()` 사용), 크로스플랫폼 원칙 (배포본은 Bash 구문 기본). `.claude/rules/MANIFEST.md`의 core/ 테이블에 신규 행 추가 (알파벳 순서로 `script-management.md` 와 `temp-file-management.md` 사이). 상시 주입 대상이라 다음 세션부터 모든 에이전트에 자동 적용. Korean/English 배포 대기 — 범용 안전 규칙이라 양쪽 배포 대상 |
| R068 | 2026-04-20 | ✅ | ✅ | **루트 CLAUDE.md 에 "의도별 진입 가이드" 섹션 추가** — Gumroad 의 obsidian-pllc 템플릿 (가상 법률회사 예시 볼트) 의 Claude.md 네비게이션 패턴 차용. 시간·대상·행위 3축 의도 카테고리 11개 (상태 파악 · 세션 맥락 · 새 노트 · 검색 · 도메인 참조 · 프로젝트 · Multi-Hub 변경 · 배포 · 규칙 수정 · 개인 기록 · 에이전트 협업) 각각을 구체적 진입 파일·명령으로 매핑. 기존 키워드 매핑 (볼트 ID 수준) 은 보조 레이어로 유지 · 의도가 불명확할 때 내려감. 관련 연구 노트: 20260420_Zettelkasten_Deep_Research_프롬프트.md, 20260420_Diary_Hub_Deep_Research_프롬프트.md, 20260420_Diary_Hub_명세.md |
| R067 | 2026-04-20 | ✅ | ✅ | **Multi-Hub Phase 1 최종 완료 — 검수 통과 + 세션 종료**. (1) `.claude/commands/core/create-vault.md` 업데이트 — Hub 선택 UX + `--hub` 옵션 예시 (CoreHub · AIHubVault) 추가. (2) 루트 `_STATUS.md` 갱신 — BasicVaults 레지스트리에 CoreHub (Core Hub, hubId=core) · AIHubVault_Minimal (Preset, hubId=minimal) 신규 등록 · AIHubVault 타입을 "Hub" → "Preset Hub (default)" 로 변경. Project_AIMindVaults 행에 Multi-Hub Phase 1 완료 기록. (3) 설계 문서 `20260419_Multi_Hub_아키텍처_설계` 헤더에 Phase 1 구현 완료 링크 추가. (4) 구현 결과 노트 `20260420_Multi_Hub_Phase1_구현_결과` 신규 작성 (Project_AIMindVaults). (5) 루트 `_SESSION_HANDOFF_CLAUDE.md` 재작성. Korean/English 배포 대기 — CoreHub/AIHubVault_Minimal 포함한 BasicVaults 전체가 deploy 시 자동 반영 예정 |
| R066 | 2026-04-20 | ✅ | ✅ | **Multi-Hub Phase 1 → 루트 규칙 반영**. (1) `.claude/rules/core/_skill-router.md` — Multi-Hub 트리거 행 추가 (키워드: Core Hub, Preset Hub, CoreHub, core-sync, core-sync-all, hub-source.json, hub-marker.json, create-hub, multi-hub, 프리셋 허브, 코어 허브, bump-version --broadcast, hubId, hubType, hub-resolver) → 설계 문서 + 구현 결과 노트 Read 지시. (2) `.claude/rules/core/_essentials.md § 4` — workspace 모드 섹션 전면 재작성: Multi-Hub 계층 표 (Core Hub / Preset Hub / 루트 / 볼트별), Core 편집 워크플로우 강제 (CoreHub 에서만 · bump-version --broadcast 자동 체인 · 실행 전 완료 보고 금지), Preset Hub 편집 순서, 절대 금지 규칙 (Preset/위성에서 Core 편집 · 위성에 hub-marker 수동 작성). 두 파일 상시 주입 대상이라 에이전트가 다음 세션부터 자동으로 Multi-Hub 트리거 인식 + Core 편집 규칙 강제 적용. Korean/English 배포 대기 — 배포 시 CoreHub 자체도 BasicVaults 에 포함되어 함께 배포됨 |
| R065 | 2026-04-19 | – | – | **Phase 8 설정 재검증 — bkit/MCP 누수 원인 제거**. 1차 실측 결과 Custom agents 6.3k·MCP deferred 67.3k 미감소 확인. 원인 진단: (1) `C:/AIMindVaults/.claude/settings.json`의 `bkit: true`가 전역 `false` 오버라이드 → 루트 세션에서 bkit 16개 에이전트 로드. (2) `C:/Users/c/.claude.json`의 전역 `mcpServers`에 `mcp-unity`·`serena` 등록 → 모든 세션에 주입. 조치: (1) `C:/AIMindVaults/.claude/settings.json`에서 `enabledPlugins` 블록 제거 (bkit·github 전역 false 승계). (2) `C:/Users/c/.claude.json`의 `mcpServers` 비움 (`{}`), `mcp-unity`·`serena` 제거. 백업 `~/.claude.json.backup-20260419`. (3) `C:/Dev_Game/GameBaseProject/.claude/settings.json`에 `serena` MCP 추가 (Unity 프로젝트에서만 로드). (4) `Vaults/Projects_Infra/Project_AIMindVaults/.claude/settings.json`·`Vaults/Lab_Game/CombatToolKit/.claude/settings.json`·`Vaults/Lab_Game/TileMapToolKit/.claude/settings.json` 3개 신규 — 해당 하위 볼트에서만 bkit 활성. 예상 효과: AIMindVaults 루트 세션 Custom agents 6.3k → 0, MCP deferred 중 mcp-unity(28 tools)·serena(19 tools) 제거분 ~8k 감소. 잔여 MCP deferred(~59k)는 Claude Desktop 내장 커넥터(Calendar·Gmail·MS Docs·Chrome 등)로 데스크탑 UI에서 비활성화 필요. 사용자 환경이라 배포 대상 아님 |
| R064 | 2026-04-18 | – | – | **Phase 8 MCP/Plugins 최적화 완료 — 사용자 환경 설정**. (1) 전역 `~/.claude/settings.json` 재구성: `mcpServers` 7개 → 1개(`notion`만 유지). 제거: `playwright`, `context7`, `github`(Failed). 이동: `mcp-unity`, `blender`, `youtube-transcript`. `enabledPlugins` 2개 모두 `false`로 전환(`bkit@bkit-marketplace`, `github@claude-plugins-official`). (2) AIMindVaults 프로젝트 `.claude/settings.json` 신규 — `bkit` 플러그인 프로젝트 범위 활성화(CombatToolKit/TileMapToolKit/Project_AIMindVaults 볼트 작업용), `blender`·`youtube-transcript` MCP 포함. (3) Unity 프로젝트 `C:/Dev_Game/GameBaseProject/.claude/settings.json` 신규 — `mcp-unity` MCP만 포함. 백업: `~/.claude/settings.json.backup-20260418`. 사용자 환경 변경이라 배포 대상 아님(Korean/English 모두 `–`). 예상 효과: MCP tools (deferred) 67.3k → ~15k, Custom agents 6.3k → ~0, 전체 베이스라인 170k → ~80k |
| R063 | 2026-04-18 | ✅ | ✅ | **`.forge/` 디렉토리 전면 제거**. (1) 29개 볼트(AIHubVault, BasicContentsVault, Domains/Labs/Projects 전체)에서 `.forge/` 디렉토리 삭제 — 외부 에이전트 결과물 스테이징 허브 개념 폐기. (2) 원본 `_forge`/`.forge` 참조 정리: 루트 `CLAUDE.md`, `.cursorrules`, `.codex/CODEX.md`, `.codex/rules/vault-routing.md`, `_WORKFLOW.md`, `.sync/_WORKFLOW.md`; AIHubVault `CLAUDE.md`, `_STATUS.md`, `AGENTS.md`, `CODEX.md`, `.codex/CODEX.md`, `.codex/rules/never-do.md`, `.antigravity/SESSION_RULES.md`, `.github/copilot-instructions.md`, `CORE_CONCEPTS.md`, `README.md`, `_Standards/CONTENTS_SPEC.md`, `Contents/CONTENTS_GLOSSARY.md`, `Contents/CONTENTS_AI_RULES.md`; 루트 `_Standards/Core/Script_Registry.md`(`.forge/staging/` 일회성 스크립트 2개 제거). (3) PS1 로직 수정 — `_tools/cli/pre_sync.ps1`, `sync_workspace.ps1`의 Hub 식별 마커를 `_forge/` → `.sync/.hub_marker`로 변경 (Node.js CLI와 일관). (4) Korean/English 배포본에는 이미 `.forge/` 제거 진행분(각 47개/141개)이 미커밋 상태로 존재 — 이번 R063 커밋에 포함하여 정리. 유지: 각 볼트 `_VAULT-INDEX.md`, `_WORKSPACE_VERSION.md`, Contents 계획 노트의 과거 기록 항목은 역사 보존 |
| R062 | 2026-04-18 | ✅ | ✅ | **규칙 주입 구조 개편 Phase 2-C + Phase 3 완료**. (1) Phase 2-C: `core/user-guidance.md` §5(workspace 동기화)·§8(편집 후 검증) 안내문을 _essentials.md 참조형으로 축약 — 구체 명령은 `_essentials.md § 4/§ 5`로 위임, user-guidance는 감지+핵심 한줄 유지. (2) Phase 3: `core/vault-individualization.md`(2.1k) → `rules-archive/vault-individualization.md` 이관. `commands/core/create-vault.md` 본문에 archive Read 지시 추가. `core/_skill-router.md`의 "새 볼트 생성" 행을 `/create-vault` Skill + archive Read로 전환. `core/user-guidance.md §2` 참조 경로를 rules-archive 경로로 수정. 루트 `CLAUDE.md` 상시 주입 "나머지" 목록에서 vault-individualization 제거. `.claude/rules/MANIFEST.md` 갱신 — core 테이블에서 제거, rules-archive에 Phase 3 섹션 신설. (3) Phase 2-A 잔여 bulk-edit-safe **최종 결론: 생성하지 않음** — encoding-safety·temp-file-management는 core 유지 (평시 필수 안전 규칙, Skill 트리거로는 자기교정 지연 리스크). Phase 3 예상 감축 -2.1k (Memory files 25.1k → ~23k). Korean/English 배포 모두 🕓 — vault-individualization은 범용 규칙이라 배포 대상, 경로 이관은 배포본에도 그대로 반영 필요 |
| R061 | 2026-04-18 | ✅ | ✅ | **규칙 주입 구조 개편 Phase 2-A 전체 완료 — 도메인 규칙 6개 Skill 전환**. custom/에서 rules-archive/로 6개 규칙 이관: `blender-mcp.md`, `notion-sync.md`, `serena-mcp.md`, `unity-scripting-style.md`, `unity-tools.md`, `discord-bot.md`. 대응 Skill 5개 신규(`commands/custom/`): `blender-workflow`, `meshy-workflow`, `notion-record`, `discord-admin`, `unity-dev` (unity-tools/unity-scripting-style/serena-mcp 3개 통합). `_skill-router.md` 전면 재작성 — 매핑 값을 "파일 경로 Read" → "Skill 호출"로 전환, Phase 2-A 완료 섹션 추가. `MANIFEST.md` 갱신 — custom/ 8개→2개(agent-ownership, multivault-personalization만 유지), rules-archive/ Phase 2-A 섹션에 6개 신규 엔트리. 잔존 custom 2개는 에이전트 협업/멀티볼트 개인화 규칙으로 배포 사용자도 해당 가능성 있어 유지. **bulk-edit-safe Skill은 보류** — 대상(encoding-safety, temp-file-management)이 core 안전 규칙이라 이동 시 세션 시작 단계 자기교정 실패 리스크. Phase 2-A 총 감축 예상 ~8k 토큰 (custom 12.7k 중 6개 이관). Korean/English 배포 모두 ⚠️: core/_skill-router.md가 배포되는데 6개 Skill은 custom/ 소속이라 배포 사용자 환경에선 Skill 미존재 → 배포본 분기 필요 (router 전체 행 또는 각 Skill 제작자 경로 제거). Phase 2-A 완료 = Skill 전환 원칙 검증. 다음: 신규 세션 실측 → 배포본 분기 버전 작성 → 온보딩/README 동시 반영 (프로젝트 메모리 참조) |
| R060 | 2026-04-18 | ✅ | – | **규칙 주입 구조 개편 Phase 2-A PoC — `/distribute` Skill 전환**. (1) `.claude/rules/custom/distribution-deploy.md`, `sync-version-priority.md` 2개 파일을 `.claude/rules-archive/`로 이동 (자동 주입 제외). (2) `.claude/commands/custom/distribute.md` 신규 — 다국어 배포 경로, 콘텐츠 기준 체크리스트, Git push 절차, sync 수정 시 Pre/Post-Check 통합 Skill. 트리거 키워드: 배포, SellingVault, git push, 영문 배포, distribute, deploy, cli.js sync, pre-sync 등. (3) `.claude/rules/core/_skill-router.md` 갱신 — 배포·Git push 행과 sync 기능 수정 행의 목적지를 `/distribute` Skill 호출로 전환, 하단에 Phase 2-A 진행 상태 섹션 추가. (4) `.claude/rules/MANIFEST.md` 갱신 — custom/에서 2개 제거, rules-archive/에 Phase 2-A 섹션 신설. **Korean 상태 ⚠️ 조정 필요**: core/_skill-router.md의 "배포·Git push" 및 "sync 기능 수정" 2개 행이 creator-only 경로(`/distribute` Skill + archive 파일)를 가리켜 배포 사용자 환경에서 깨진 링크가 됨. 배포 시 두 행을 제거한 슬림 버전으로 분기 관리 필요 (C안). custom 영역 변경분은 English/Korean 모두 배포 무관. Phase 2-A 목표 7개 Skill 중 첫 PoC, Unity/Blender/Meshy/Discord/Notion은 실측 후 진행 |
| R059 | 2026-04-18 | ✅ | ✅ | **규칙 주입 구조 개편 Phase 2-B 완료 — user-guidance.md D안 분할**. 원본(4.3k)을 리스크 등급별로 이층 분리: (1) `.claude/rules/core/user-guidance.md` 슬림화 — 고위험 6섹션(§2 볼트 생성 수동 복사, §4 편집 모드 혼동, §5 workspace 동기화 버전 누락, §8 편집 후 검증 BAD/INDEX, §10 무한 재귀 삭제, §11 스크립트 생성 사전 승인)만 인라인 즉시 차단용으로 유지. (2) `.claude/rules-archive/user-guidance-detail.md` 신규 — 원본 12개 섹션 전체 이관 (트리거 체계 유형 A/B/C 포함). (3) `.claude/rules/core/_skill-router.md` 확장 — "유저 질문/막힘" 행을 "유저 가이드 저위험" 행으로 변경, 트리거 키워드 확장 + 목적지를 `rules-archive/user-guidance-detail.md`로 전환. (4) `.claude/rules/MANIFEST.md` 갱신 — core/ user-guidance 설명 수정, rules-archive/에 Phase 2-B 섹션 신설. 단순 archive 이동(C안) 기각 사유: 유형 B 트리거는 감지 즉시 차단이 핵심이라 archive Read 지연 시 실수 발생 가능 |
| R058 | 2026-04-18 | ✅ | ✅ | **규칙 주입 구조 개편 Phase 1 완료**. `.claude/rules/core/_essentials.md` 신규 (보고 언어, 토큰 절약, 볼트 라우팅, 편집 모드, Post-Edit Review, 노트 작성 frontmatter/H1/파일명, 세션 종료 통합 — 8.6 KB). `.claude/rules/core/_skill-router.md` 신규 (12+ 트리거 키워드 → 규칙 파일 경로 매핑 — 3.8 KB). 흡수된 7개 파일 (`token-optimization`, `session-exit`, `note-writing`, `vault-routing`, `post-edit-review`, `edit-mode-separation`, `report-language`)을 `.claude/rules-archive/`로 이동. `.claude/rules/MANIFEST.md` 전면 재작성 (core 11개, custom 10개, archive 8개). 루트 `_STATUS.md` 볼트 레지스트리에 "경로" 컬럼 추가 (볼트 ID → 경로 lookup 일원화). 루트 `CLAUDE.md` 슬림화 12.6 KB → 6.6 KB (47% 감축) — 볼트 레지스트리 테이블 제거하고 `_STATUS.md` 참조로 전환, 키워드 추론은 "키워드 → 볼트 ID" 2컬럼 표로 간소화, 주입 구조 섹션 신설 |
| R054 | 2026-04-15 | – | – | 다국어 배포 구조 도입. `distribution-deploy.md` (custom)에 Korean/English 2경로 + 2레포 명시, GitHub Issues 트리아지 규칙 추가, 다국어 운영 섹션 신설. `_ROOT_VERSION.md` 스키마에 배포 반영 상태 컬럼(Korean/English) 추가. 원본 볼트 `Project_AIMindVaults`에 [[20260415_영문_배포판_번역_계획]] 신규, [[20260415_베타_포스팅_계획_Discord_NaverCafe]] 영문판 의존성 반영 |
| R055 | 2026-04-17 | – | – | Discord 도메인 볼트 신설 (`Vaults/Domains_Infra/Discord/`). AIMindVaults 디스코드 서버 운영 결정의 사전 정보수집 단계. 루트 `CLAUDE.md` 볼트 레지스트리·진입 프로토콜 키워드 추가, 루트 `_STATUS.md` 레지스트리 등록. 봇·채널·자동화·모더레이션 도메인 지식 축적 시작 |
| R056 | 2026-04-17 | – | – | AIMindVaults Admin Bot 운영용 custom 스킬/룰 추가 (제작자 전용, 배포 동기화 대상 아님). `.claude/commands/custom/discord-manage.md` 신규, `.claude/rules/custom/discord-bot.md` 신규. 두 MANIFEST custom 섹션에 등록 |
| R057 | 2026-04-18 | – | – | **규칙 주입 구조 개편 Phase 0 PoC**. `.claude/rules-archive/` 신설, `meshy-api.md`를 custom/에서 archive/로 이동하여 자동 주입 제외 여부 검증 준비. 루트 `CLAUDE.md`에 "아카이브된 규칙" 섹션 + 네임스페이스 구조에 rules-archive 추가. `.claude/rules/MANIFEST.md` custom에서 meshy-api 제거, "rules-archive/" 섹션 신설. `.claude/settings.local.json`에 `permissions.deny` 13개 (치명 삭제·force push·SellingVault 쓰기 차단) + `skipAutoPermissionPrompt: true` 추가. 계획 노트 `Vaults/Projects_Infra/Project_AIMindVaults/Contents/Project/plan/architecture/20260418_규칙_주입_구조_개편_계획.md` 신규 |

## 레거시 스키마 (2026-04-15 이전)

배포 반영 상태 컬럼 도입 전 기록. 필요 시 과거 반영 여부는 배포본 git 히스토리로 역추적한다.

| 버전 | 날짜 | 변경 내용 |
|------|------|----------|
| R053 | 2026-04-13 | PS1 잔존 참조 전면 제거. Codex 스킬 3개, CODEX.md 28개, .codex/rules/ 84개, .codex/skills/session-end 27개, .github/copilot-instructions 28개, .antigravity/SESSION_RULES 28개, AGENT_ONBOARDING_CLAUDE/CODEX, .codex/rules/edit-scope, agent-ownership, Hub _WORKFLOW, Hub_Sync_Targets, TOOLS_INDEX 전면 개편 — 총 200+ 파일 Node.js CLI 참조로 전환 |
| R052 | 2026-04-13 | `docs/` 신설 (architecture, cli-reference, customization). core commands 6개 PS1→Node.js CLI 전환. AGENT_ONBOARDING.md PS1 참조 전환 + docs 연동. README.md docs 참조 추가 |
| R051 | 2026-04-13 | core/custom rules 8개 PS1→Node.js CLI 참조 전환. PS1 전체 제거. standards 커맨드 추가. Script_Registry 전면 개편 |
| R050 | 2026-04-13 | TestVault 삭제. GameDesign, Blender, Git, AI_Gen4Game 4개 볼트 루트 `CLAUDE.md` 레지스트리 + 라우팅 키워드 등록. `Domains_3D`, `Domains_VCS`, `Domains_AI_Asset` 카테고리 신설 |
| R049 | 2026-04-13 | frontmatter `tags` 표준화. 계층 태그 flatten, PascalCase→kebab-case 변환, 볼트 식별 태그 제거 (2차에 걸쳐 ~696파일). `note-writing.md` 태그 규칙 추가 — 기본 형식 + 사용자 오버라이드 구조. `AGENT_ONBOARDING.md` §8에 타입/태그 시스템 설명 섹션 추가 |
| R048 | 2026-04-13 | 빈 타입 86건 처리. `folder-index` 코어 타입 신설 (note-writing.md). Domain.md/Project.md 51건 folder-index, CombatToolKit Legacy 27건 design, Grok 로그 5건 reference, 개별 3건 (plan/knowledge/study-note) 부여 |
| R047 | 2026-04-13 | `master_index_build.ps1` Resolve-Path trailing backslash 버그 수정 (TrimEnd 추가). `/sync-all` 코어 스킬 신설 — 전체 위성 볼트 워크스페이스 일괄 동기화. MANIFEST 등록 |
| R046 | 2026-04-13 | frontmatter `type` 표준화. `note-writing.md`에 코어 타입 목록(21개) + 볼트 전용 타입 확장 규칙 추가. 동의어 통합 72개(standards→standard, knowledge-note/domain→knowledge, planning-note→plan, research-note→research), 1회성 타입 12개 흡수, 위성 볼트 EXAMPLE_JUGGL 노트 156개 제거. ObsidianDev·Unity CLAUDE.md에 전용 타입 선언 |
| R045 | 2026-04-13 | `Vaults/Domains_Dev/JavaScript/` 신규 볼트 생성. 루트 `CLAUDE.md`, `_STATUS.md`에 JavaScript 레지스트리와 라우팅 키워드 추가 |
| R044 | 2026-04-13 | `vault-individualization.md` CLAUDE.md 필수 항목에 수집 범위·경계 추가. `vault-routing.md` 라우팅 판단 시 후보 볼트 CLAUDE.md 수집 범위 참조 규칙 추가 |
| R043 | 2026-04-08 | `user-guidance.md` 코어 규칙 신설. 12개 코어 기능별 유저 혼란 상황 + 에이전트 안내 지침. MANIFEST 등록. `create-vault.md`, `open-vault.md`에 Obsidian 미등록 볼트 URI 금지 규칙 추가. `AGENT_ONBOARDING.md` §16에 볼트 등록 안내 추가 |
| R042 | 2026-04-08 | `temp-file-management.md`에 "무한 재귀 경로 삭제 (Incident Rule)" 섹션 추가. PowerShell flatten-and-delete 1순위, robocopy mirror 2순위. 실패하는 방법(7z, Remove-Item, rd, .NET Delete) 명시 |
| R041 | 2026-04-08 | `Vaults/Domain_Art/ArtInsight/` 신규 볼트 생성. 루트 `AGENTS.md`, `CLAUDE.md`, `_STATUS.md`에 ArtInsight 레지스트리와 라우팅 키워드 추가, ArtInsight 개별 진입 문서/상태 문서/콘텐츠 규칙 개별화, 첫 노트 `20260408_미적_안목을_기르는_분별의_기준.md` 작성 |
| R040 | 2026-04-08 | 콘텐츠 인덱서 우선 검색 강제 규칙 추가. `token-optimization.md` §0 신설 (인덱서 → fallback 순서), `vault-routing.md`에 첫 접근 시 인덱스 빌드 강제, `vault-individualization.md`에 생성 후 초기 빌드 단계 추가 |
| R039 | 2026-04-08 | 볼트명 변경 CodeStyle → AI_Coding. 루트 CLAUDE.md 레지스트리/라우팅, 루트 _STATUS.md 레지스트리 갱신. 볼트 내부 태그 전체 AI_Coding으로 교체 |
| R038 | 2026-04-08 | 콘텐츠 인덱싱 완료 강제 보강. AIHub `.sync/_tools/cli/post_note_edit_review.ps1`가 인덱서를 별도 PowerShell 프로세스로 실행해 성공 판정을 안정화하고, root `.codex/skills/create-video-note`, `create-article-note`, `create-pdf-note`에 `POST_EDIT_INDEX_UPDATED=1` 확인과 수동 fallback 절차를 명시 |
| R037 | 2026-04-08 | 콘텐츠 인덱싱 규칙 강화. `post-edit-review.md`에 노트 추가/편집 후 인덱싱 필수 조건(`POST_EDIT_INDEX_UPDATED=1`) 추가, `/reindex` 스킬 경로를 `.sync/_tools/cli/` 구조에 맞게 갱신 |
| R036 | 2026-04-08 | Codex 전용 소스 노트 파이프라인 추가. root `.codex/skills/create-article-note/`, `.codex/skills/create-pdf-note/` 신규 생성, `.codex/skills/create-video-note/`를 동일 기준으로 확장 |
| R035 | 2026-04-06 | `/open-notes` 스킬 custom/ → core/ 격상. 활성 볼트 판별·경로 변환·파일 존재 확인 3단계 안전장치 추가. MANIFEST 등록 |
| R034 | 2026-04-06 | `AGENT_ONBOARDING.md` 신규 — 에이전트 범용 온보딩 문서. 환경 개요, 핵심 규약 14개 압축, 규칙 파일 경로 참조 |
| R033 | 2026-04-06 | `distribution-sync.md` Juggl_StyleGuide 배포 제거, `juggl-style-sync.md` 볼트별 개별 관리로 변경. 전체 볼트 `docs/` → `.docs/` 리네임, `recent_files.txt` 삭제, `Juggl_StyleGuide/` 볼트별 `Contents/`로 이동 |
| R032 | 2026-04-05 | `/reindex` 스킬 신설 (`.claude/commands/core/reindex.md`) — 단일/전체 볼트 콘텐츠 인덱스 재빌드. MANIFEST 등록 |
| R031 | 2026-04-05 | `/open-notes` 스킬 신설 (`.claude/commands/custom/open-notes.md`) — local-rest-api로 복수 노트 새 탭 열기. 글로벌 CLAUDE.md 스킬 목록에 등록 |
| R030 | 2026-04-02 | `session-exit.md` — 볼트별 핸드오프 작성 시 루트 핸드오프에 참조 남기기 규칙 추가 |
| R029 | 2026-04-01 | 세션 핸드오프 에이전트별 분리. `_SESSION_HANDOFF.md` → `_SESSION_HANDOFF_CLAUDE.md` + `_SESSION_HANDOFF_CODEX.md`. `session-exit.md` 규칙 갱신, `CLAUDE.md` 진입 프로토콜 갱신 |
| R028 | 2026-04-01 | JissouGame 신규 볼트 생성. `Projects_Game` 카테고리 신설, 루트 `AGENTS.md`, `CLAUDE.md`, `_STATUS.md`, `.claude/rules/core/vault-individualization.md` 갱신 |
| R027 | 2026-04-01 | TileMapToolKit 신규 볼트 생성. 루트 `AGENTS.md`, `CLAUDE.md`, `_STATUS.md`에 레지스트리/라우팅 추가 |
| R026 | 2026-04-01 | `note-writing.md` — frontmatter `agent` 필드 규칙 명시: 최신 작업자만이 아니라 해당 노트에 작업한 에이전트를 누적 기록 |
| R025 | 2026-03-23 | 볼트 개별화 규칙 신설 (`.claude/rules/core/vault-individualization.md`) — 볼트 생성 시 이름/분류/CLAUDE.md/태그 표준화 |
| R024 | 2026-03-23 | 멀티볼트 개인화 규칙 신설 (`.claude/rules/custom/multivault-personalization.md`) — 에이전트/플러그인/스킬 커스텀 설정 |
| R023 | 2026-03-21 | Personal 카테고리 신설 + `Vaults/Personal/Diary/` 다이어리 볼트 생성. 루트 CLAUDE.md, _STATUS.md에 등록. 라우팅 키워드 추가 |
| R022 | 2026-03-21 | 세션 핸드오프 시스템 도입. 루트 `_SESSION_HANDOFF.md` 신규, `session-exit.md` 핸드오프 작성 규칙 추가, 루트 `CLAUDE.md` 진입 프로토콜에 핸드오프 읽기 추가 |
| R021 | 2026-03-21 | 신규 볼트 2개 생성: `Vaults/Domains_Infra/AI` (AI 도메인), `Vaults/Projects_Infra/Project_AIMindVaults` (프로젝트) — AIHubVault 콘텐츠 분리 준비 |
| R020 | 2026-03-21 | `note-writing.md` 위키링크 필수 규칙 추가 — 새 노트 생성 시 같은 볼트 내 관련 노트 위키링크 1개 이상 강제 |
| R019 | 2026-03-21 | `note-writing.md` H1 제목 규칙 추가 — URI 예약문자·이모지 금지 (강제). 파일명에도 이모지 금지 추가 |
| R018 | 2026-03-21 | `Vaults/Domains_Infra/Search/` 신규 볼트 생성. 검색 엔진·인덱싱·텍스트 매칭 도메인 지식 허브. 루트 CLAUDE.md, _STATUS.md에 등록 |
| R017 | 2026-03-21 | `Vaults/Domains_Infra/CICD/` 신규 볼트 생성. CI/CD 및 배포 동기화 도메인 지식 허브. 루트 CLAUDE.md, _STATUS.md에 등록 |
| R016 | 2026-03-21 | Codex 데스크탑 앱 전환. 루트+11볼트 `AGENTS.md` 신규 생성(2단계 라우팅+실행 구조). playbooks → `.codex/skills/` SKILL.md 형식 변환. `.antigravity/workflows/` → `.codex/skills/`로 이관. `agent-ownership.md` 충돌 방지 규칙 신규. 기존 CODEX.md, .antigravity에 deprecated 표시 |
| R015 | 2026-03-19 | `.claude/rules/vault-routing.md` 신규: 볼트 라우팅 강제 규칙. BasicContentsVault 직접 작업 금지, 콘텐츠 배치 전 볼트 레지스트리 확인 필수 |
| R014 | 2026-03-19 | 에이전트 규칙 정본 참조 방식 전환. Cursor/Codex/Antigravity 복제 규칙 8개 삭제 → `.claude/rules/` 정본 참조 + 에이전트 고유 `agent-rules` 파일로 대체. AIHubVault 내부 Codex/Antigravity 진입점에도 정본 참조 추가 |
| R013 | 2026-03-19 | 루트 `_STATUS.md` 경량 레지스트리 구조로 재설계 (Now/Next/Blocked 제거 → 볼트 타입+콘텐츠+작업 에이전트 날짜). `session-exit.md` 루트 갱신 규칙 개정. 전체 11개 볼트 등록 |
| R012 | 2026-03-18 | `Vaults/Lab_Infra/ObsidianDev/` 신규 볼트 생성. Lab 카테고리 신설 (Domain+Project 복합). 루트 CLAUDE.md Labs 섹션 추가 |
| R011 | 2026-03-18 | `.claude/commands/auto-organize.md` 신규: 노트/볼트 생성 시 폴더 자동 분류 스킬. AI 제안 → 사용자 승인 방식 |
| R010 | 2026-03-18 | `Vaults/Domains_Infra/Notion/` 신규 볼트 생성. 루트 CLAUDE.md 레지스트리 + _STATUS.md 섹션 추가 |
| R009 | 2026-03-18 | `.claude/commands/create-vault.md` 신규: 볼트 생성 스킬. BasicContentsVault 기반 복제 + 후속 작업 프로세스 정의 |
| R008 | 2026-03-18 | `note-writing.md` — 비유적/은유적 표현 금지 규칙 추가. 작업명·제목은 내용을 직접 서술 |
| R007 | 2026-03-17 | `juggl-style-sync.md` — Juggl `local:` 값은 파일명 사용 규칙 명시 (H1 제목 아님) |
| R006 | 2026-03-17 | `note-writing.md` — 마크다운 볼드+괄호 렌더링 규칙 추가. `**텍스트(괄호)**` 패턴 금지 |
| R005 | 2026-03-17 | `README.md` 신규: 멀티볼트 시스템 소개, AI 규칙 체계, 빠른 시작 가이드 (git 배포 진입점) |
| R004 | 2026-03-17 | `.claude/rules/token-optimization.md` 신규: 토큰 절약 및 실행 위임 강제 규칙 |
| R003 | 2026-03-17 | `edit-mode-separation.md`에 루트 레벨 편집 시 `_ROOT_VERSION.md` 기록 강제 규칙 추가 |
| R002 | 2026-03-17 | `.claude/rules/` 신규 3개: temp-file-management, distribution-sync, script-creation-approval |
| R001 | 2026-03-17 | `.antigravity/workflows/` 신규 2개: create-video-note, sync-distribution |
